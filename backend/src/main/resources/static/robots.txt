# 이 도메인은 API 서버입니다. 사람이 읽을 페이지가 없습니다.
#
# **막아 두는 이유가 중복 색인이 아니라 호출입니다.** 화면의 도서관 링크가
# `{API}/api/go/{도서관부호}` 로 나가는 <a> 태그라, 화면을 렌더링하는 크롤러는 그것을
# 따라갑니다. 소장 도서관이 스무 곳이면 링크도 스무 개이고, 그 하나하나가 우리 쪽
# 호출 제한을 깎으면서 도서관 마스터 적재를 건드립니다. `/api/diagnose` 는 더 나쁩니다.
# 열 때마다 정보나루를 한 번 부릅니다.
#
# **다만 /api/libraries 는 열어 둡니다.** 구글은 자바스크립트를 실행해 화면을 그려 보는데,
# 이 호출을 막으면 도서관 목록을 받지 못해 「API 서버에 연결하지 못했습니다」 배너가 뜬
# 화면을 색인하게 됩니다. 그 문장이 검색 결과에 실리는 것이 크롤링 몇 번보다 나쁩니다.
# 이 호출은 서버가 메모리에 담아 둔 목록으로 답하므로 정보나루를 부르지 않습니다.

User-agent: *
Allow: /api/libraries
Disallow: /api/
