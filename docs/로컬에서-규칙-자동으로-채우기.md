# 로컬 클로드 코드에게 시키기 — OPAC 규칙과 상세 패턴 채우기

이 저장소의 원격 세션에서는 **국내 도서관 사이트에 접속할 수 없습니다.** 그리고 예전에
GitHub Actions 러너에서 돌렸을 때도 30묶음 가운데 18묶음이 접속조차 되지 않았습니다.
**국내 회선에서 돌아야 뜻이 있는 일**이라, 로컬에서 도는 클로드 코드에게 넘깁니다.

아래 「넘길 말」을 그대로 붙여 넣으시면 됩니다. 마지막의 「보고 형식」대로 돌아온 답을
원격 세션에 다시 붙여 주시면 그다음 판단(배포 뒤 확인, 회선 문제 여부)을 이어 갑니다.

---

## 무엇이 달라졌는가 (2026-09-10)

목표는 **도서관 이름을 누르면 그 책의 상세 페이지가 뜨는 것**입니다. 그런데 상세 주소는
대부분 도서관 내부 키(`bookkey`, `book_key`, `regNo`)라 `{isbn13}` 자리표로는 만들 수
없고, 실제로 `ISBN_DETAIL` 규칙은 지금까지 0줄입니다. 그래서 길을 하나 더 냈습니다.

| | 예전 | 지금 |
|---|---|---|
| 상세 페이지 | 자리표 규칙(`ISBN_DETAIL`)만. 내부 키면 포기 | **누를 때 서버가 ISBN 검색 결과를 받아 정규식으로 상세 링크를 뽑아 보냅니다**(`DetailResolver`). 정규식은 `detail-patterns.csv` 에 시스템마다 한 줄 |
| 링크에 넣는 ISBN | 저작의 첫 ISBN | **그 도서관이 실제로 가진 판**(`heldIsbns`). 첫 판이 없으면 다음 판으로 한 번 더 |
| 못 찾으면 | — | 검색 결과로 내려가고, 화면 문구가 그 가능성을 미리 말합니다 |
| 조사 도구 | 검색 규칙만 | 상세 패턴까지 만들고 세 겹으로 검증. `--patterns` 는 이미 규칙이 있는 도서관의 상세만 |

**패턴은 서버와 같은 방식으로 검증합니다.** 서버는 자바스크립트를 돌리지 않고 UA 가
`wimb-opac-check/1.0` 입니다. 브라우저에는 자바스크립트가 그린 링크까지 보이지만 서버에는
없으므로, 조사 스크립트는 패턴을 자바스크립트 없는 `fetch` 로 받은 HTML 에 대고 확인합니다.
`scripts/test-opac-browse.py` 가 가짜 OPAC 열한 종류로 이것을 고정합니다. **실제 OPAC 에는
아직 한 번도 대 보지 못했습니다.** 그것이 이번에 로컬에서 할 일입니다.

**서버가 그 OPAC 에 닿는지는 별개입니다.** 패턴은 국내 회선의 브라우저로 확인하는데
서버는 미국 리전 VM 에서 받습니다. 해외 IP 를 막는 OPAC 이면 패턴이 맞아도 검색 결과로
내려앉고, 화면에서는 「패턴이 안 맞는다」와 구별되지 않습니다. 그래서 배포 뒤
`/api/diagnose/opac` 으로 시스템마다 하나씩 확인하는 단계가 마지막에 있습니다.

---

## 넘길 말 (이 아래를 그대로 붙여 넣으세요)

> `where-is-my-book` 저장소에서 도서관 OPAC 의 **상세 패턴**과 검색 주소 규칙을 채워
> 주세요. 목표는 도서관 이름을 누르면 **그 책의 상세 페이지**가 뜨는 것입니다.
>
> 먼저 이 브랜치를 받으세요. 상세 해석기와 패턴 도구가 여기에 있습니다.
>
> ```bash
> git fetch origin && git checkout claude/opac-approach-review-pv05l2
> ```
>
> 그리고 `CLAUDE.md` 의 「OPAC 주소 규칙을 추측해서 넣지 마세요」 절과
> `docs/도서관-주소-규칙-채우기.md` 의 「상세 페이지까지」 절을 읽으세요.
>
> **준비**
>
> ```bash
> pip install playwright && playwright install chromium
> ./scripts/test-opac-browse.py          # 가짜 OPAC 열한 종류. 먼저 이게 통과해야 합니다
> ./scripts/test-opac-discover.py
> ./scripts/opac-discover.py --probe     # 검증용 소장 데이터 (우리 API 를 씁니다)
> ```
>
> **1. 가장 값진 것부터: 이미 검색 규칙이 있는 도서관 217곳(열세 개 시스템)의 상세 패턴**
>
> ```bash
> ./scripts/opac-browse.py --patterns
> ./scripts/opac-browse.py --emit > findings.txt
> ./scripts/opac-discover.py --import-patterns findings.txt \
>     >> backend/src/main/resources/opac/detail-patterns.csv
> cd backend && ./gradlew test && cd ..    # 패턴이 잘못되면 서버가 뜨지 않습니다. 여기서 잡습니다
> ```
>
> `--patterns` 는 `templates.csv` 의 ISBN 검색 주소마다 결과 화면에서 책을 눌러 상세
> 주소를 보고, ISBN 이 그대로 있으면 `ISBN_DETAIL` 을, 내부 키면 두 책의 상세 href
> 공통 앞머리로 `DETAIL_PATTERN` 을 만듭니다. 시스템(호스트)마다 한 줄이면 됩니다.
> `.opac-work/patterns.jsonl` 에 주소마다 결과와 사유가 남습니다.
>
> 패턴이 안 나온 시스템은 사유가 셋 중 하나입니다. **사유별로 세어 보고에 적어 주세요.**
>
> - 「자바스크립트 없이 받으면 그 책이 보이지 않습니다」 — 서버가 링크를 볼 수 없는
>   시스템입니다. 지금 도구로는 안 되고, 나중에 XHR 로 받는 JSON 을 규칙으로 만들어야
>   합니다. 가능하면 그 시스템의 결과 화면이 부르는 XHR 주소(브라우저 개발자 도구의
>   네트워크 탭에서 ISBN 이 들어간 요청)를 **보고에만** 적어 주세요. 규칙으로 넣지 마세요.
> - 「뽑은 상세 링크를 새 창에서 열면 그 책이 나오지 않습니다」 — 세션에 묶인 키입니다.
>   안 됩니다.
> - 「패턴이 그 책보다 다른 링크를 먼저 잡습니다」 — 결과 화면에 같은 모양의 다른 링크가
>   앞에 있습니다. `.opac-work/patterns.jsonl` 의 `examples` 를 보고 사람이 열쇠를
>   `호스트/경로조각` 으로 좁히거나 정규식을 손봐서 `findings.txt` 에 `묶음키 | DETAIL_PATTERN
>   | 열쇠 | 정규식` 줄로 넣을 수 있습니다. **다만 그 정규식도 반드시 `--import-patterns`
>   를 지나게 하고, 세 겹(첫 번째 링크가 그 책, 없는 ISBN 에는 안 잡힘, 새 창에서 열림)을
>   직접 확인한 것만 넣으세요.**
>
> **2. 이미 규칙이 있는 기관에서 빠진 55곳**
>
> ```bash
> ./scripts/opac-browse.py --gaps
> ```
>
> 고양 19 · 안산 16 · 서초 10 · 은평 7 · 순천 2 · 의정부 1 곳입니다. 묶음키가 홈페이지의
> 첫 경로 조각까지 보는 탓에 한 시스템이 둘로 갈렸고, 하필 사람이 많이 가는 큰 도서관만
> 빠졌습니다. `--gaps` 는 그 기관의 기존 규칙 주소를 열어 **빠진 도서관 이름이 검색 화면에
> 실제로 보이는지** 봅니다. 전부 보이면 「넓혀도 됩니다」와 함께 `묶음키*` 줄을 찍어 주니
> 그 줄을 `findings.txt` 에 넣고 `--import-file` 로 반영하세요.
>
> **안산은 조심하세요.** 지금 규칙이 `lib.ansan.go.kr/smalllib/...` 라 작은도서관 전용
> 검색입니다. 시립도서관이 안 보이면 넓히지 말고 시립용 주소를 따로 찾아야 합니다.
>
> **3. 아직 규칙이 없는 묶음**
>
> ```bash
> ./scripts/opac-browse.py --limit 30      # 큰 묶음부터. 중단해도 이어서 됩니다. 상세도 함께 시도합니다
> ./scripts/opac-browse.py --emit > findings.txt
> ./scripts/opac-discover.py --import-file findings.txt \
>     >> backend/src/main/resources/opac/templates.csv
> ./scripts/opac-discover.py --import-patterns findings.txt \
>     >> backend/src/main/resources/opac/detail-patterns.csv
> ```
>
> **4. 확인하고 커밋·푸시**
>
> ```bash
> ./scripts/opac-discover.py --gaps        # 새로 갈린 묶음이 생겼는지
> cd backend && ./gradlew test && cd ..    # 자리표 없는 줄, 깨진 정규식, 겹치는 열쇠는 서버가 안 뜹니다
> git status                               # templates.csv 와 detail-patterns.csv 가 보이는지 (예전에 .gitignore 가 삼켰습니다)
> ```
>
> 커밋 메시지는 저장소의 기존 것들을 보고 맞춰 주세요. 무엇을 확인해서 넣었는지와 몇 곳이
> 늘었는지를 적습니다. 이 브랜치에 그대로 커밋하고 `git push -u origin
> claude/opac-approach-review-pv05l2` 로 올리세요.
>
> **5. 배포된 서버가 실제로 닿는지**
>
> 푸시하면 GitHub Actions 가 이미지를 만들고 VM 이 2분마다 받아 갑니다. `curl -s
> https://where-is-my-book.duckdns.org/api/version` 의 커밋 해시가 방금 푸시한 것으로
> 바뀐 뒤에, **패턴을 넣은 시스템마다 하나씩** 다음을 돌려 주세요. 서버가 미국에 있어서
> 그 OPAC 이 해외 IP 를 막으면 패턴이 맞아도 못 받습니다. 그것을 여기서 갈라냅니다.
>
> ```bash
> curl -s "https://where-is-my-book.duckdns.org/api/diagnose/opac?lib=<도서관부호>&isbn=<그 도서관이 가진 ISBN>"
> ```
>
> 도서관부호와 ISBN 은 `.opac-work/patterns.jsonl` 의 `libCodes` 와 `.opac-work/probe.json`
> 에서 고르면 됩니다. 답의 `status` 가 `RESOLVED` 면 됩니다. `FETCH_FAILED` 면 회선(해외 IP
> 차단)이고, `NO_MATCH` 면 패턴이나 자바스크립트 문제입니다. 이 호출은 그 도서관 서버로
> 요청을 한 번 보내니 시스템마다 한 번씩만 하세요.
>
> **절대 완화하면 안 되는 것들입니다. 통과율을 올리려고 손대지 마세요.**
>
> - **음성 대조를 빼지 마세요.** 검색어와 무관하게 늘 목록을 뿌리는 OPAC 이 전부 「성공」이
>   되어, 어느 책을 눌러도 남의 책이 뜨는 규칙이 들어갑니다.
> - **확인은 쿠키 없는 새 창에서 합니다.** 주소창에는 검색어가 남는데 새 창에서 열면
>   무시하는 OPAC 이 있습니다(제주·구미·용산). 상세 링크도 마찬가지입니다. 같은 창에서
>   확인하면 통과하지만 사용자에게는 죽은 링크가 나갑니다.
> - **패턴 검증을 브라우저로 바꾸지 마세요.** 서버는 자바스크립트를 돌리지 않습니다.
>   브라우저에 보이는 링크로 통과시킨 패턴은 서버에서 잡히지 않습니다.
> - **패턴은 잡을 그룹이 하나여야 하고, 첫 번째로 잡히는 링크가 그 책이어야 합니다.**
>   서버는 첫 번째 것을 씁니다.
> - **`detail-patterns.csv` 와 `templates.csv` 에 손으로 붙이지 마세요.** 반드시
>   `--import-patterns` / `--import-file` 을 지나게 하세요. 정규식의 따옴표를 CSV 규칙으로
>   감싸면 서버가 뜨지 않는데 그 통로가 글자 그대로 씁니다.
> - **`scripts/opac-discover.py` 의 `UA` 와 `DetailResolver.USER_AGENT` 를 다르게 만들지
>   마세요.** 다른 UA 로 확인한 패턴은 서버에서 다른 페이지를 받을 수 있습니다.
> - **양성에 쓰는 ISBN 은 그 도서관이 실제로 소장한 책이어야 합니다.** 아니면 0건이 나왔을
>   때 규칙이 틀린 것인지 그 책이 없는 것인지 구별할 수 없습니다.
> - **주소를 짐작해서 넣지 마세요.** 틀린 규칙은 HTTP 200 을 주면서 결과만 0건이 되어
>   「소장한다더니 그 책이 없네」로 보입니다. 깨진 링크와 달리 눈에 띄지 않습니다.
> - **`_csrf` 같은 세션 토큰이 붙는 주소는 규칙이 될 수 없습니다.** 오늘은 되고 내일은
>   안 됩니다.
> - **같은 호스트에 한 번에 하나씩, 3초 간격을 지키세요**(`--host-delay`). robots.txt 가
>   막은 경로는 건드리지 않습니다. 남의 서버입니다.
> - **정보나루를 직접 부르지 마세요.** 검증용 소장 데이터는 `--probe` 가 우리 API 를 통해
>   받습니다. 인증키는 서버에만 있습니다.
> - **`TITLE_SEARCH` 는 만들지 마세요.** 한글 질의어의 인코딩을 자동으로 틀리면 200 이
>   오면서 결과만 0건이 되어 눈에 띄지 않습니다.
>
> **막히면 그대로 두세요.** 이 프로젝트는 **누락이 오병합보다 낫다**는 규칙으로 돌아갑니다.
> 패턴이 없는 도서관은 검색 결과로, 규칙이 없는 도서관은 홈페이지로 가고 화면이 그 사실을
> 밝힙니다. 그것은 정상 동작이지 고쳐야 할 실패가 아닙니다.
>
> **보고 형식** (끝나면 이 표들을 그대로 답에 넣어 주세요)
>
> 1. 시스템(호스트)마다 한 줄: `호스트 | 도서관 수 | 패턴 찾음/못 찾음 | 사유 | 배포 뒤
>    /api/diagnose/opac 의 status`
> 2. `--gaps` 결과: 기관마다 「넓힘 / 일부만 보임 / 넓히지 않음」과 이유
> 3. 새로 규칙을 얻은 묶음 수와 도서관 수, 못 얻은 묶음의 사유별 수
>    (`./scripts/opac-discover.py --summary` 와 `.opac-work/browse.jsonl` 의 `note`)
> 4. 자바스크립트로 그리는 시스템의 XHR 주소(있으면)
> 5. 커밋 해시와 `git push` 결과, 그리고 `curl -s .../api/status` 의 `opacRuleLibraries`,
>    `opacDetailPatterns` 값

---

## 이 도구가 만드는 것과 만들지 않는 것

**`ISBN_SEARCH`, `ISBN_DETAIL`, `DETAIL_PATTERN` 을 만듭니다.** 검색 결과에서 책을 눌러
들어간 주소에 ISBN 이 그대로 있으면 자리표 규칙(`ISBN_DETAIL`)을 얻고, 내부 키면 두 책의
상세 href 공통 앞머리로 패턴(`DETAIL_PATTERN`)을 만들어 서버와 같은 방식으로 받은 HTML 에
대고 검증합니다. 패턴의 열쇠는 검색 주소의 호스트라 시스템마다 한 줄입니다.

**`TITLE_SEARCH` 는 만들지 않습니다.** ISBN 검색이 안 되는 OPAC 은 제목으로 내려가는 대신
그냥 버립니다. 한글 질의어라 인코딩을 함께 맞춰야 하는데, 그것을 자동으로 틀리면 **200 이
오면서 결과만 0건**이 되어 눈에 띄지 않습니다. 그 자리는 사람이 주소창의 한글이
`%C4%DA...`(EUC-KR)인지 `%EC%BD%94...`(UTF-8)인지 보고 넣는 편이 안전합니다.

**자바스크립트로 결과를 그리는 시스템의 XHR 규칙은 아직 없습니다.** 2026-09-10 조사에서
31묶음 134곳이 이 유형이었습니다. 서버가 링크를 볼 수 없어 패턴이 안 나오고, 그 도서관은
검색 결과까지 갑니다. 다음 단계는 그 시스템이 부르는 JSON 주소를 규칙으로 만드는 것인데,
로컬 조사의 보고(위 4번)가 그 출발점입니다.

## 틀리면 어느 쪽으로 틀리나

**버리는 쪽으로 틀립니다.** 세 겹 검증은 전부 「이게 아니면 버린다」라서, 통과하지 못한
규칙이나 패턴은 들어가지 않고 그 도서관은 지금처럼 검색 결과나 홈페이지로 갑니다. 화면이
그 사실을 밝히므로 사용자를 속이지 않습니다.

실제로 **맞는 규칙을 버리는 경우가 있습니다.** 검증에 쓰는 「그 도서관이 소장한 책」은
정보나루가 알려 준 것인데, 정보나루의 소장 정보와 그 도서관 OPAC 이 어긋날 수 있습니다
(노원에서 21곳과 14곳으로 갈렸습니다). 그러면 규칙이 맞아도 그 책이 0건이라 버려집니다.
**아쉽지만 이 방향의 실수는 안전합니다.**

---

## 결과를 원격 세션으로 가져오려면

로컬에서 브랜치에 커밋해 푸시하시면 됩니다. 바뀌는 파일은 `templates.csv` 와
`detail-patterns.csv` 둘뿐이고, 충돌이 나도 줄 단위라 합치기 쉽습니다.

돌린 뒤 `.opac-work/patterns.jsonl` 과 `.opac-work/browse.jsonl` 에 주소·묶음마다 성공·실패와
사유가 남습니다. 실패 사유별로 세어 보면 다음에 무엇을 손봐야 하는지 보이므로, 위의 「보고
형식」대로 정리해 주시면 됩니다. (저장소에는 커밋하지 않습니다. `.gitignore` 가
`.opac-work/` 를 막고 있습니다.)
