# where-is-my-books

전국 공공도서관 통합 검색 도구입니다. 미리 선택해 둔 도서관들을 대상으로 책을 검색하면 어느 곳에 소장되어 있는지 알려주고, 상세 정보와 대출 신청은 해당 도서관 페이지로 넘깁니다.

전체 설계는 `docs/구현계획.md`에 있고, 무엇이 되고 무엇이 아직 안 되는지는 `README.md`에 있습니다. 이 문서는 그 계획 가운데 **반복해서 어기기 쉬운 제약**만 모아 둔 것입니다.

**`docs/구현계획.md` 는 고치지 마세요. 훅이 편집을 막습니다.** 착수 전에 세운 계획의 기록이라,
지금 코드에 맞춰 고치면 무엇을 예상했고 실제로는 무엇이 달랐는지가 사라집니다. 계획과 갈린
사실은 `docs/가정과-검증상태.md` 에, 지금 지켜야 하는 제약은 이 문서에 적으세요. 그 둘은
일부러 막지 않았습니다. 고치라고 있는 문서입니다. 훅의 전체 구성은 `README.md` 의
「저장소가 스스로 지키는 것」에 있습니다.

**무엇이 검증되었고 무엇이 추측인지는 `docs/가정과-검증상태.md`를 보세요.** API 문서를 열어 보지 못한 채 만든 부분이 있어서, 그것을 확인된 사실처럼 다루면 안 됩니다.

## 현재 상태

검색어를 넣으면 화면에 결과가 그려지는 데까지 이어 놓았습니다.

**실제로 배포되어 돌고 있습니다.** 인증키가 승인되었고, Google Compute Engine 무료 등급 VM
한 대에 API 서버를 올려 Caddy 로 HTTPS 까지 붙였습니다. 도서관 마스터를 실제로 받아 화면에
그리는 것까지 확인했습니다. 절차는 `docs/구글-서버-만들기.md` 에 있습니다.

**서지 응답의 실제 모양을 확인했습니다.** `authors` 는 가정대로 `지음 ; 옮김` 형태로 오지만,
**구분 기호 주변 공백이 제각각입니다.** 같은 책의 다른 판이 서로 다르게 옵니다.

```
"코스모스 :특별판 "              콜론 뒤에 공백이 없고 끝에 공백이 붙습니다
"칼 세이건 지음 ;홍승수 옮김"     세미콜론 뒤에 공백이 없습니다
"칼 세이건 지음;홍승수 옮김"      세미콜론 앞뒤로 없습니다
```

그래서 **KORMARC 구분 기호를 `" : "` 처럼 양쪽 공백으로 찾으면 안 됩니다.** 한쪽에만 공백이
있어도 잡아야 합니다(`(?:\s+:\s*|\s*:\s+)`). 다만 공백을 아예 요구하지 않으면 「입/출력」이나
「10:30」 같은 제목이 쪼개지므로 한쪽은 반드시 요구합니다.

그리고 **판본·각색 표기는 부제에서도 찾아야 합니다.** 부제가 분리되기 시작하면 「데미안 :
청소년판」의 「청소년판」이 표제에서 사라지는데, 표제만 보면 병합 거부 근거를 잃고 그냥
「데미안」과 합쳐집니다. `RealResponseShapeTest` 가 실제로 받은 문자열로 이것을 고정합니다.

키가 활성화되기 전에 받았던 오류 응답 덕분에 설계를 무너뜨릴 뻔한 것을 셋 찾았습니다
(아래 「정보나루 호출 규칙」 참고).

```
서지 → BibNormalizer → WorkMatcher → WorkClusterer → SearchDocBuilder → IndexVerifier → promote_search_doc
검색어 → BookSearchService → 저작 → isbn13_list 전체 → HoldingsLookup → 화면
목록  → LineParser → MultiCheckService → 저작 확정 → (화면 먼저) → 소장을 한 권씩 채움
서가  → ShelfService(있나?) → 없으면 ShelfHarvester → CallNumberOrder 로 정렬 → 조각 파일
        → ShelfStore → 화면 (정보나루를 부르지 않습니다)
찾기  → 수집할 때 적어 둔 find.txt → ShelfFind → 자리 번호 → 화면이 그 자리로 옮겨 감
```

- **`kr.wimb.bib`** — 정규화, ISBN 처리, 저자 대조, 판정 규칙(R0~R7), 군집화, work_id 승계
- **`kr.wimb.index`** — 색인 문서 생성과 전환 전 검증
- **`kr.wimb.holdings`** — 소장 조회. 미소장과 확인 불가를 타입에서부터 구분합니다
- **`kr.wimb.data4library`** — **외부 API 형식을 아는 유일한 곳입니다.** 정보나루의 응답
  구조를 여기 밖으로 새어 나가게 하지 마세요. 가정이 틀려도 피해가 여기서 멈춥니다.
- **`kr.wimb.ingest`** — 어댑터 계약, 호출 예산 원장, HTTP 재시도와 간격 제어
- **`kr.wimb.query`** — 붙여넣은 목록의 줄 해석. **외부 호출이 전혀 없습니다.** 그래야
  목록을 받자마자 화면을 그릴 수 있습니다
- **`kr.wimb.shelf`** — 서가. 청구기호 조립과 정렬 규칙, 장서 수집, 언제 세울지 정하는 곳.
  **정보나루를 부르는 것은 세울 때뿐이고 보는 것은 파일만 읽습니다.** 서가 안에서 책을
  찾는 것(`ShelfFind`)도 세울 때 함께 적어 둔 색인 파일만 읽습니다
- **`kr.wimb.api`** — Spring Boot 컨트롤러, 한 권 검색과 여러 권 검색 서비스
- **`backend/src/main/resources/db/migration`** — 실제 PostgreSQL 에 적용해 제약까지 확인한 스키마
- **`frontend/public/`** — 앱과 따로 도는 정적 파일. 개인정보처리방침(`privacy.html`)과
  공유 미리보기 그림(`og.png`)이 있습니다. 아래 「화면 밖의 정적 페이지」를 보세요.
- **`frontend/`** — 도서관 선택, 한 권 검색, 여러 권 검색, 서가 화면. 도서관 목록은 `/api/libraries` 에서
  받고, **목록을 받지 못하면 `src/data/sampleLibraries.ts` 의 샘플 24곳으로 내려앉되 그 사실을
  화면에 밝힙니다.** 조용히 샘플을 보여 주면 실제 목록이라고 오해합니다.
  **그리고 「서버에 닿지 못했다」(`ApiUnavailable`)와 「서버는 답했지만 목록을 주지
  못했다」(`ApiUnreadable`)를 배너에서 갈라 놓습니다.** 사람이 할 일이 다릅니다. 앞은 서버를
  띄우거나 주소를 고쳐야 하고, 뒤는 정보나루 인증키를 봐야 합니다. 같은 문구로 말하면 멀쩡한
  서버를 다시 띄우게 됩니다.

`BookSearchService` 는 **임시로** 검색할 때마다 정보나루에서 서지를 받아 그 자리에서 묶습니다.
정규화와 저작 묶기는 최종 설계와 같은 코드를 쓰므로, 적재를 돌리기 시작하면 `fetchBooks` 만
색인 조회로 갈아 끼우면 됩니다. **이것을 최종 구조로 착각하고 여기에 기능을 쌓지 마세요.**

- 정규화, ISBN 처리, 저자 대조, 판정 규칙(R0~R7)이 구현되어 있고 테스트가 붙어 있습니다.
- `backend/src/test/resources/bib/golden-pairs.tsv` 가 「같은 책」의 실질적인 명세입니다.
  실제 데이터에서 틀린 사례를 만날 때마다 여기에 한 줄씩 추가하세요.
- 말뭉치 빈도표(`AuthorTokens.DocumentFrequency`)는 아직 없습니다. 군집화를 실제로
  돌려야 만들어지므로 지금은 `ASSUME_COMMON` 을 씁니다. 「J.D. 샐린저」와
  「제롬 데이비드 샐린저」가 갈리는 것이 그 때문이고, 빈도표가 생기면 해결됩니다.

```bash
cd backend && ./gradlew test     # 정규화, 군집화, 색인, 어댑터, 정보나루 응답 파싱
cd backend && ./gradlew bootRun  # API 서버 (http://localhost:8080). D4L_AUTH_KEY 가 필요합니다
cd frontend && npm test          # 선택 상태 계산과 URL 인코딩
cd frontend && npm run dev       # 화면 확인 (http://localhost:5173)
./scripts/verify-schema.sh       # 마이그레이션과 스키마 제약 (루트가 아닌 계정으로)
```

배포는 `docs/배포.md` 를 보세요. **프론트만 Vercel 에 올리고 API 서버는 따로 둡니다.**
정보나루에 등록해야 하는 것은 **나가는(egress) IP** 이고, 서버리스는 그것이 고정되지 않아
조용히 1일 500건 한도가 적용됩니다. 들어오는 IP 와 혼동하지 마세요. VM 한 대를 권하는 이유가
IP 가 하나뿐이라 모호함이 없기 때문입니다. Cloud Run 과 App Engine 도 서버리스라 같은
이유로 안 됩니다.

**서버가 해외에 있어도 느려지지 않습니다.** `ThrottledHttpTransport` 의 `pace()` 가 잠금을
잡지 않은 채 요청만 120ms 간격으로 내보내므로 왕복 시간이 서로 겹칩니다. 30권 확인에서
국내와 미국의 차이가 0.2초입니다. **호출을 `pace()` 의 잠금 안으로 옮기지 마세요.**
그 순간 호출이 직렬화되어 왕복 시간이 그대로 쌓입니다.

**병목은 우리 쪽 간격이 아니라 정보나루의 응답 시간입니다.** 실측으로 `libSrchByBook` 한
번이 **4.9초**, `srchBooks` 한 번이 **3.7초**, `libSrch` 한 쪽이 2초입니다(2026-09-09). 그래서
차례로 부르는 곳이 있으면 그 수만큼 초가 곱해집니다. 스무 권 확인이 넷씩 겹쳐도 **59초**였고
판본 아홉 개짜리 저작 하나가 40초였습니다. 겹칠 수 있는 호출은 전부 겹칩니다.
(ISBN × 지역) 호출(`HoldingsLookup`), 둘째 쪽 이후(`librariesHolding`), 검색의 회전
(`fetchBooks`: 입력 그대로 ∥ 붙여 쓴 표기 → 둘째 쪽 ∥ 저자 되찾기 → 다른 표기들), 화면의
동시 요청(8), 줄 확정(6)입니다. **대신 정보나루에 한꺼번에 나가 있는 요청 수는
`WIMB_DATA4LIBRARY_MAX_IN_FLIGHT`(기본 12)로 묶어 둡니다.** 겹치는 곳이 곱해져 수십 개가
한꺼번에 나가면 안 됩니다. 정보나루가 느려지거나 오류를 주면 이 값을 먼저 줄이세요.
**차례로 부르는 자리를 새로 만들지 마세요.** 그 자리마다 4초씩 붙습니다.

배포 단위는 `backend/Dockerfile` 하나이고, **등록 전에 `curl -s https://api.ipify.org` 로
실제 나가는 주소를 눈으로 확인하는 것까지가 한 묶음**입니다.

**서버는 자기 버전을 말합니다**(`GET /api/version` 이 커밋 해시와 빌드 시각을 줍니다).
VM 이 `latest` 하나만 물어 가므로, 이것이 없으면 화면이 예전 그대로일 때 **「고치다 만 것」인지
「배포가 안 된 것」인지 구별되지 않아 없는 버그를 찾게 됩니다.** 실제로 빌드 셋이 겹쳐
오래된 이미지가 새 이미지를 덮어쓴 적이 있고, 그때 하루치를 추측으로 보냈습니다.
배포 스크립트도 뜬 뒤에 이 값을 기록에 남깁니다. **커밋 해시는 비밀이 아니지만 인증키처럼
실제로 감춰야 하는 값은 절대 여기에 싣지 마세요.**

**빌드에 `concurrency` 를 반드시 두세요.** 없으면 푸시가 겹칠 때 빌드들이 동시에 돌며
셋 다 `latest` 를 밀어 넣고, **먼저 끝난 것이 아니라 나중에 끝난 것이 이깁니다.** 커밋
순서와 무관해서 오래된 코드가 새 코드를 덮어쓴 채 배포됩니다. 실제로 그렇게 되어
권차 수정이 서비스에 나가지 못한 적이 있습니다.

**검색이 0건일 때 `foundBooks` 를 보세요.** 정보나루가 거르기 전에 몇 건을 줬는지입니다.
0이면 정보나루가 못 찾은 것이고, 0이 아닌데 저작이 0이면 우리가 버린 것이고, 저작이 있는데
화면에 없으면 화면 문제입니다. **이 숫자가 없으면 셋을 구별할 방법이 없어 추측하게 되고,
실제로 여러 번 헛짚었습니다.**

**이미지는 GitHub Actions 가 만들고 VM 은 받기만 합니다.** 무료 등급 VM 에서 컴파일하면
10~15분이 걸립니다(기본 CPU 가 코어의 0.25개, 표준 영구 디스크 30GB 에서 쓰기 45 IOPS).
러너에서는 2~3분이고 VM 은 `docker pull` 만 합니다. **이것은 「Actions 에서 정보나루를
부르지 마라」와 다른 이야기입니다.** 그 규칙은 러너 IP 가 고정되지 않아 한도가 500건으로
떨어지기 때문인데, 이미지를 만드는 일은 정보나루를 부르지 않습니다.

**테스트를 통과해야 이미지가 올라갑니다.** VM 이 2분마다 새 이미지를 물어 가므로 깨진
것을 올리면 그대로 서비스에 나갑니다. 자동화가 만든 위험이라 러너에서 막습니다.
`.github/workflows/build-image.yml` 의 테스트 단계를 빼지 마세요.

운영에서 쓰는 환경 변수는 `D4L_AUTH_KEY`(필수), `WIMB_CORS_ALLOWED_ORIGINS`,
`WIMB_DATA4LIBRARY_DAILY_CALL_BUDGET`, `WIMB_OPAC_LINKS_ENABLED` 입니다. 전체 목록과 기본값은
`docs/배포.md` 의 표에, 서버에 그대로 올릴 수 있는 본보기는 `scripts/vm/wimb.env.example` 에
있습니다. 프론트는 `VITE_API_BASE` 로 API 주소를 받는데,
**Vite 의 `VITE_*` 는 빌드할 때 코드에 박히므로 값을 바꾸면 반드시 다시 배포해야 합니다.**
값만 넣고 재배포하지 않으면 번들에 예전 주소가 남아 화면이 바뀌지 않습니다.

**서버 설정을 고치는 것과 반영되는 것은 다릅니다.** 환경 변수는 컨테이너가 뜰 때 한 번
읽으므로 `~/wimb.env` 를 고쳐도 돌고 있는 서버는 예전 값 그대로이고, 그 상태로
`deploy.sh` 를 불러도 **이미지가 그대로면 「새 이미지가 없습니다」 한 줄만 남기고
끝납니다.** 두 단계 모두 오류를 내지 않아서 고친 사람은 반영된 줄 압니다. 그래서
`scripts/vm/set-env.sh` 가 고치기 · `deploy.sh --force` 로 다시 띄우기 · 뜬 컨테이너가 그
키를 들고 있는지 확인하기를 한 묶음으로 합니다. **이 묶음을 쪼개지 마세요.** 그리고
`--env-file` 이 읽는 파일은 셸이 아니므로 **값을 따옴표로 감싸면 따옴표까지 값이
됩니다.** 어느 주소와도 맞지 않게 되는데 서버는 그것을 오류로 보지 않아 화면에서 CORS
로만 드러납니다.

**`WIMB_CORS_ALLOWED_ORIGINS` 에 프리뷰 주소를 별표로 함께 넣으세요.** Vercel 은 프리뷰
배포마다 주소를 새로 만들어서(`where-is-my-book-<해시>-<계정>.vercel.app`) 운영 주소만 적어
두면 프리뷰에서 API 가 통째로 막힙니다. 화면에는 「API 서버에 연결하지 못했습니다」로
보이는데 **서버는 멀쩡하고 주소도 맞습니다.** 그 문구를 믿고 멀쩡한 서버를 다시 띄우게
되는데, 우리가 줄곧 갈라 놓으려는 「서버가 없다」와 「서버는 있는데 물어보지 못했다」가
여기서 뒤바뀝니다. 그래서 `WimbConfiguration` 이 `allowedOrigins` 가 아니라
`allowedOriginPatterns` 를 씁니다(앞은 글자가 그대로 같아야 합니다). 별표를 여는 것이
괜찮은 이유는 이 API 에 인증도 쿠키도 없어서 CORS 가 막아 줄 것이 애초에 없기
때문이고, 남의 몫 쓰기는 주소별 호출 제한이 막습니다. 다만 `https://*.vercel.app` 처럼
넓히지는 마세요. 누가 부를 수 있는지가 목록에서 보여야 합니다.

**`WIMB_SHELF_HARVEST` 를 서버의 환경 변수에 넣지 마세요.** 서가 수집은 다 받고 나서
프로세스를 끝내는 일회성 작업이라(`ShelfHarvestRunner` 가 `System.exit` 을 부릅니다),
서버에 걸어 두면 뜰 때마다 수집하고 그대로 죽습니다. **서비스가 통째로 내려가는데 원인이
설정 파일에 있어** 코드에서 찾게 됩니다. 수집은 따로 뜬 컨테이너가 합니다
(`scripts/vm/harvest-shelf.sh`).

메모리 1GB 짜리 기계(Google 무료 등급 e2-micro)에서 알아 둘 것이 둘 있습니다.

- **힙은 메모리의 50% 입니다**(`JAVA_OPTS`). 75% 로 올리면 JVM 자체가 쓰는 몫이 더해져
  한도를 넘고 OOM 킬러가 컨테이너를 조용히 죽입니다.
- **`docker build` 전에 스왑을 잡아야 합니다.** 안에서 Gradle 이 도는데 컴파일만으로 1GB 를
  넘겨 빌드가 죽습니다.

스키마에서 알아 둘 것:

- `short_id` 를 바꾸려 하면 **트리거가 막습니다.** 규칙을 문서로만 두면 언젠가 어겨집니다.
- 전문 검색 색인은 PGroonga 가 있으면 그것을, 없으면 자체 생성 바이그램을 씁니다.
  `search_text` 와 `search_bigrams` 를 항상 함께 채우므로 전환이 색인 교체만으로 끝납니다.
- 색인 전환은 `promote_search_doc('search_doc_vN')` 하나로 합니다.
  **반드시 검증을 통과한 뒤에만** 부르세요.

다음 작업은 셋입니다. **도서관 링크의 정확도를 확인해 다시 켜기**, **호출 예산 원장을
재배포에도 살아남게 하기**, 그리고 **서가를 실제 도서관으로 한 번 세워 보기**입니다.

마지막 것은 아직 **가짜 장서로만 확인했습니다.** 화면과 정렬은 1,200권짜리 만든
데이터로 돌려 봤지만, 실제 도서관에서 `itemSrch` 를 수백 번 부르는 것은 해 보지
않았습니다. 확인할 것이 셋입니다. **쪽 크기를 500 으로 받아 주는지**(안 받아 주면
호출이 다섯 배입니다), **`pageNo` 를 몇 쪽까지 넘길 수 있는지**(매뉴얼이 말해 주지
않습니다), 그리고 **`callNumbers` 가 실제로 얼마나 채워져 오는지**입니다. 마지막 것이
비면 서가에 세울 책이 없습니다. 작은 도서관의 작은 대주제부터 열어 보세요.

앞의 것은 **지금 꺼 두었습니다**(`wimb.opac.links-enabled`, 기본값은 켜짐이고
`application.yml` 이 꺼 둡니다). 규칙은 다 들어 있는데 실제 OPAC 에서 맞는지 확인하지
못했고, 틀린 규칙은 링크가 깨지는 것보다 나쁩니다. 그 도서관이 HTTP 200 을 주면서 결과만
0건이 되어 「소장한다더니 그 책이 없네」로 보이고, 그러면 소장 정보 자체를 믿지 않게 됩니다.
**확인한 만큼씩 켜세요. 한꺼번에 켜면 어느 규칙이 틀렸는지 알 방법이 없습니다.**

끄는 자리는 `OpacTemplates` 안이고 **`bestFor` 와 `kindFor` 가 아무것도 돌려주지 않게 하는
것이 전부입니다.** 부르는 쪽에서 보면 규칙이 없는 도서관과 똑같아서, 이미 있는 홈페이지
폴백을 그대로 탑니다. **규칙 표를 비우거나 빈 객체로 바꾸지 마세요.** 그러면 셋을 함께
잃습니다. 보완의 기반인 315줄, `/api/status` 의 `opacRuleLibraries` 가 「꺼서 0」인지
「파일을 잃어서 0」인지 가르는 근거(`opacLinksEnabled` 를 함께 내보내는 이유입니다), 그리고
`/api/diagnose/opac` 입니다. 진단은 `asIfEnabled()` 로 스위치를 건너뛰므로 **끈 상태에서도
배포된 서버에서 한 줄씩 확인할 수 있습니다.** 서버가 그 OPAC 에 닿는지는 나가는 IP 에
달려 있어 로컬에서 확인한 것으로는 알 수 없기 때문에 이 통로가 꼭 살아 있어야 합니다.
**화면으로 나가는 경로에서 `asIfEnabled()` 를 부르지 마세요.** 그 순간 스위치가 아무것도
막지 못합니다.

켰을 때의 상태는 이렇습니다. 검색 규칙 표(`templates.csv`)가 315줄(도서관
307곳) 채워져 있고 **상세까지 가는 곳은 49곳**입니다. 도봉·순천·의정부는 누를 때 검색 결과에서
상세 링크를 뽑는 해석기(`DetailResolver`)와 상세 패턴 표(`detail-patterns.csv`, 네 줄)로,
춘천은 상세 주소에 ISBN 이 그대로 들어가는 `ISBN_DETAIL` 로 갑니다. 나머지 258곳은 검색
결과에서 멈춥니다. 제목 링크가 자바스크립트 함수에 열쇠만 넘기는 시스템(포항·은평 등)은
정규식 한 줄로 주소를 뽑을 수 없고, 결과를 자바스크립트로 그리는 시스템(노원·부천 등)은
서버가 링크를 볼 수 없어서입니다. 채우는 절차는 `docs/로컬에서-규칙-자동으로-채우기.md` 에
있습니다. 뒤의 것은 `InMemoryApiBudget` 이라 컨테이너가 새로 뜰 때마다 그날 쓴 건수가 0으로
돌아갑니다.

(`authors` 필드의 실제 표기는 확인했습니다. 위의 「현재 상태」를 보세요.)

## 구조를 한 줄로

**서지는 미리 색인하고, 소장은 조회할 때 가져와 캐시에 쌓습니다.** 이 두 경로를 혼동하지 마세요.

- 서지 색인을 두는 이유는 호출량 절감이 아니라 검색 품질 통제입니다. 그리고 한 저작에 묶인 ISBN 전체를 알아야 소장 조회가 정확해지기 때문입니다.
- 소장 데이터를 미리 적재하지 않는 이유는 1,619개관 벌크 적재가 1억 2천만 행에 이르는 데 비해, 조회 시점 호출이 호출 한도 안에서 충분히 감당되기 때문입니다.

---

## 데이터 소스에 관한 제약

### 사용할 수 없는 소스

아래는 실제로 종료되었습니다. 학습 데이터를 근거로 다시 제안하지 마세요.

- **알라딘 OpenAPI**: 신규 키 발급 2026-09-04 종료, 서비스 완전 종료 2026-10-30
- **네이버 책 검색 API**: 2026-07-31 종료, 공식 대체 없음
- **교보문고**: 공식 개발자 API 없음

예스24 Open API와 카카오 책 검색 API는 정상 운영 중이지만 1차 범위 밖입니다. 어댑터 자리만 열어 두고 지금은 구현하지 않습니다.

### 정보나루 호출 규칙

매뉴얼 원본이 `docs/reference/정보나루-Open-API-Manual-v20260210.pdf` 에 있습니다. 항목 이름이나 파라미터가 헷갈리면 추측하지 말고 이걸 보세요.

- **`libSrchByBook` 의 `region` 은 필수입니다(13절).** 전국을 한 번에 받는 방법이 없습니다. 소장 조회는 선택한 도서관들이 걸친 **시도 수만큼 호출이 곱해집니다.** 30권 × 판본 3개 × 시도 2곳 = 180회입니다.
- **`libSrch` 가 위경도까지 줍니다(1절).** 그래서 공공데이터포털의 전국도서관표준데이터를 이름과 주소로 대조할 필요가 없습니다. 계획서가 가장 큰 위험으로 꼽았던 단계가 사라졌습니다.
- **정보나루 하나로 1차 범위가 채워집니다.** 도서관 마스터는 `libSrch`, 서지는 `srchBooks`, 소장은 `libSrchByBook` 입니다. 국립중앙도서관 서지정보 API와 공공데이터포털은 서지 품질 보강용으로 나중에 붙입니다.
- **1일 호출 한도는 30,000건이고, IP 등록이 전제 조건입니다.** 등록하지 않으면 500건입니다. 조회는 반드시 등록된 IP를 가진 서버에서 나가야 합니다.
- **GitHub Actions에서 정보나루를 호출하지 마세요.** 러너의 IP는 고정되지 않아 조용히 500건 한도가 적용됩니다. 예약 트리거로만 씁니다.
- `libSrchByBook`은 한 번 호출로 그 지역의 소장 도서관을 돌려줍니다. **(책 × 도서관)으로 호출하지 마세요.** 도서관 수가 아니라 시도 수만 곱합니다.
- **소장 조회는 `numFound` 를 보고 끝까지 넘겨 받아야 합니다.** 첫 쪽만 보고 끝내면 뒤쪽
  도서관이 통째로 빠진 채 답이 나가고, 빠진 도서관은 「그 도서관에는 없다」로, 그것도
  **「빠짐없이 확인했다」는 표시를 달고** 나갑니다. 실제로 소장한 책을 없다고 답하는
  것입니다. 더 나쁜 것은 **소장 도서관이 많은 책일수록 심해진다**는 점이라, 가장 자주
  찾는 책에서 가장 자주 틀립니다. `pageSize` 를 크게 주는 것으로는 해결되지 않습니다.
  서버가 그 값을 그대로 받아 준다는 보장이 없고 더 작게 잘라 줘도 우리는 알 방법이 없어서,
  `numFound` 가 없으면 첫 쪽에 실제로 온 건수를 한 쪽 분량으로 삼습니다.
- **소장 조회는 저작의 ISBN 전체(`search_doc.isbn13_list`)를 대상으로 합니다.** 판본 하나만 조회하면 도서관이 다른 판을 가지고 있어도 미소장으로 나옵니다.
  판본이 상한(20)을 넘으면 **거절하지 말고 앞의 것만 묻되 `complete` 를 내립니다.** 예전에는
  400 으로 거절했고 화면은 그것을 「확인 불가」로 그려서, 판본이 많은 책일수록 아무것도
  알려 주지 못했습니다.
- **(ISBN, 지역) 답은 프로세스 안에서 6시간 기억합니다**(`CachingHoldingsClient`). 도서관을
  하나 더 고르고 「확인」을 다시 누르는 것이 가장 흔한 동작인데, 그때마다 스무 권을 다시
  물으면 사람이 몇 초를 다시 기다리고 예산도 샙니다. **실패는 기억하지 않고, 빈 답은
  기억합니다.** 그리고 **답을 받은 시각을 그대로 들고 다녀 화면의 「조회 기준」 날짜가 그
  시각에서 나옵니다.** 캐시가 이것을 지금 시각으로 바꿔 말하면 옛 답이 새 답처럼 읽힙니다.
  여러 답이 섞이면 가장 오래된 날짜를 씁니다.
- **소장 조회의 `region` 은 정보나루가 시도별 `libSrch` 목록에서 알려 준 소속을 씁니다.**
  도서관 마스터를 전국 한 번에 받지 않고 **시도마다 따로 받는 이유가 이것입니다.**
  `libSrchByBook` 은 정보나루가 그 도서관에 붙여 둔 지역으로 걸러지는데, 예전에는 주소
  앞머리에서 시도를 읽었습니다. 그 둘이 어긋나면 **그 도서관은 물어보지도 않은 채
  「없음」으로 나가고**, 「고양시립」 두 곳처럼 주소에서 시도를 못 읽는 곳은 확인 불가로만
  답할 수 있었습니다. 시도별 목록에 들어온 시도가 곧 소속이라 그 어긋남이 사라집니다.
  주소 읽기는 소속이 없을 때의 예비입니다(`WimbController.regionFor`). **전국 한 번에 받기로
  되돌리지 마세요.** 호출이 열일곱 번 대신 여섯 번으로 줄지만 소속을 잃습니다.
- **고른 도서관 가운데 시도를 알아내지 못한 곳이 있으면 `complete` 로 답하지 마세요.**
  소속도 없고 주소에서도 시도를 못 읽으면 **그 도서관은 조회 대상에서 아예 빠집니다.**
  빠진 도서관은 결과 목록에 있을 수 없으므로 그대로 「그 도서관에는 없다」로 나갑니다.
  물어보지 않고 없다고 답하는 것이라, 실제로 소장한 책을 놓칩니다. 전부 못 물어보는
  경우는 `holdingsOf` 가 걸러 내지만 **일부만 빠지는 쪽이 눈에 띄지 않아 더 위험합니다.**
  `unaskableCount` 로 세어서 확인 불가로 넘깁니다.
- **원장이 재배포마다 0으로 돌아갑니다.** 운영이 아직 `InMemoryApiBudget` 을 쓰고 있어서,
  컨테이너가 새로 뜨면 그날 쓴 건수가 사라집니다. 실제로 검색을 수십 번 한 날에
  `/api/diagnose` 가 8건이라고 답했습니다. **남은 건수를 근거로 삼지 마세요.** 그리고
  한도의 80% 에서 배경 갱신을 멈추는 보호가 이 상태에서는 걸리지 않습니다.
- 호출 예산 원장(`api_budget`)을 거치지 않는 호출 경로를 만들지 마세요. 한도의 80%에 도달하면 배경 갱신을 멈추고, **사람이 기다리는 요청을 배경 작업보다 우선합니다.**
- **`bookExist` 는 사용자가 그 도서관을 눌렀을 때만 부릅니다.** (도서관 × ISBN)이라
  목록에 달면 호출이 폭발합니다. 아래 "대출 상태" 항목을 보세요.
- **정보나루는 오류도 HTTP 200 으로 돌려줍니다.** 본문에 `errCode` 와 `error` 가 들어옵니다
  (`authErr` 인증정보 불일치, `vitalizationErr` 키 미활성). **오류 본문에는 `lib` 도 `doc` 도
  없으므로, 상태 코드만 보고 넘기면 빈 목록이 되어 화면에 "미소장"으로 나갑니다.**
  `Data4LibraryResponse.errorOf` 가 이것을 걸러 `ApiErrorException` 을 던집니다.
  이 검사를 우회하는 호출 경로를 만들지 마세요.
- **오류 형식이 엔드포인트마다 다릅니다.** 실제로 `libSrch` 는 XML, `srchBooks` 는 JSON 으로
  돌려주었습니다. 그래서 오류 검사는 두 형태를 모두 봅니다.
- **질의 문자열의 공백은 `%20` 으로 보냅니다. `+` 로 보내면 안 됩니다.**
  자바의 `URLEncoder` 는 공백을 `+` 로 바꾸는데, 그것은 HTML 폼 본문의 규칙이고 질의
  문자열에서 `+` 를 공백으로 되돌려 주는 것은 서버 마음입니다. 되돌리지 않는 서버에서는
  `title=마의+산` 이 **「마의+산」이라는 글자를 그대로 찾는 검색**이 되어 0건이 나옵니다.
  사용자에게는 「그런 책이 없다」로 보이고, **오류도 로그도 남지 않으며 제목에 공백이 없는
  책은 멀쩡히 나오기 때문에** 검색이 고장 났다고 의심하기도 어렵습니다. 「코스모스」는
  되는데 「마의 산」은 안 되는 식입니다. `Data4LibraryClient.encode` 가 되돌려 놓습니다.
- **HTTP 헤더 값에 한글을 넣지 마세요.** 헤더는 ASCII 만 허용하므로 자바가 요청을 만들다가
  예외를 던지고, 요청이 나가지도 않은 채 실패합니다. 화면에는 "확인 불가"로만 보여서
  정보나루 장애와 구별되지 않고, 예산은 이미 깎인 뒤입니다. `WimbConfigurationTest` 가
  실제로 `HttpRequest` 를 만들어 보며 이것을 잡습니다.
- **고장이 스스로를 키우지 않게 하세요.** 도서관 마스터를 못 받으면 목록이 비는데, 예전에는
  **목록이 비어 있을 때 재시도 빗장을 건너뛰었습니다.** 정보나루가 답하지 않는 동안에는
  목록이 늘 비어 있으므로 아무도 기다리지 않고, 화면을 한 번 열 때마다 전국 한 번 + 시도
  열일곱 번을 다시 부릅니다. **가장 안 될 때 가장 많이 부르는 셈**이고, 그 헛호출이 하루
  한도를 갉아먹어 고장을 늘립니다. 실제로 이렇게 하루 1,410건을 썼습니다. 비어 있는 쪽이
  오히려 더 기다려야 합니다.
- **인증 계열 오류(`authErr`, `vitalizationErr`, `outOflimit`)를 만나면 5분간 부르지 않습니다.** 사람이
  고쳐야 낫는 상태라 재시도해도 성공하지 않는데, 여러 권 검색은 한 번에 수십 번을 부릅니다.
  이 차단을 없애면 실패할 것이 뻔한 요청으로 남의 서버를 수십 번 두드리게 됩니다.
  **`outOflimit` 을 여기에 넣지 않으면 안 됩니다.** 「잠시 뒤 다시」로 읽히지만 실제 뜻은
  「등록한 IP 로 나가고 있지 않다」일 때가 많고, 어느 쪽이든 사람이 고치거나 날이 바뀌기
  전에는 낫지 않습니다.
- **실패한 이유를 화면까지 실어 보내세요**(`ApiErrorAdvice`). 정보나루는 무엇이 잘못됐는지
  한국어로 또박또박 알려 줍니다. 「1일 500건 이상 요청 시 IP 등록이 필요합니다」처럼 사람이
  바로 손쓸 수 있는 문장입니다. 그런데 Spring 의 기본 오류 본문은
  `server.error.include-message` 가 `never` 라 이유를 싣지 않아서, 화면에는 **「API 오류
  503」**만 떴습니다. 그래서 「검색이 안 된다」는 말만 남고 원인을 코드에서 찾게 됩니다.
  **하루를 그렇게 헛짚었습니다.** 서버가 이미 알고 있는 답을 화면이 버리면 그다음은 전부
  추측입니다. 정보나루의 `errCode` 도 함께 내보내 화면이 코드별로 할 일을 갈라 말할 수
  있게 합니다. **인증키는 절대 싣지 마세요.**
- **다만 그 문구의 독자는 방문자입니다.** 위의 규칙대로 이유를 실어 보냈더니, 그 문장이
  **운영자 한 사람을 위해 쓴 것**이었습니다. 그때는 화면을 보는 사람이 우리뿐이라 맞는
  선택이었는데, 주소를 공개하면 독자가 바뀝니다. 처음 온 사람에게 「인증키를 확인해
  주세요」나 「나가는 IP 가 등록한 것과 같은지 확인해 주세요」는 **아무 뜻도 없고**, 자기가
  뭘 잘못했나 싶게 만들거나 그대로 갈무리되어 돌아다닙니다. 그래서 응답에 둘을 함께
  싣습니다. `reason` 은 **방문자용**이라 화면이 그리는 것은 이것뿐이고, `detail` 은
  **운영자용**이라 정보나루 원문과 무엇을 봐야 하는지가 들어갑니다. **화면에 `detail` 을
  그리지 마세요. 버리지도 마세요.** 운영자가 실제로 쓰는 통로는 `/api/diagnose` 입니다.
  방문자 문구는 셋을 갈라 말합니다. 오늘 몫을 다 쓴 것은 **내일이면 낫고**, 인증 계열은
  **기다려도 낫지 않으며**(우리가 고쳐야 합니다), 나머지는 **잠시 뒤에 다시 하면 됩니다.**
  그리고 **어느 경우에도 「책이 없다」로 읽히면 안 됩니다.** 조회에 실패한 화면과 소장한
  곳이 없는 화면은 사용자에게 똑같이 「안 나온다」로 보이는데, 그 둘을 갈라 놓는 것이 이
  서비스가 존재하는 이유입니다. `ApiErrorAdviceTest` 가 운영자 어휘가 방문자 문구로 새어
  나가는 것을 잡습니다.
- **5xx 는 우리 사정이라 내부 문구가 섞입니다.** `/api/libraries` 는 정보나루 예외의
  `getMessage()` 를 그대로 실어 「정보나루 srchBooks 가 오류를 돌려주었습니다: … 
  (outOflimit)」 같은 문장을 만듭니다. 4xx 는 요청이 잘못된 것이라 우리가 쓴 문구가 곧
  방문자에게 할 말이지만(「검색 조건이 비어 있습니다」), 5xx 는 그렇지 않습니다.
- **`outOflimit` 은 「너무 많이 불렀다」가 아니라 「등록한 IP 로 나가고 있지 않다」는 뜻일
  때가 많습니다.** 등록하지 않은 IP 는 하루 500건이라 금방 닿습니다. **GCE 무료 등급 VM 은
  기본이 임시 외부 IP 라 다시 띄우면 주소가 바뀌고,** 그 순간 등록이 어긋나 조용히 500건
  한도로 떨어집니다. 증상은 「갑자기 아무것도 안 나온다」입니다. `curl -s
  https://api.ipify.org` 로 실제 나가는 주소를 확인하고 등록된 것과 맞춰 보세요.
  **`GET /api/diagnose` 가 이것을 한 번에 답합니다.** 서버가 자기 나가는 IP 를 말하고,
  정보나루를 실제로 한 번 불러 보고, 못 부르면 `errCode` 와 이유를 그대로 내놓습니다.
  **「우리 예산이 남았다」와 「정보나루가 답한다」는 다릅니다.** 원장은 우리가 센 숫자일
  뿐이라, IP 등록이 어긋나 있으면 원장이 아무리 넉넉해도 한 건도 못 받습니다. 실제로 그
  상태였는데 원장만 보고 있었습니다. 이 화면은 열 때마다 호출을 한 번 쓰므로 자동으로
  새로 고치지 마세요.
- **정보나루가 답하지 않을 때 500 을 내지 마세요.** 500 은 "우리 서버가 고장"이라는 뜻입니다.
  실제로는 물어보지 못한 것이므로 `/api/holdings` 는 `unreadable: true` 로, 나머지는 503 과
  이유로 답합니다. 화면이 그것을 "결과 없음"이 아니라 "확인 불가"로 그려야 합니다.

### 서지정보 API 호출 규칙

**ISBN을 하나씩 조회하지 마세요.** `start_publish_date`와 `end_publish_date`로 월 단위 구간을 나눠 `page_size=500`으로 훑습니다. 2000년 이후 약 150만 건이 3,000회 요청으로 끝납니다.

---

## 제품 결정

### "미소장"과 "확인 불가"를 절대 섞지 마세요

소장 조회는 외부 호출이라 실패할 수 있습니다. 실패를 "미소장"으로 표시하면 **실제로 있는 책을 없다고 답하게 되어 헛걸음을 만듭니다.** 이 도구의 존재 이유가 무너지는 지점입니다.

- 판본 하나의 조회가 실패하면 그 판본만 빼고 나머지로 답하되, "일부 판본을 확인하지 못했습니다"를 표시합니다.
- 정보나루가 응답하지 않으면 캐시에 있는 책만 답하고, 나머지는 "확인 불가"로 둡니다.
- 호출 예산이 소진되어도 마찬가지입니다.

화면은 **네 상태**를 구분하고, 판정은 `frontend/src/domain/holdingState.ts` 한 곳에서만 합니다.
화면마다 따로 판정하면 언젠가 한쪽이 확인 불가를 미소장으로 그리게 되고, 그때는 아무도
눈치채지 못합니다. 화면을 늘릴 때 이 구분을 세 가지로 줄이지 마세요.

| 상태 | 조건 | 문구 |
|---|---|---|
| 확인하지 않음 | 고른 도서관이 없음 | "도서관을 고르면 어디에 있는지 확인합니다" |
| 확인 불가 | 조회 실패, 또는 확인하지 못한 판본이 있는데 찾은 곳도 없음 | "확인 불가" |
| 소장 | 찾은 곳이 있음 | "n곳에 있습니다" |
| 미소장 | **빠짐없이 확인했는데** 찾은 곳이 없음 | "고른 도서관에는 없습니다" |

**미소장이라고 말할 수 있는 것은 마지막 한 줄뿐입니다.** 그리고 노란 주의 상자는 "확인
불가"에만 씁니다. 안내까지 같은 색으로 칠하면 정작 확인하지 못한 책이 눈에 띄지 않습니다.

### 소장은 `/api/holdings` 한 곳에서만 답합니다

**한 권 검색도 여러 권 검색도 마찬가지입니다.** 목록을 먼저 돌려주고, 소장은 책마다 따로
물어 도착하는 대로 채웁니다. 한 번에 다 계산해서 돌려주면 수십 초 동안 빈 화면이 됩니다.

실제로 `/api/search` 가 저작마다 `libSrchByBook` 을 돌리고 있었습니다. 저작이 200개면
호출도 200번이고 요청 간격이 120ms 라 **그것만으로 24초**였습니다. 사용자는 그것을
「검색이 안 된다」로 읽습니다. **여기에 소장 조회를 다시 넣지 마세요.**

그리고 검색 응답에 소장 항목을 두지 않는 것도 같은 이유입니다. 두면 「물어본 적 없음」과
「물어봤는데 없음」이 똑같은 빈 목록으로 나가고, 화면이 그것을 「고른 도서관에는
없습니다」로 그립니다. **응답 모양에서부터 그럴 수 없게 해 두었습니다.**

- `GET /api/search` 는 조건으로 책을 찾기만 합니다. **소장 조회를 부르지 마세요.**
  **제목(`q`)·저자(`author`)·출판사(`publisher`)를 각각 받아 정보나루에 그대로 넘깁니다.**
  한 칸에 다 넣고 우리가 쪼개면 「김영하 알쓸신잡」 같은 입력에서 0건이 나오고, 사용자는
  그것을 「이 책이 없다」로 읽습니다. 셋 다 비면 400 입니다. 조건이 없으면 정보나루가
  전체 대출데이터를 훑어서 아무 뜻도 없는 결과에 호출만 씁니다.
  결과는 제목 일치도로 세워 **위에서 100개까지** 돌려주고, **화면이 20개씩 나눠 보여 주며
  펼친 것만 소장을 물어봅니다.** 예전에 20개로 잘랐던 것은 화면이 받은 것을 전부 그리고
  저작마다 소장을 물어봤기 때문인데, 이제 그 숫자와 조회 횟수가 분리되었습니다.
  정렬은 `BibNormalizer.normalizeKey` 를 씁니다. 적재·군집화와 같은 함수를 써야 검색에서만
  어긋나지 않습니다. 제목 없이 저자나 출판사로만 찾으면 정보나루가 준 순서를 흔들지 않습니다.
- **ISBN 을 판별할 수 없어 뺀 자료는 몇 건인지 세어서 화면까지 올립니다**(`droppedNoIsbn`).
  소장 조회가 ISBN 으로만 되므로 빼는 것 자체는 맞지만, 조용히 빼면 사용자는 그것을
  「그런 책이 없다」로 읽습니다. 찾던 책이 하필 그 자료였을 때 아무 단서도 없이 사라집니다.
- **`keyword` 를 제목 검색에 쓰지 마세요.** 매뉴얼 16절이 `title` 과 별개 항목으로 두고 있어
  제목 검색의 대체처럼 보이지만, 실제로 불러 보니 **주제어 검색이었습니다.**
  `keyword=레미제라블` 은 47건을 주면서 그 안에 「레미제라블」이라는 책이 한 권도 없고,
  `keyword=마의 산` 도 마찬가지입니다. 세미콜론으로 나누는 `레;미제라블` 은 0건입니다.
- **`title` 은 어절의 앞에서부터 맞춥니다.** 「미제라블」은 「레 미제라블」을 찾지만
  「제라블」과 「레 미제」는 0건이고, 「마의」는 「마의 산」을 찾지만 「의 산」은 0건입니다.
  **그래서 「레미제라블」로는 「레 미제라블」 낱권이 한 건도 걸리지 않습니다.**
  민음사 세트만 붙여 쓴 표기라 세트만 나오고, 사용자는 그것을 「낱권이 없다」로 읽습니다.
  공백을 넣을 자리를 우리가 알 방법이 없으므로 **띄어쓰기를 달리해 다시 찾는 것으로는
  풀리지 않습니다.** 자세한 사례는 `docs/가정과-검증상태.md` 2-4 에 있습니다.
- **제목으로 찾을 때는 저자로 한 번 더 찾습니다**(`recoverByAuthor`). 위의 어절 규칙 때문에
  「레미제라블」은 「레 미제라블」 낱권을 한 권도 찾지 못하는데, 공백을 어디에 넣을지는
  우리가 알 수 없습니다. 대신 첫 검색으로 **저자는 알게 되므로** 그것으로 되찾고, 표제
  정규화 키가 맞는 것만 더합니다. 정규화가 공백을 지우므로 「레미제라블」과 「레 미제라블」의
  키가 같아지고, 딸려 온 「파리의 노트르담」은 걸러집니다. 실측에서 `foundBooks` 가 49에서
  141로 늘고 민음사 낱권 1~5권이 전부 들어왔습니다.
  **조건을 두 번 잘못 걸었고 둘 다 실측이 잡았습니다.** 처음에 「제목이 맞은 책이 하나도
  없을 때」로 걸었다가 한 번도 발동하지 못했습니다. 「레미제라블」은 그 제목의 책이 열다섯
  개나 나오는데, 없는 것은 그 제목의 책이 아니라 띄어 쓴 표기의 판이었기 때문입니다.
  다음에 「붙여 쓴 질의일 때만」으로 걸었더니 반쪽만 고쳐졌습니다. 「레 미제라블」로 찾으면
  붙여 쓴 판이 **0건**이었습니다. **못 찾는 것은 양쪽 방향 모두이므로 표기를 가리지
  마세요.** 저자를 알아내지 못하면 부르지 않으므로 0건 검색에서는 호출이 늘지 않습니다.
- **정보나루의 도서 검색은 음반과 영상물도 함께 돌려줍니다.** 「해리포터」로 찾으면
  워너브라더스 DVD 가 열여섯 건, 「레미제라블」로 찾으면 유니버설픽쳐스 블루레이와 OST 가
  열다섯 건 섞여 옵니다. 이 값들은 `880` 으로 시작하는데 **대한민국 EAN 국가 코드이지
  ISBN 이 아닙니다**(`Isbn.isNotABookNumber`). **이것을 「ISBN 을 판별하지 못한 책」과 같이
  세지 마세요.** 화면이 「DVD 가 열여섯 건 빠졌습니다」라고 알리게 되는데, 도움이 되지 않고
  불안만 만듭니다. 갈라 놓은 뒤 `droppedNoIsbn` 이 「해리포터」 16→2, 「레미제라블」 15→0,
  「코스모스」 6→1 로 줄었습니다.
- **그런데 진짜 책인데 ISBN 에 오타가 난 것이 실제로 있습니다.** 「칼세이건
  코스모스」(사이언스북스)의 `9788983711893` 은 체크디지트가 맞지 않습니다(정답은 끝자리 2).
  「토지」(나남)의 `9787893007504` 는 접두어까지 중국 것입니다. 소장을 물어볼 수는 없지만
  **사용자가 찾던 바로 그 책일 수 있으므로 표제와 출판사를 화면까지 올립니다.**
- **정보나루 검색은 넣은 글자를 그대로 찾습니다.** 우리 정규화(`normalizeKey`)는 받아온
  뒤 순위를 매길 때만 쓰이므로 검색 자체에는 아무 영향이 없습니다. 그래서 「마의 산」과
  「마의산」이 서로 다른 검색이 되고, 도서관마다 표기가 갈리는 한국어 서명에서는 한쪽으로만
  찾으면 멀쩡히 있는 책을 놓칩니다. 사용자에게는 그것이 **「그런 책이 없다」로 보입니다.**
- **띄어쓰기가 다른 검색은 같은 결과를 받아야 합니다.** 그래서 `fetchBooks` 가 정규화 키가
  같은 표기를 **모두** 찾아 합칩니다. 입력 그대로 → 띄어쓰기를 뺀 표기(입력에 공백이 있으면
  늘, 0건일 때만이 아닙니다) → 저자로 되찾기 → 그렇게 모인 결과에서 **글자는 같고
  띄어쓰기만 다른 표기**(`BibNormalizer.spellingKey`)를 발견하면 그 표기로도 찾습니다(상한 2).
  「레미제라블」로 시작해도 되찾기가 「레 미제라블」을 보여 주면 그 표기로 한 번 더 찾아,
  「레 미제라블」로 시작한 사람과 같은 목록을 받습니다. 함께 찾은 표기는 `alsoSearchedTitles`
  로 화면까지 올려 밝힙니다. **같은 자료는 ISBN 으로 한 번만 셉니다.** 예전에 「0건일 때만
  띄어쓰기를 뺀다」로 걸었더니 세트 한 건이 걸려 0건이 아닌 「레미제라블」에서 발동하지
  않았습니다. 조건을 결과의 많고 적음에 걸지 마세요.
- **저자도 띄어쓰기가 다르면 다른 검색입니다.** 「칼 세이건」과 「칼세이건」이 서로 다른
  검색이라 저자 칸에 띄어쓰기를 달리 넣으면 **결과가 아예 없었습니다.** 제목보다 나쁜 것은
  저자만 넣은 검색에는 되찾을 실마리가 될 제목조차 없다는 점입니다. 그래서 `fetchBooks` 가
  저자 표기도 함께 돌립니다. 띄어쓰기를 뺀 저자 ∥ 제목이 있으면 **저자 없이 제목만으로 찾아
  공백과 구두점을 지운 저자 표기가 맞는 것만 더하기**(`addMatchingAuthor`) ∥ 저자만 **붙여 쓴
  한글 4~8음절**이면 띄어쓰기 자리를 하나씩 옮긴 표기를 전부 물어보기(`spacedAuthorGuesses`,
  「칼세이건」→「칼 세이건」「칼세 이건」「칼세이 건」) → 결과에서 본 다른 저자 표기(상한 2).
  전부 1회전과 3회전에 겹쳐 나가므로 걸리는 시간은 늘지 않습니다. **자리 옮기기를 「0건일
  때만」으로 걸지 마세요.** 붙여 쓴 표기로 등록된 판이 한 건만 있어도 발동하지 않게 되는데,
  제목에서 같은 조건을 두 번 잘못 걸었습니다. 조건은 질의의 모양에 겁니다. 화면에는
  **책을 데려온 표기만** `alsoSearchedAuthors` 로 밝힙니다. 자리를 옮긴 표기는 이름 길이만큼
  있어서 전부 늘어놓으면 아무것도 못 찾은 것까지 화면에 나갑니다.
- **띄어쓰기를 다루는 자리는 제목·저자·출판사 셋 다입니다.** 하나라도 빠지면 그 칸으로
  검색한 사람에게 **「그런 책이 없다」로 보입니다.** 어긋나는 방향이 둘이라는 것을 먼저
  보세요. ① 사용자가 공백을 넣었는데 등록은 붙여 쓴 것, ② 사용자가 붙여 썼는데 등록은
  띄어 쓴 것입니다. **①은 쉽고 ②가 어렵습니다.** 공백을 어디에 넣을지 우리가 알 수
  없기 때문입니다. 장치가 셋이고 셋 다 셋 필드에 걸쳐 있어야 합니다.
  **① 붙여 쓴 표기로도 함께 묻기**(`withoutSpaces`): 제목·저자·출판사 전부.
  **② 제목만으로 받아 와 우리가 거르기**(`addMatching`): 저자와 출판사를 **둘 다 떼고**
  물어야 합니다. 하나라도 질의에 남겨 두면 그 표기가 틀렸을 때 여기서도 0건이 옵니다.
  우리 키(`spellingKey`, `normalizePublisher`)가 공백을 지우므로 **이 하나로 ①과 ②가
  함께 풀립니다.** 제목이 있을 때 쓸 수 있는 가장 좋은 길입니다.
  **③ 띄어쓰기 자리 옮기기**(`spacedGuesses`): ②를 쓸 수 없을 때만입니다. 제목이 0건이면
  저자를 알 수 없어 되찾기도 다른 표기 찾기도 **구조적으로 발동하지 못합니다.** 그 자리에만
  씁니다. 자리를 옮겨 물을 때도 **다른 조건은 떼고 묻고 우리가 거릅니다.**
  **견주기가 안전하다고 질의까지 안전한 것으로 착각하지 마세요.** `normalizePublisher` 가
  공백을 지우니 괜찮다고 보고 출판사 회전을 통째로 빠뜨렸습니다. 견주는 것은 받아 온
  값이고, 질의는 사용자가 적은 표기 그대로 나갑니다.
  함께 찾은 출판사 표기는 화면에 밝히지 않습니다. 출판사는 사용자가 직접 적어 넣은
  조건이라 목록의 출판사 열에 그대로 보입니다.
- **자리 옮기기는 0건일 때만, 붙여 쓴 한글일 때만, 옮길 자리 수만큼만 부릅니다.**
  **책을 찾은 검색에는 한 건도 붙지 않습니다.** 예전에는 0건 검색에 호출을 한 건도 더
  쓰지 않는 쪽이었는데, 그 상태에서는 「마의산」처럼 붙여 쓴 제목이 **영영 0건**이었습니다.
  0건인 사람은 어차피 빈 화면을 받으므로 한 회전을 더 기다릴 값이 있습니다.
  **조건을 「결과가 적을 때」로 넓히지 마세요.** 한 건이라도 받으면 되찾기가 훨씬 정확하게
  그 자리를 맡습니다. 「0건일 때만」과 「적을 때만」은 다릅니다. 앞은 다른 장치가 발동할
  수 없는 자리이고, 뒤는 도는 장치를 결과 수로 꺼 버리는 일입니다.
  음절 상한(8)을 넘는 이름은 옮기지 않습니다. 「위즈덤하우스미디어그룹」처럼 긴 이름은
  자리가 열 곳이고 공백이 둘 이상일 수 있어 한 자리만 옮겨서는 맞지 않습니다.
  **그래서 「출판사만 넣고, 붙여 쓰고, 아홉 음절이 넘는」 검색은 지금도 0건입니다.**
- **화면이 밝히는 것은 함께 찾은 표기(`alsoSearchedTitles`, `alsoSearchedAuthors`)뿐입니다.**
  저자로 되찾은 사실(`recoveredByAuthor`)과 제목만으로 되찾은 사실은 화면에 적지 않습니다. **표기는 사용자가 목록에서 확인할 수 있지만
  되찾기는 우리 내부의 회전이라 확인할 방법도 없고 할 일도 없습니다.** 값 자체는 진단에
  쓰므로 응답에 그대로 둡니다. 검색 칸 아래의 띄어쓰기 안내와 「확인하지 못한 것을
  미소장으로 세지 않았습니다」도 같은 이유로 뺐습니다. 앞의 둘은 우리가 알아서 하는 일이고,
  뒤엣것은 바로 앞의 「일부를 확인하지 못했습니다」가 이미 말하고 있습니다. **다시 넣지
  마세요.** 화면이 자기가 한 일을 전부 설명하면 정작 사용자가 할 일이 묻힙니다.
- **같은 등급 안에서는 대출건수 순으로 세웁니다**(`WorkResult.loanCount`). 결과가 여러 번의
  검색을 합친 것이라 정보나루가 준 순서를 그대로 두면 어느 표기로 넣었는지에 따라 순서가
  달라집니다.
- **다만 대출건수가 정하는 것은 묶음 사이의 순서이고, 묶음 안은 권차 순입니다**
  (`BookSearchService.groupKey`). 낱권은 권마다 대출건수가 제각각이라 그대로 두면 **3권
  다음에 1권이 나옵니다.** 실측으로 「레미제라블」의 민음사 낱권이 **1권, 5권, 2권, 3권,
  4권** 순이었고 그 사이에 다른 출판사 셋이 끼어 있었으며, 상서각은 **2권이 1권보다
  위**였습니다(2026-09-09). 그래서 **권차를 뗀 표제 키와 출판사가 같은 것을 한 묶음**으로
  보고, 묶음 사이는 그 묶음에서 **가장 큰** 대출건수로, 묶음 안은 권차 순으로 세웁니다.
  낱권 하나가 덜 빌린다고 묶음 전체가 내려가면 찾던 책이 목록 아래로 사라집니다.
- **묶음 키에 출판사를 반드시 넣으세요.** 표제 키만으로 묶으면 「레미제라블」의 민음사
  낱권과 열린책들 낱권이 한 묶음이 되고, 권차 순으로 세우면 **민음사 1권, 열린책들 1권,
  민음사 2권처럼 출판사가 번갈아 나옵니다.** 지금보다 나빠집니다. 저작을 묶을 때 출판사가
  다르면 합치지 않는 규칙을 정렬에서도 그대로 씁니다. 출판사를 모르는 것끼리는 한 묶음으로
  둡니다. 모르는 것을 근거로 쪼개지 않는 것도 같은 규칙입니다.
- **권차 없는 세트와 합본은 묶음의 맨 뒤입니다.** 찾는 사람은 대개 낱권을 원하고, 세트
  ISBN 은 도서관이 낱권으로 등록하는 일이 많아 미소장으로 나오기 쉽습니다. 묶음의 첫 줄은
  가장 눈에 띄는 자리라 거기에 헛일이 되기 쉬운 것을 두지 않습니다.
- **「더 보기」 경계는 일부러 보정하지 않습니다.** 낱권 묶음이 스무 번째 자리에 걸치면
  뒷권이 다음 묶음으로 넘어갑니다. 그런데 묶음 정렬이 찾던 낱권을 앞자리로 올리므로 경계에
  걸리는 것은 대개 찾던 책이 아니고, **소장 정렬이 낱권을 훨씬 자주 갈라 놓습니다.** 순서는
  살아 있어 뒤엉키지 않습니다. 보정하려면 묶음 키를 응답에 실어야 하고 화면의 묶음 크기가
  변하는데, 그 크기가 곧 한 번에 나가는 소장 조회 횟수라 **토지처럼 권수가 스물을 넘는
  책에서 조회가 두 배로 늘어납니다.** 드물게 걸리는 일에 치를 값이 아닙니다.
- **상·중·하는 공백이나 괄호로 떨어져 있을 때만 권차입니다**(`BibNormalizer` 의
  `VOLUME_SANG_HA`). 붙여 쓴 것까지 잡으면 **「천하」가 「천」의 3권이 되고 「명상」이
  「명」의 1권이 됩니다.** 실제로 그렇게 잘리고 있었습니다. 표제 키가 한 글자로 바뀌므로
  그 책을 찾는 사람에게는 **제목이 안 맞는 것으로 밀려** 목록 아래로 내려가고, 낱권이
  아닌데 낱권으로 갈라집니다.
- **권차 필드가 숫자가 아니어도 버리지 마세요**(`BibNormalizer.volumeOrdinal`). 매뉴얼은
  `vol` 을 「권」이라고만 적어 두고 형식을 말하지 않습니다. 숫자만 남기고 버리면 「상」으로
  오는 책들은 권차가 없는 것이 되어 **상·중·하가 한 저작으로 합쳐지고**, 상권만 가진
  도서관이 「있음」으로 나옵니다. 숫자를 먼저 보고 없으면 상·중·하와 로마 숫자를 봅니다.
- **권차는 받은 표기를 그대로 붙이고 「권」을 덧붙이지 않습니다**(`kr.wimb.bib.Volume`).
  「토지 상」과 「레미제라블 1」이지 「토지 상권」도 「레미제라블 1권」도 아닙니다. 서수를
  표제에 붙이면 상·하 두 권뿐인 책이 **「1」과 「3」**으로 나와 사용자가 없는 2권을 찾고,
  「권」을 붙이면 정보나루가 주지 않은 말을 우리가 지어내는 것입니다. 권차는 서수와 표기를
  함께 든 `Volume` 으로 다니고, `TitleParts.volNo()` 는 정렬과 「같은 권인지」 판정에만
  씁니다. 표제에 붙이는 말은 `Volume.mark()` 가 압니다. `SearchDocBuilder.displayTitle`
  말고 다른 곳에서 권차를 글자로 만들지 마세요.
- **그래서 표제 꼬리의 숫자 권차를 세 자리까지 읽습니다**(`BibNormalizer.VOLUME_PATTERNS`).
  「권」이 붙은 꼬리는 원래 세 자리까지 봤는데 숫자만 있는 꼬리는 두 자리까지였습니다.
  「권」을 떼고 나면 **우리가 만든 표제를 우리가 다시 읽지 못합니다.** 정렬과 순위는
  표제를 다시 뜯어 권차와 표제 키를 얻으므로, 「원피스 100」이 권차 없는 책이 되어 표제
  키가 「원피스100」이 되고 **그 권만 낱권 묶음 밖으로 떨어져 목록 아래로 내려갑니다.**
  네 자리는 여전히 보지 않으므로 「코스모스 2020」 같은 연도는 그대로 걸러집니다.
- **여러 권 검색의 첫 후보도 대출건수로 고릅니다**(`MultiCheckService.popularityBonus`).
  예전에는 판본 수로 보너스를 줘서 같은 제목의 저작 가운데 덜 알려진 판이 첫 후보가 되었고,
  **같은 책인데 한 권 검색과 여러 권 검색의 소장 도서관이 다르게 나왔습니다.** 두 화면은
  같은 `/api/holdings` 를 부르므로 차이는 **어느 저작의 ISBN 목록을 넘기느냐**뿐입니다.
  한 권 검색이 같은 등급 안에서 대출건수로 세우므로 여기서도 같은 신호를 써야 첫 후보가
  같아집니다. 보너스는 로그로 묶어 0.1 을 넘지 않습니다. 제목이 그대로 맞는 것(1.0)을
  뒤집으면 안 됩니다.
- **후보 목록은 한 권 검색과 같은 묶음 정렬을 쓰고, 상한도 같습니다**
  (`BookSearchService.sortGrouped`, `MAX_WORKS`). 점수만으로 세우고 스물넷에서 잘랐을 때는
  **같은 판의 낱권이 대출건수에 따라 흩어지고 덜 빌린 권만 잘려 나갔습니다.** 실제로
  「마의 산」에서 을유문화사 판이 1권과 3권만 남고 2권이 사라졌고, 한 권 검색으로는 나오는
  판이 여러 권 검색에서는 안 나왔습니다. 묶음 사이는 점수(가장 잘 맞는 판 기준), 묶음 안은
  권차 순이라 첫 후보는 가장 잘 맞는 묶음의 첫 권입니다. 화면은 스무 개씩 펼칩니다.
  **후보 정렬을 여기서만 따로 만들지 마세요.** 한 권 검색과 갈리면 같은 책이 두 화면에서
  다른 순서로 나옵니다.
- **입력 그대로의 검색은 첫 쪽이 가득 찼을 때만 둘째 쪽까지 받습니다**(`MAX_PAGES_AS_TYPED`).
  소장 조회는 목록에 있는 ISBN 으로만 나가므로 **목록에 없는 판은 물어보지도 못하고**, 그
  판만 가진 도서관은 「없음」이 됩니다. 첫 쪽이 덜 찼으면 더 받을 것이 없어 부르지 않습니다.
- **줄 확정은 여섯 줄씩 겹쳐서 돌립니다**(`MultiCheckService.RESOLVE_CONCURRENCY`). 줄마다
  정보나루를 두세 번 부르고 `srchBooks` 한 번이 3.7초라, 서른 줄을 차례로 돌리면 몇 분이
  걸립니다. 요청 사이의 120ms 는 전송 계층이 따로 지키므로 겹치는 것은 정보나루의 응답
  시간뿐이고, 결과 순서는 줄 순서 그대로입니다.
- **검색 조건은 제목·저자·출판사·ISBN 넷을 받습니다.** ISBN 은 사람이 하이픈이나 공백을
  섞어 옮겨 적으므로 숫자와 X 만 남기고 넘깁니다. 그대로 넘기면 0건이 나오고 사용자는
  그것을 「이 책이 없다」로 읽습니다.
- **순위를 견줄 때는 권차를 뗍니다.** 표제에는 권차를 붙여 주는데(`displayTitle` 이
  「레 미제라블 1」을 만듭니다), 그 표제를 그대로 견주면 **낱권이 전부 「제목이 덜 맞는
  것」으로 밀립니다.** 실제로 「레미제라블」을 찾았을 때 민음사 낱권이 **62~67위**였고,
  후보를 스물넷까지 늘려도 목록에 들지 못했습니다. 권차 없는 판들이 앞자리를 다 차지한
  것입니다. **규칙 둘이 부딪친 자리입니다.** 「갈라 놓았으면 표제에 드러내라」와 「제목이
  맞는 것을 위로 올려라」가 같은 문자열을 서로 다르게 봅니다. 표제는 사람이 읽는 것이고
  순위는 기계가 견주는 것이므로, **견줄 때는 `parseTitle(...).titleKeyCore()` 를 씁니다.**
- **한 권 검색과 여러 권 검색은 같은 조회 함수를 씁니다**(`fetchBooks`). 예전에는 띄어쓰기
  재시도와 저자 되찾기가 `search()` 안에만 있고 `worksFor()` 는 그냥 받아 왔습니다. 그래서
  **같은 「레미제라블」인데 한 권 검색에서는 민음사 낱권이 나오고 여러 권 검색에서는 한 권도
  나오지 않았습니다.** 개선을 한쪽 경로에만 넣지 마세요.
- **한 권 검색의 목록은 묶음(20개)의 소장 확인이 끝난 뒤에 소장 → 미소장 → 확인 불가
  순으로 한 번 세워서 그립니다**(`domain/resultOrder.ts`). 그때까지는 진행 막대가 어디까지
  왔는지 말합니다. 예전에는 답이 도착하는 대로 채우고 「있는 책 먼저 보기」 체크박스로
  켜야 올라왔는데, 그 체크박스는 없앴습니다. 답이 올 때마다 순서가 바뀌면 읽던 자리가
  사라지므로 **세우는 것은 묶음마다 한 번뿐**이고, 「더 보기」로 펼친 묶음은 이미 그린 묶음을
  건드리지 않고 그 아래에 세워서 붙습니다. 도서관을 고르지 않았거나 아직 「확인」을 누르지
  않았으면 기다릴 것이 없으므로 일치도 순으로 바로 그립니다.
- **표지가 열리지 않아도 줄 배치가 무너지면 안 됩니다.** 한 권 검색의 줄은 「표지 56px +
  나머지」 두 칸 격자입니다. 표지 로딩이 실패했을 때 `display: none` 으로 지우면 표지가
  흐름에서 빠지고 **본문이 첫 칸으로 밀려 들어가 제목과 저자가 두세 글자마다 줄바꿈됩니다.**
  실제로 그 상태로 돌고 있었고, 표지 주소가 죽은 책에서만 나타나 눈에 잘 띄지 않았습니다.
  감출 때는 `visibility: hidden` 으로 **자리를 남긴 채** 감추세요(여러 권 검색의 후보 표지도
  같은 방식입니다). `.book__body { grid-column: 2 }` 가 그 배치를 한 번 더 붙들어 둡니다.
  **소장 결과는 본문 칸이 아니라 격자의 셋째 칸(`.book__holding`)입니다.** 좁은 화면에서
  표지 아래 한 줄을 통째로 써야 도서관 이름과 대출 확인 단추가 한 줄에 들어갑니다. 본문
  안으로 되돌리면 400px 화면에서 도서관 한 곳이 세 줄을 차지합니다.
- `POST /api/check/resolve` 는 줄을 읽고 책을 확정하기만 합니다. **소장 조회를 부르지 마세요.**
- `POST /api/holdings` 가 저작 하나의 소장을 답합니다. 저작에 묶인 **모든 판본**을 함께 보냅니다.
- **도서관 체크박스가 바뀔 때마다 다시 조회하지 마세요.** 한 권 검색은 체크 한 번에 20권을,
  여러 권 검색은 30권을 다시 묻습니다. 열 곳을 고르는 동안 200~300회가 나가고 화면은 계속
  버벅이며 하루 예산도 그만큼 새어 나갑니다. 검색하거나 목록을 확인할 때 한 번 부르고,
  그 뒤로 선택이 바뀌면 **사용자가 누를 때만** 다시 부릅니다.
- **소장 결과에는 그것이 어느 선택 기준인지를 함께 들고 다닙니다.** 없으면 선택이 바뀐
  뒤에도 예전 답을 그대로 보여 주게 됩니다. 방금 체크를 푼 도서관이 소장 목록에 남고,
  새로 고른 도서관은 물어본 적이 없는데도 「없음」으로 나옵니다. **뒤엣것이 특히
  나쁩니다.** 실제로 있는 책을 없다고 답하는 것이라 헛걸음을 만듭니다.
  기준이 다르면 아예 쓰지 않고, 「위의 확인을 누르면 알려 드립니다」로 보여 줍니다.
- 후보 점수에 **소장 여부를 쓰지 마세요.** 소장을 알려면 먼저 책을 확정해야 하므로 순서가
  맞지 않고, 후보마다 조회하면 호출이 몇 배로 늘어납니다.
- **여러 권 검색 화면은 자료가 흐르는 순서대로 ① 목록 → ② 책 확인 → ③ 도서관 순입니다.**
  예전에는 집계(「한 곳에서 다 빌리기」, 「도서관별 소장」)가 줄 목록 위에 있어서, 맨 아래에서
  후보를 바꾼 뒤 다시 위로 올라가 결과를 봐야 했습니다. **집계를 줄 목록 위로 올리지 마세요.**
  후보를 바꾸는 자리가 결과보다 아래에 있으면 같은 일이 되풀이됩니다. 소장 확인의 진행
  막대와 「고른 도서관 n곳에서 확인」 단추만 ① 바로 아래에 둡니다. 선택이 바뀌었을 때
  눌러야 하는 것이라 어느 단계보다 먼저 보여야 하기 때문입니다.
- **여러 권 검색의 입력 칸은 여덟 줄입니다**(`MultiCheck.INPUT_ROWS`). 결과가 없는 동안
  남은 높이를 전부 채우게 해 두었더니 넓은 화면에서 수십 줄짜리 빈 칸이 되어 실제로 「너무
  크다」는 말을 들었습니다. 다시 채우게 하지 마세요. 더 긴 목록은 모서리를 끌어 늘립니다.
- **고른 책은 늘 카드로 보이고, 자동으로 고른 것에도 선택 표시를 답니다.** 예전에는 확정된
  줄은 카드가 없었고, 모호한 줄은 첫 후보를 자동으로 골라 놓고도 `picked` 일 때만 테두리를
  칠해서 골라져 있는데 안 골라진 것처럼 보였습니다. `picked` 는 「자동 선택」과 「직접 선택」을
  글자로 가르는 데 씁니다. 자동인데 직접 고른 것처럼 말하면 사용자는 자기가 누른 것이
  먹히지 않았다고 읽고, 표시가 없으면 아무것도 고르지 않은 것으로 읽습니다.
- **그 선택 표시 옆에 링크 칩을 두지 마세요**(`.pick--chosen`). 표시는 한 줄뿐인데 카드가
  flex 줄이면 카드 높이만큼 제 칸을 차지해서, 아래가 훤히 비어 있는데도 본문 칸이 표시
  너비만큼 좁아집니다. 그 칸에 「정보나루 책 정보」를 두었더니 **옆에 자리가 남는데도 두
  줄로 접혔고, 한국어는 음절마다 끊을 수 있어 「정보나루 책 정 / 보」처럼 낱말 가운데서
  갈라졌습니다.** 실측으로 화면 360px 에서 본문 칸이 105px 까지 줄었고 칩은 135px 입니다.
  카드를 격자로 두고 **링크 줄만 표시 아래로 내려 한 줄을 통째로 쓰게 합니다.** 표제와
  저자는 여러 줄로 흐르는 글이라 좁아져도 읽히지만 칩은 접히면 안 됩니다. 좁은 화면에서
  소장 결과를 표지 아래로 내리는 `.book__holding` 과 같은 방식입니다. 그래도 모자랄 때를
  대비해 `.chip` 에 `word-break: keep-all` 을 걸어 끊기는 자리가 띄어쓰기가 되게 합니다.
- **도서관에 관한 것은 전부 ③ 의 도서관 줄 하나에 모읍니다**(`LibrarySection`). 그 도서관에
  있는 책과 없는 책, 대출 상태 확인 단추, 홈페이지 링크가 거기 한 곳뿐입니다. ② 의 줄은
  어느 도서관에 있는지를 **이름으로만** 말하고 링크도 단추도 달지 않습니다. 예전에는 「한
  곳에서 다 빌리기」와 「도서관별 소장」과 줄마다의 소장 목록이 저마다 도서관 링크와 대출
  확인 단추를 달아 같은 단추가 세 곳에 흩어져 있었습니다. **다시 흩어 놓지 마세요.**
  책 제목에 링크를 다는 것은 그 도서관의 책 페이지로 갈 수 있을 때(`linkBadge` 가 비어
  있지 않을 때)뿐입니다. 규칙이 없는 도서관은 어느 책을 눌러도 홈페이지가 열려 같은 링크가
  책 수만큼 반복됩니다.
- **이름과 제목을 늘어놓는 자리는 하나가 한 줄입니다**(`.namelist`). ② 의 「있는 곳」
  도서관(`LineWhere`), ③ 의 「있음」·「없음」 책(`LibraryRankRow`), 그 아래 「고른 도서관에
  없는 책」과 「확인하지 못한 책」(`Leftovers`)이 전부 여기에 해당합니다. 예전에는 넷 다
  가운뎃점으로 이어 한 문단으로 적었는데, 한국어는 음절마다 줄이 바뀔 수 있어 **이름이
  가운데서 끊기고 가운뎃점만 줄 끝에 남습니다.** 항목이 많을수록 어디까지가 하나인지 알 수
  없어지는데, 그 자리들은 「어디로 가면 되나」와 「거기서 무엇을 빌리나」에 답하는 자리라
  그것을 못 읽으면 아무 말도 하지 않은 셈입니다. 제목이 링크인 ③ 은 더 그렇습니다. 누를
  자리가 두 줄에 걸쳐 끊겨 있으면 어느 것을 누르는지 알 수 없습니다.
  **이름표가 「있는 곳」처럼 짧은 말이면 `.labeled` 로 목록 옆 첫 칸에 두고, 제 줄로 올리지
  마세요.** 이름표가 제 줄을 차지하면 항목이 하나뿐인 흔한 경우에 줄이 둘로 늘고, 서른
  줄짜리 목록에서는 그것이 서른 줄입니다. 이름표가 이미 제목 줄인 `Leftovers` 는 `.labeled`
  없이 목록만 씁니다. 「확인하지 못한 판본이…」처럼 목록 전체에 대한 단서는 항목 칸 아래에
  따로 붙입니다(`.labeled__note`). 항목 하나의 말이 아닙니다. **그리고 이 규칙을 자리마다
  따로 적지 마세요.** 한 곳에 두었으니 네 자리가 갈릴 일이 없습니다.
- **전부 가진 도서관은 「전부 있음」 표시로 목록 맨 위에 옵니다**(`LibraryRank.full`). 목록은
  소장 권수 → 거리 → 이름 순이라(`rankLibraries`) 전부 가진 곳이 자연히 위로 오고, 위치를
  알면 그 안에서 가까운 곳이 먼저입니다. 예전 `FullCoverage` 는 전부 가진 도서관을 따로
  세우고 대출 상태를 (도서관 × 책)만큼 한꺼번에 물어 빌릴 수 있는 권수로 다시 세웠는데,
  같은 도서관이 두 목록에 나오고 대출 단추도 둘이 되어 없앴습니다. **대출 상태를 물어본
  뒤에도 순서를 바꾸지 않습니다.** 도서관마다 따로 누르므로 「다 물어본 시점」이 없고,
  답이 올 때마다 자리를 옮기면 읽던 곳이 사라집니다. 대신 집계 한 줄을 도서관 줄에 적어
  접어도 보이게 합니다. 한 곳에 다 있는 도서관이 없을 때만 여러 곳을 엮은 조합을 **문장으로**
  보이고 링크는 달지 않습니다(`planTrip`). 같은 도서관이 바로 아래 목록에 있습니다.
  위치는 `App` 이 들고 도서관 선택 칸과 여러 권 검색에 내려 줍니다.
- 집계(`frontend/src/domain/tripPlan.ts`)는 **확인이 끝난 책만** 셉니다. 확인하지 못한 책을
  미소장에 넣으면 없다고 답하는 것이고, 소장에 넣으면 있다고 답하는 것입니다. 둘 다 헛걸음입니다.
- 「고른 도서관에 없는 책」과 「확인하지 못한 책」을 한 목록에 넣지 마세요. 앞은 서점으로
  넘어가야 하는 책이고 뒤는 다시 확인하면 있을 수 있는 책이라 행동이 다릅니다.
- **모호한 줄의 후보에는 표지와 출판사를 함께 보여 줍니다.** 제목과 저자만으로는 어느
  판인지 모릅니다. 같은 제목의 번역본이 출판사마다 있고 사용자가 찾는 것은 대개 자기가
  아는 그 판입니다. 후보는 처음부터 접어 두고 「이 책이 아닌가요?」로 펼치며, 고르면 다시
  접습니다. `chosen` 만으로는 「첫 후보를 골랐다」와 「아직 안 골랐다」가 같은 값이라
  `picked` 를 따로 둡니다. 배지에서 「골라 주세요」를 계속 띄워 두면 사용자는 자기가 누른
  것이 먹히지 않았다고 읽고 같은 자리를 다시 누릅니다.
- **줄 확정 단계에도 같은 구분이 있습니다.** `NOT_FOUND` 는 "물어봤는데 그런 책이 없다",
  `LOOKUP_FAILED` 는 "물어보지 못했다"입니다. 정보나루가 흔들릴 때 뒤를 결과 없음으로
  표시하면 사용자가 멀쩡한 줄을 고치려 들게 됩니다. 화면에서도 「고쳐야 하는 줄」과
  「확인하지 못한 줄」로 갈라 놓습니다.

줄 해석(`kr.wimb.query.LineParser`)에서 되풀이되는 실수:

- 목록 번호는 **두 자리까지만** 뗍니다. 「1984. 조지 오웰」의 「1984.」를 번호로 읽으면 안 됩니다.
- 줄 전체를 제목으로 **먼저** 보되, **결과가 나왔다고 무조건 멈추지 마세요.** 정보나루의
  제목 매칭이 관대해서 저자를 붙인 줄도 무언가를 찾아냅니다. 실제로 「좀머 씨 이야기 -
  파트리크 쥐스킨트」가 **「파트리크 쥐스킨트 작품집 (전5권)」으로 확정**되었습니다.
  작품집 표제에 그 말들이 다 들어 있어서인데, 찾던 책이 아닌 데다 확정까지 되어 사용자는
  고를 기회조차 없었습니다. **제목이 그대로 맞았을 때(`GOOD_ENOUGH`)만 멈추고**, 아니면
  다음 해석도 해 본 뒤 더 나은 쪽을 씁니다. 제목만 넣은 줄은 첫 시도에서 맞으므로 호출이
  늘지 않습니다.
- **구분자 뒤에 오는 말은 저자로도 출판사로도 읽습니다**(`TITLE_AUTHOR`,
  `TITLE_PUBLISHER`). 「마의 산 - 토마스 만」과 「마의 산 - 을유문화사」는 **생김새가 같아서
  줄만 보고는 어느 쪽인지 알 수 없습니다.** 사람 이름과 출판사 이름을 글자로 가르는 규칙을
  만들지 마세요. 그 규칙이 틀리는 날 조용히 엉뚱한 책이 확정됩니다. **가르지 않고 둘 다
  만들어 물어본 뒤 점수로 고릅니다.** 출판사 해석이 필요한 이유는 **고전 번역서에서 판을
  가르는 것이 저자가 아니라 출판사**이기 때문입니다. 저작을 출판사별로 갈라 놓았으므로
  「마의 산 - 토마스 만」은 범우사·을유문화사·열린책들·동서문화사·지식을만드는지식이
  저마다 다른 후보로 나오고, **저자를 붙여도 후보가 하나도 줄지 않습니다.**
  출판사 비교는 `normalizePublisher` 로 합니다. 법인격 표기를 떼므로 「민음사」와
  「(주)민음사」가 같아지는데, `normalizeKey` 로 견주면 갈립니다.
- **나눠 읽은 두 해석은 한 회전에 함께 내보냅니다**(`MultiCheckService.lookUpTogether`).
  **차례로 부르면 해석마다 4초가 붙습니다.** `srchBooks` 한 번이 3.7초라 서른 줄이면 그것만
  몇 분입니다. 서로의 결과를 필요로 하지 않으므로 겹칠 수 있고, 한꺼번에 나가는 요청 수는
  `WIMB_DATA4LIBRARY_MAX_IN_FLIGHT` 가 따로 묶습니다.
- **그래서 「줄 전체가 그대로 맞으면 나눠 묻지 않는다」를 없애지 마세요.** 해석이 셋으로
  늘었으므로 이것이 없으면 구분자가 든 줄마다 호출이 세 배가 됩니다. 제목만 적은 줄이 전부
  여기에 해당해서 **그런 줄의 호출은 한 건도 늘지 않습니다.** 점수가 같으면 저자 해석을
  씁니다(`bestOf`). 붙여 넣는 목록은 「제목 - 저자」가 훨씬 흔합니다.
- **늘어나는 것은 모호한 줄당 한두 건입니다.** 출판사 해석 하나에, 구분자 뒤엣말에 공백이
  있으면 붙여 쓴 표기까지 하나 더입니다. 한국어 인명은 대개 공백이 있어(「토마스 만」,
  「칼 세이건」) **저자를 적은 줄에서도 두 건이 나갑니다.** 그 둘은 아무것도 데려오지
  못하지만, 어느 쪽인지 미리 알 방법이 없어 치르는 값입니다. 서른 줄이 전부 모호해도
  60건이라 하루 한도 30,000건에서는 문제가 되지 않고, 전부 1회전에 겹쳐 나가므로 걸리는
  시간도 늘지 않습니다.
- **조각이 셋 이상인 줄은 앞부분만 제목으로 보는 해석을 하나 더 답니다**
  (`LineParser.titleOfThreeOrMore`). 「제목 / 저자 / 출판사」는 표에서 복사하면 나오는 흔한
  모양인데, 마지막 구분자에서만 나누면 **저자가 제목 안으로 딸려 들어가**(「마의 산 /
  토마스 만」) 한 건도 안 나옵니다. 사용자에게는 「그런 책이 없다」로 보이고, 화면은
  잘못 읽은 것을 그대로 보여 줍니다. **세 칸을 제대로 받지는 않습니다.** 저자든 출판사든
  하나만 있으면 판을 가르는 데 충분해서, 제목만이라도 제대로 읽어 0건을 면하는 데까지가
  값이 맞습니다. 안전망은 **맨 뒤**에 둡니다. 저자나 출판사까지 맞은 해석이 점수로
  이겨야 합니다.
- **조각을 셀 때 쉼표를 세지 마세요**(`STRONG_SPLITTERS`). 쉼표는 제목 안에 흔히
  들어갑니다. 세면 「총, 균, 쇠 - 재레드 다이아몬드」가 네 조각이 되어 **앞부분만 제목으로
  보는 해석이 「총」을 찾습니다.** 한 글자로 아무것이나 데려오는 셈입니다. 그래서 쉼표로
  나눈 목록(「마의 산, 토마스 만, 을유문화사」)은 지금도 읽지 못합니다. **제목을 잘라
  먹는 쪽보다 못 읽는 쪽이 낫습니다.** 쉼표가 제목의 일부인지 칸 구분인지 가를 근거가
  줄 안에 없습니다.
- **못 찾은 줄의 문구로 출판사 해석을 고르지 마세요**
  (`MultiCheckService.explanationWhenNotFound`). 출판사 해석은 저자 해석과 짝으로 만든
  것이라, 맨 뒤에 있다는 이유로 그것을 쓰면 「총, 균, 쇠 - 재레드 다이아몬드」가
  **「출판사: 재레드 다이아몬드」**로 나갑니다. 사람 이름을 출판사라고 말하는 셈이고,
  사용자는 고칠 수 있는 줄을 어떻게 고칠지 알 수 없습니다.
- **서점 주소는 읽되 화면에서 안내하지 않습니다.** 주소 안에서 ISBN13 을 찾아 쓰는
  방식이라 붙여 넣으면 동작하지만, **예스24나 알라딘처럼 자체 상품 번호만 쓰는 주소는
  읽지 못합니다.** 많이 쓰는 서점일수록 그렇습니다. 되는 경우가 드문 것을 예시로 세우면
  사용자는 안 되는 주소를 넣어 보고 고장이라고 여깁니다. **`LineParser` 의 URL 처리를
  쓰이지 않는 코드로 보고 지우지 마세요.** 서점에서 복사해 붙이는 사람이 있고, 그때
  주소에 ISBN 이 있으면 그대로 읽힙니다.
- 체크디지트가 틀린 숫자를 제목으로 넘기지 마세요. 0건이 나오고 사용자는 그것을
  「이 책이 없다」로 읽습니다.
- 어떻게 읽었는지를 항상 화면에 되돌려 보여 줍니다.

### 서가는 미리 받지 않고 사람이 열 때 세웁니다

읽을 책을 아직 정하지 않은 사람의 입구입니다(`kr.wimb.shelf`, `components/Shelf.tsx`).
그 도서관 장서를 **청구기호 순으로 세워 실제 서가처럼** 보여 줍니다. 「한 권 검색」·
「여러 권 검색」과 나란한 셋째 탭입니다.

**다른 탭과 같은 자리에 그립니다.** 한때 화면을 통째로 덮고(`position: fixed`) 아래 탭
바를 쓰게 했는데, **탭을 누르는 순간 예고 없이 배치가 바뀌어 다른 데로 온 것처럼
보였습니다.** 탭이 제자리에 있으면 이동이 이동으로 읽힙니다. 넓은 화면에서 좁아질까 봐
그렇게 했던 것인데 실제로 재 보니 1,280px 에서 표지가 153px 로, 휴대폰의 81px 보다
두 배 넓었습니다. **화면을 덮는 것은 「이 책 주변 서가」 하나뿐입니다.** 그쪽은 한 권을
들여다보는 모드이고 닫는 단추가 있어서 덮는 것이 기대되는 동작입니다.

**서가에 제 스크롤 칸을 주지 마세요.** 브라우저 스크롤바가 하나 더 생겨 페이지 스크롤과
따로 놉니다. 이 저장소가 예전에 두 칸을 각자 스크롤하게 두었다가 같은 이유로
걷어냈습니다. 서가는 페이지와 함께 내려가고, 몇 번째 줄인지는 **「페이지를 얼마나
내렸는가 − 서가가 시작하는 자리」**로 셉니다. 머리말은 `sticky` 로 붙여 둡니다.

**초성 색인 자리는 좁은 화면에서만 비웁니다.** 색인은 화면 오른쪽 가장자리에 붙박이로
있고 본문은 1,100px 에서 가운데로 모이므로, 화면이 충분히 넓으면 색인이 본문 바깥 여백에
떠서 비울 자리가 없습니다. 그런데도 비워 두면 서가만 26px 좁아져 **붙박이 머리말과
오른쪽 끝이 어긋납니다.**

**실시간으로 조회할 수 없습니다. 이것이 이 화면의 모든 설계를 정합니다.**

`itemSrch` 에 **정렬 항목이 없습니다.** 매뉴얼 전체에서 `sort` 를 받는 것은 `srchBooks`
하나뿐이고(50쪽), 고를 수 있는 것은 `title author pub pubYear isbn loan` 입니다. **청구기호가
없습니다.** 그리고 `srchBooks` 는 `libCode` 를 안 받아 응답에 `callNumbers` 자체가 없습니다.
청구기호 범위로 거르는 조건도 어느 엔드포인트에도 없습니다.

그래서 받아 온 한 쪽은 그 갈래 안에서 **아무렇게나 뽑힌 500권**입니다. 정렬해도 한 줄에
`004 / 181 / 325 / 513` 이 섭니다. 서가가 아니라 뒤섞인 목록입니다. **「813.6 다음에 무엇이
꽂혀 있나」는 그 갈래를 다 보고 나서야 답할 수 있습니다.** 이건 우리 구현이 게으른 것이
아니라 정렬이라는 일 자체의 성질입니다.

**그렇다고 전국을 미리 받을 수도 없습니다.** 1,619곳 × 15만 권 ÷ 500 = **48만 회**이고 하루
한도가 25,000이라 **스무 날**이 걸립니다. 디스크도 **65GB** 라 무료 등급 VM 의 30GB 에
들어가지 않습니다.

- **단위는 (도서관 × KDC 대주제)입니다**(`Kdc`). 열 개뿐이라 차림표를 코드에 둘 수 있습니다.
  세부주제(`dtl_kdc`, 두 자리)로 자르면 서가는 작아지지만 **어디에 몇 권이 있는지 알려고
  100번을 물어봐야 합니다.** 대주제 이름과 코드는 매뉴얼 59쪽 표 그대로이고 우리가 지어낸
  것이 아닙니다.
- **사람이 여는 서가만 세웁니다**(`ShelfService`). 아무도 열지 않는 도서관에는 호출도
  디스크도 한 건 쓰지 않습니다. 한 서가가 5천~10만 권이라 40~200회, 15초~1분입니다.
  **한 번 세우면 그 뒤로는 몇 명이 몇 번을 열든 정보나루 호출이 0건입니다.**
- **순서 규칙을 고쳤으면 `ShelfSortKey.VERSION` 을 올리세요.** 순서는 수집할 때 한 번
  계산해 파일에 적어 두므로, **규칙만 고치면 이미 세워 둔 서가는 예전 순서 그대로입니다.**
  게다가 갱신할 때가 되어도 **권수가 그대로면 다시 세우지 않으므로**, 아무도 손대지
  않으면 **영영** 예전 순서로 남습니다. 화면에는 아무 이상이 없어 보이고, 규칙을 고친
  사람은 고쳐졌다고 믿습니다. 실제로 전집 순서를 고쳤을 때 이 장치가 없어서 사람이
  서버에 들어가 손으로 지워야 했습니다. 판 번호를 차림표에 함께 적어 두면 번호가 다른
  서가를 **낡은 것으로 보고 뒤에서 다시 세웁니다.** 보여 주는 것은 멈추지 않으므로
  사람은 예전 서가를 보다가 어느 순간 새 순서를 받습니다. **「확인한 날만 올리기」가 판
  번호를 올리게 하지 마세요.** 그것은 순서를 다시 적은 것이 아니라 날짜만 고친 것이라,
  올려 버리면 예전 순서인 서가가 최신인 척합니다.
- **기한이 지나도 지우지 않습니다.** 기준일과 함께 그대로 보여 주고 갱신은 뒤에서 합니다.
  지우고 다시 세우면 그동안 서가가 비어 사람이 몇십 초를 기다리는데, 20일 전 서가라도 빈
  화면보다 낫습니다. 기준일이 화면에 적혀 있으니 거짓말도 아닙니다. **소장 캐시가
  「만료시켜 버리지 않는다」로 지키는 규칙과 같습니다.**
- **갱신 전에 권수만 한 번 물어봅니다.** 그대로면 다시 세우지 않아 수백 번을 아낍니다.
  그때 **기준일(`asOf`)은 올리지 않고 「확인한 날」(`checkedAt`)만 올립니다.** 받아 온 적
  없는 날짜를 받아 온 것처럼 말하게 되기 때문입니다. 신착과 폐기가 같은 수만큼 일어나면
  못 잡지만, 기한이 다시 차면 어차피 세웁니다.
- **증분 수집을 붙이지 마세요.** `startDt` 는 등록일 기준이라 신착만 오고 **폐기를 못
  잡습니다.** 결국 언젠가 전체를 다시 받아야 하니 그 사이를 메우는 반쪽짜리입니다.
  35회 아끼려고 정렬된 파일에 끼워 넣는 복잡도를 치를 값이 없습니다.
  그리고 **`type=ALL` 과 `startDt` 를 함께 주면 날짜가 조용히 무시됩니다**(매뉴얼 7쪽:
  「type 코드를 우선 조회」). 갱신인 줄 알았는데 매번 전체를 받게 되고, 호출 수를 보기
  전에는 모릅니다.
- **호출이 새지 않게 막는 것이 셋입니다.** 같은 서가를 둘이 열어도 한 번만 세우고,
  한꺼번에 세우는 서가를 둘로 묶고(`max-concurrent-builds`), 하루 몫이 `reserve-calls`
  아래로 내려가면 시작하지 않습니다. **서가는 내일 세워도 되지만 검색은 그 사람이 지금
  기다리고 있습니다.**
- **보는 통로와 세우는 통로를 갈라 두세요.** 보는 `GET` 은 파일을 흘려보내는 것이라
  정보나루를 부르지 않고, 세우는 `POST` 는 수백 번을 부릅니다. 무게를 하나로 매기면
  **낮게 잡을 때 아무나 수백 회를 태우고, 높게 잡으면 진행률을 몇 번 물어보다 막힙니다.**
- **다 만든 뒤에 한 번에 갈아 끼웁니다.** 임시 자리에 적고 마지막에 이름을 바꿉니다.
  세우는 데 몇십 초가 걸리는데 그동안 서비스하는 서버가 반쯤 쓰인 서가를 읽으면 책 몇
  권짜리 서가가 나가고, 그것은 고장으로 읽힙니다.
- **쪽 크기는 달라는 대로 준다고 믿지 않습니다.** 매뉴얼이 기본값 100 만 적고 상한을 말하지
  않아서, 크게 물어보고 **실제로 몇 건이 왔는지 세어** 정합니다. 준 것보다 많이 왔다고
  가정하면 그만큼이 통째로 빠지는데, 빠진 책은 서가에 없으므로 **아무도 눈치채지 못합니다.**
- **한 쪽이라도 못 받으면 통째로 실패합니다.** 반쪽짜리 서가는 구멍이 난 채로 그럴듯해
  보이고, 빠진 책은 「그 자리에 없다」가 됩니다.
- **아직 세우지 않은 서가는 404 가 아니라 「없음」이라는 상태입니다.** 화면이 「고장」과
  「아직 안 세움」을 갈라 말해야 합니다. 「미소장」과 「확인 불가」를 갈라 두는 것과 같습니다.
- **`/api/status` 의 `shelfLibraries` 를 보세요.** 소장 캐시와 같은 볼륨에 있어서 볼륨이
  붙지 않은 채 뜨면 둘이 함께 0 이 됩니다. **배포 직후 0 이면 수집을 다시 돌릴 것이 아니라
  볼륨부터 보세요.**
- 미리 데워 두고 싶으면 `--wimb.shelf.harvest=141321:8` 로 컨테이너를 따로 띄웁니다
  (`scripts/vm/harvest-shelf.sh`). **평소에는 필요 없습니다.** 화면에서 부를 수 있게 하면
  우리 API 에 인증이 없어 아무나 수천 건을 태울 수 있고, 자동으로 돌리면 실패했을 때
  아무도 모릅니다.

### 청구기호는 받는 값이 아니라 조립하는 값입니다

`itemSrch` 의 `callNumber` 는 **잎이 아니라 상자**입니다. 자기 글자가 없고 `separate_shelf_code`
`separate_shelf_name` `book_code` `shelf_loc_code` `shelf_loc_name` `copy_code` 여섯을
자식으로 답니다. 매뉴얼 6~7쪽의 결과화면(그림)에서 확인했습니다.

예전 코드가 그 요소의 자기 글자만 읽어서 **모든 도서관에서 언제나 null 이었습니다.** 배포된
서버로 여섯 곳을 물어 여섯 곳 모두 null 인 것을 확인했습니다(2026-09-20). **예외도 로그도
나지 않아** 「이 도서관은 청구기호를 안 준다」로 읽혔고 문서에도 그렇게 적혀 있었습니다.
**값이 비는 것과 못 읽는 것은 다릅니다.**

- **청구기호 문자열을 주는 항목이 응답 어디에도 없습니다.** `class_no` + `book_code` 로
  「818 박36ㅈ」을 우리가 만듭니다(`ShelfItem.callText`). 별치와 복본이 있으면 앞뒤에 붙입니다.
  **없는 조각을 지어내지 마세요.** 없는 것보다 틀린 청구기호가 나쁩니다. 서가에서 헛걸음하게
  만드는 값입니다.
- **한 줄은 한 책이 아니라 한 복본입니다.** `callNumbers` 는 복본마다 하나씩 담고, 자료실이
  다르면 **서가에서 서로 다른 자리에 꽂힙니다.** 한 줄로 합치면 둘 중 하나를 찾으러 간
  사람이 그 자리에서 책을 찾지 못합니다.
- **청구기호가 없는 자료는 서가에 세우지 않습니다.** 어디에 꽂혔는지 모르는 책을 순서
  어딘가에 끼워 넣으면 그 자리를 보고 찾아간 사람이 헛걸음합니다.

### 서가 순서는 여섯 단계이고 판정은 한 곳에서만 합니다

`CallNumberOrder` 입니다. 자료실 → 별치기호 → 분류번호 → 도서기호 → 권차 → 복본기호.

- **분류번호는 소수점 아래를 자릿수 단위로 견줍니다.** 실수로 읽으면 `813.6` 과 `813.62` 가
  뒤집힙니다. 소수점 아래가 크기가 아니라 **더 좁혀 들어간 갈래**라서입니다. `.9` 와 `.10`
  이 그 차이를 잘 보여 줍니다. 크기로는 0.9 가 크지만 서가에서 `.10` 은 `.1` 바로 뒤,
  `.2` 앞자리입니다.
- **꼬리에 남은 숫자도 크기로 견줍니다**(`ShelfSortKey.natural`). 전집은 권 번호가
  도서기호 꼬리에 붙어 옵니다(「박65ㅌ1」「박65ㅌ2」…). 그 꼬리를 글자 그대로 견주면
  **「ㅌ10」이 「ㅌ2」보다 앞에 섭니다.** 「1」이 「2」보다 작아서입니다. 가운데 숫자에
  대해서는 처음부터 지키던 규칙인데 꼬리에만 빠져 있었고, **전집은 권수가 열을 넘는 일이
  흔해서 실제 서가와 가장 크게 어긋나 보이는 자리입니다.** 「실제 서가 순이 아닌 것
  같다」는 말이 여기서 나왔습니다.
- **도서기호는 한글부·숫자부·꼬리 자모로 나눠 견줍니다.** 글자 그대로 견주면 「김9ㄱ」이
  「김63ㄷ」 뒤로 갑니다. **서가에서는 바로 옆자리라 눈에 띄지 않고**, 한 자리 도서기호를
  쓰는 저자에게서만 일어납니다.
- **실제로 도는 것은 `sortKey` 입니다.** 수집할 때 한 번 계산해 파일에 순서대로 적어 두면
  화면도 서버도 다시 정렬하지 않습니다. 20만 권을 브라우저가 정렬할 수는 없습니다.
  자릿수가 다른 숫자는 **0 으로 채우지 않고 자릿수를 앞에 적습니다.** 채울 자릿수를 정하는
  방법은 그 자릿수를 넘는 값이 올 때 조용히 틀립니다.
- **그런데 사람이 읽고 고치는 것은 비교자 쪽입니다.** 둘이 갈리면 고친 규칙이 화면에
  반영되지 않은 채 아무 오류 없이 돕니다. 그래서 둘을 따로 구현하고 **같은 순서를 내는지
  섞어 가며 200번 견줍니다**(`CallNumberOrderTest`).
- **초성 색인은 저자명이 아니라 도서기호 첫 글자에서 뽑습니다.** 저자명에서 뽑으면 색인을
  눌러 간 자리와 그 자리에 실제로 있는 책이 달라집니다. 저자명은 여럿이 섞여 오고 번역서는
  원저자가 앞에 오지만, **서가에 꽂히는 자리는 도서기호가 정합니다.** 쌍자음은 기본 자음으로
  접어 색인을 열넷으로 둡니다.

### 서가 화면에서 지키는 것

- **표지 아래에 글자를 적지 않습니다.** 실제 서가에서 책을 고르는 일은 표지를 훑는 일이지
  제목을 한 권씩 읽는 일이 아닙니다. 제목을 달면 화면이 목록이 되어 검색 결과와 다를 것이
  없어집니다. 대신 **`aria-label` 에 제목·저자·청구기호를 전부 담습니다.** 눈으로 훑는
  사람에게는 그림이 정보이지만 화면을 읽어 주는 사람에게는 아무 말도 하지 않습니다.
- **표지가 없는 책은 제목과 저자로 그립니다.** 빈 자리로 두면 실제 서가에서 책이 빠진
  자리처럼 보여 고장으로 읽힙니다. **색은 분류번호가 정합니다**(`domain/coverPaper.ts`).
  무작위로 칠하면 알록달록해져 눈이 쉴 곳이 없고, 새로 고칠 때마다 색이 달라져 같은 책이
  다른 책으로 보입니다. **서가와 「이 책 주변 서가」가 같은 규칙을 써야 합니다.** 갈리면
  녹색 책을 눌렀는데 갈색 책이 열립니다.
- **표지가 오기 전에 자리를 잡아 둡니다**(`.cover__art` 의 `aspect-ratio: auto 1 / 1.45`).
  `height: auto` 만 두면 그림이 오기 전 이미지 높이가 **0** 이라, 표지가 도착할 때마다 그
  칸이 바닥에서 솟아오릅니다. 한 줄에 네 권이 제각각 솟으니 **스크롤하는 동안 서가 전체가
  계속 출렁입니다.** 실측으로 56칸 가운데 12칸이 높이 0 이었습니다. **`auto` 를 빼지
  마세요.** 빼면 그림의 실제 비율을 무시하고 모든 표지가 같은 비율로 눌리는데, 판형이
  제각각인 채로 아래 선에 맞춰 서는 것이 이 화면의 핵심입니다.
- **표지를 서서히 나타나게 하지 마세요.** 한 번 넣었다가 뺐습니다. 보이는 줄만 그리므로
  스크롤하면 표지가 끊임없이 새로 붙는데, 나타나는 효과는 붙을 때마다 투명도가 0 에서
  시작합니다. 그래서 **줄이 지나갈 때마다 네 권이 한꺼번에 깜빡이고**, 빠르게 밀면 화면
  전체가 반짝거립니다. 실제로 「반짝거리는 현상이 너무 많이 보인다」는 말을 들었습니다.
  헤드리스 크로미움으로 스크롤하며 2,828번 재어 보니 **288번이 투명한 상태**였고, 빼고
  나서 0번입니다. 자리는 바로 위의 `aspect-ratio` 가 이미 잡아 두므로 배치는 흔들리지
  않습니다. **붙는 것이 눈에 띄는 것과 화면이 반짝거리는 것은 다릅니다.**
- **보이는 줄만 그립니다**(`domain/shelfLayout.ts`). 20만 권이면 5만 줄이라 전부 DOM 에
  올리면 브라우저가 멈춥니다. 여기서 한 줄만 틀려도 스크롤할 때 책이 건너뛰거나 겹치는데,
  **빠르게 밀 때만 잠깐 보여서 눈으로는 잡기 어렵습니다.** 화면에서 떼어 두었습니다.
  **한 줄이 두 조각에 걸칠 수 있습니다.** 조각 크기가 네 권으로 나누어떨어지지 않으면 늘
  그렇고, 그때 한 조각만 받으면 그 줄의 뒷부분이 빈 채로 남습니다.
- **접는 자리와 펴는 자리를 벌려 두세요. 한 값으로 되돌리지 마세요**(`useCollapsed`).
  머리말이 접히면 **문서가 그만큼 짧아지고**(실측 144px → 57px), 브라우저가 보던 자리를
  지키려고 **스크롤 위치를 그만큼 되돌립니다**(scroll anchoring). 기준이 하나면 그 순간
  기준 아래로 내려가 다시 펴지고, 펴지면 문서가 길어지며 스크롤이 밀려 올라가 또
  접힙니다. **손을 떼도 혼자 펴졌다 접혔다 합니다.** 실측으로 740px 로 보낸 스크롤이
  734px 에, 760px 이 736px 에, 780px 이 742px 에 멈췄습니다. 벌린 폭(130px)이 줄어드는
  높이(87px)보다 커야 어느 쪽으로 넘어가도 되돌려진 자리가 반대쪽 기준을 넘지 못합니다.
  **그리고 접힘이 바뀌면 서가가 시작하는 자리를 다시 재세요.** 그 자리가 87px 올라오는데
  그것으로 몇 번째 줄인지를 세므로, 다시 재지 않으면 접힌 동안 줄이 한 줄 가까이 어긋나
  스크롤할 때 책이 건너뛰거나 겹쳐 보입니다.
- **접히는 머리말에 한 겹을 더 두세요.** `grid-template-rows` 를 `1fr` 에서 `0fr` 로 옮겨
  접는데, 그 규칙은 **줄 하나**를 정의합니다. 자식이 둘이면 둘째가 암묵적 줄로 밀려 높이가
  `auto` 로 남아서 **글자만 투명해지고 자리는 그대로 남습니다.** 실제로 접었는데도 머리말이
  117px 이었고 서가 위에 빈 띠가 생겼습니다.
- **기준일을 값에서 떼지 마세요.** 이 서가는 실시간이 아니라 그날 받아 둔 것입니다. 날짜가
  없으면 사람은 그것을 지금 상태로 읽고, 그 뒤에 들어온 새 책이 없는 것을 고장으로 여깁니다.
- **세우는 동안 진행률을 보여 주세요.** 빙글빙글 도는 표시만 두면 멈춘 것과 구별되지 않아
  사람이 새로 고칩니다. 서버는 돌고 있던 것을 그대로 두므로 **실제로는 아무것도 되돌아가지
  않는데, 그래도 사람은 그것을 알 방법이 없습니다.**
- **안쪽 사정을 화면에 적지 마세요.** 세 가지를 차례로 넣었다가 전부 뺐습니다. 차림표
  아래의 「정보나루가 서가 순서로 주지 않아서…」, 갈래마다 붙인 「바로 열림」·「처음
  여는 서가」, 기다리는 화면의 「다음부터는 바로 열립니다」입니다. **셋 다 우리가 안쪽에서
  어떻게 해 두었는지를 말하는 것이지 읽는 사람이 할 수 있는 일이 아닙니다.** 읽고 싶은
  갈래를 고를 뿐이고, 기다려야 하면 기다리는 화면이 그때 말해 줍니다. 검색 칸 아래의
  띄어쓰기 안내를 뺀 것과 같은 이유입니다. **다시 넣지 마세요.** `built` 값 자체는
  진단에 쓰므로 응답에 그대로 둡니다. 순위 숫자를 응답에는 두고 화면에는 적지 않던 것과
  같습니다.
- **기다리는 화면이 할 말은 둘뿐입니다.** 무엇을 하는 중인가(「서가를 둘러보는
  중입니다」)와 어디까지 왔는가(막대와 권수)입니다.
- **서가 안에 출처를 다시 적지 마세요.** 한때 서가가 화면을 통째로 덮어 푸터가 가려졌기
  때문에 넣었던 것인데, 다른 탭과 같은 자리에 그리게 되면서 **푸터가 그대로 보입니다.**
  같은 화면에 출처가 두 번 나오면 하나가 다른 출처처럼 읽힙니다.
- **「이 책 주변 서가」는 서가 위에 덮습니다.** 화면을 갈아 끼우면 서가가 처음부터 다시
  그려져 보던 줄을 잃습니다. 그리고 **옆자리는 새로 물어볼 것이 없습니다.** 이미 청구기호
  순으로 잘라 둔 파일의 앞뒤 칸이라, 조각 경계를 넘을 때만 이웃 조각을 한 번 더 받습니다.
- **「이 책 주변 서가」의 대출 확인은 ISBN 하나만 물어봅니다.** 「저작에 묶인 판본을 전부
  물어보라」는 규칙은 **그 도서관이 어느 판을 가졌는지 모를 때**의 규칙인데, 서가에 선 이
  책은 정보나루가 **그 도서관의 장서로 알려 준 바로 그 ISBN** 입니다. 청구기호까지 함께 온
  그 한 권을 눈앞에서 보고 있는 것이라, 다른 판을 끼워 물으면 **옆 서가에 있는 다른 책의
  대출 상태를 이 책의 것으로** 보여 주게 됩니다. **이 화면에 소장 조회를 붙이게 되면 이
  예외도 함께 거둬들여야 합니다.** 그리고 단추는 **누를 때만** 부르고 답에서 기준 날짜를
  떼지 않습니다. 둘 다 `LoanCheck` 가 이미 지킵니다.
- **자리 눈금은 손으로 끕니다**(`.nearby__ruler`). 좌우로 한 권씩 넘기는 것만으로는
  수천 권짜리 서가를 가로지를 수 없습니다. 눈금이 이미 「전체에서 어디쯤인가」를 말하고
  있으므로 그것을 끌 수 있게 합니다. **보이는 선은 3px 이지만 누르는 자리는 44px 입니다.**
  선만 한 자리는 손가락으로 집을 수 없습니다. 그리고 **`touch-action: none` 이 꼭
  필요합니다.** 없으면 브라우저가 가로로 끄는 것을 스크롤로 먼저 가져가서 자리는 안
  움직이고 화면만 흔들립니다. **바깥 상자의 좌우 스와이프도 막아야 합니다.** 그러지 않으면
  한 번 끌 때 자리가 두 번 움직입니다.
- **「서가 번호」는 정보나루가 주지 않습니다.** 가장 가까운 값이 `shelf_loc_name`
  (「[상동도서관]부천작가(1층)」)이고, 자리를 가리키는 것은 선반 라벨의 **청구기호 범위**
  입니다. 없는 번호를 지어내지 마세요.

### 서가 안에서 찾는 것은 검색이 아니라 길 찾기입니다

서가 화면에 검색이 없다는 규칙은 그대로입니다. 찾는 책이 있는 사람은 검색 탭으로 갑니다.
그런데 **서가 앞에 선 사람에게도 「그 자리로 가고 싶다」가 있습니다.** 수천 권짜리 서가를
손가락으로 밀어 가로지를 수는 없고, 초성 색인은 도서기호 첫 글자까지만 데려다줍니다.
머리말의 「책 찾기」(`ShelfSearch`)가 하는 일은 **이 서가의 자리 번호를 돌려주는 것**뿐이고,
서가 화면은 그 줄로 스크롤하고 「이 책 주변 서가」는 그 자리로 옮겨 갑니다. 소장도 대출도
말하지 않습니다.

- **못 찾은 것은 「이 서가에는 없습니다」이지 「그런 책이 없다」가 아닙니다.** 보고 있는
  것은 한 도서관의 한 갈래, 그중 한 자료실입니다. 그 책이 **다른 자료실이나 다른 갈래에
  서 있을 수 있고** 다른 도서관은 보지도 않았습니다. 그리고 **못 물어본 것과 없는 것을
  갈라 말합니다.** 앞은 잠시 뒤 다시 하면 되고 뒤는 다른 갈래를 봐야 합니다. 「미소장」과
  「확인 불가」를 갈라 두는 것과 같은 자리입니다.
- **색인은 수집할 때 함께 적고 줄 번호가 곧 자리 번호입니다**(`ShelfFind`, `find.txt`).
  그래서 찾는 일이 파일 한 번 훑기로 끝나고 **정보나루 호출이 0건**입니다. 조각을 열
  필요도 없습니다. **빈 줄도 한 줄입니다.** 표제도 저자도 없는 자료의 줄을 건너뛰면 그
  뒤의 자리 번호가 전부 한 칸씩 밀리는데, 화면은 그 번호로 조각을 찾아가므로 **엉뚱한 책
  앞에 서고 틀렸다는 것은 아무 데도 드러나지 않습니다.**
- **정규화한 형태를 파일에 적어 두지 마세요.** 파일이 두 배가 되는 것도 문제지만, 적어 둔
  것은 **규칙을 고쳐도 그대로 남아** 화면과 어긋납니다. 받아서 그때 정규화하면 몇
  밀리초입니다. 쓰는 함수는 `BibNormalizer.spellingKey` 입니다. `normalizeKey` 는
  「청소년판」 같은 표기를 떼는데, 그것은 같은 저작으로 묶을 때의 규칙이고 **여기서 견주는
  것은 사람이 서가에서 보고 있는 글자**입니다.
- **색인이 없는 서가는 낡은 것으로 보고 다시 세웁니다**(`ShelfMeta.findable`,
  `ShelfService.outOfDate`). 색인이 생기기 전에 세운 서가가 그렇고, 권수가 그대로면 갱신도
  건너뛰므로 **아무도 손대지 않으면 영영 없습니다.** 그렇다고 **`ShelfSortKey.VERSION` 을
  올려서 해결하지 마세요.** 그 번호는 「순서를 다시 적었다」는 뜻이라, 순서를 고치지도 않고
  고쳤다고 말하게 됩니다. 그동안 **화면은 찾기 단추를 내지 않습니다.** 눌러 봐야 실패하는
  단추를 두면 사람이 자기가 뭘 잘못했나 싶어집니다.
- **글자를 칠 때마다 묻지 않습니다.** 한글은 조합하는 동안 낱자가 오가서 「ㅌ」, 「토」,
  「톶」처럼 아직 말이 아닌 것으로도 묻게 되는데, 답은 쓸모가 없고 요청만 늘어납니다.
  그리고 **한 글자로는 찾지 않습니다**(`ShelfFind.MIN_QUERY`). 수천 줄에 걸려 아무 데도
  데려다주지 못하면서 파일을 통째로 훑습니다. 그 자리는 초성 색인이 이미 맡고 있습니다.
- **찾기의 무게는 조각 읽기보다 무겁고 서가 세우기보다 가볍습니다**(`costOf` 에서 2).
  정보나루를 부르지는 않지만 자료실 색인을 통째로 훑습니다.
- **자리로 옮겨만 놓지 말고 어느 것인지 표시하세요**(`ShelfCover` 의 `found`). 한 화면에
  스무 권이 서 있고 표지에는 글자가 없어서, 그 앞에 세워 주고 끝내면 사람이 다시 하나씩
  눌러 보게 됩니다.
- **옮겨 갈 때 붙박이 머리말이 덮는 높이를 빼세요**(`scrollToRow` 의 `headHeight`).
  빼지 않으면 찾아간 책이 머리말 뒤에 반쯤 가린 채 섭니다. 실측으로 표지 106px 가운데
  42px 이 가려졌습니다. **지금 잰 값이라야 맞습니다.** 내려가면 머리말이 접혀 문서가 그만큼
  짧아지고 서가가 시작하는 자리도 같은 만큼 올라오므로, 두 값이 서로 지워져 펼친 채로
  계산하든 접힌 채로 계산하든 책이 서는 자리가 같아집니다. **초성 색인과 찾기가 같은
  계산을 씁니다**(`scrollToIndex`). 갈리면 한쪽만 가려 서는데, 어느 쪽인지는 눌러 본
  사람만 압니다.
- **펼치면 머리말이 높아지므로 자리를 다시 재야 합니다.** 그래서 펼침 상태를 `Shelf.tsx`
  가 들고 있고(`finding`), 접힘과 같은 자리에서 함께 다시 잽니다. 이 상태를 컴포넌트 안에
  가두면 서가가 시작하는 자리가 움직인 것을 모른 채 줄을 세게 되어, 스크롤할 때 책이
  건너뛰거나 겹쳐 보입니다.
- **입력 칸에 `size={1}` 을 주세요.** 아무것도 적지 않으면 `input` 은 스무 글자
  폭(190px)을 **최소 너비로** 주장하고, 그러면 320px 화면에서 머리말이 화면보다 넓어져
  **페이지가 통째로 가로로 밀립니다.** 실측으로 문서가 359px 이 되어 도서관 선택 칸까지
  함께 넘쳤습니다. CSS 의 `min-width: 0` 은 이 최소 너비를 지우지 못합니다.
- **「이 책 주변 서가」의 찾기 판은 불투명해야 합니다**(`.nearby__bar .find`). 그 화면의
  머리 줄은 서가 위에 띄워 둔 것이라 아래의 서가 라벨을 밀어내지 못합니다. 반투명하게
  두면 라벨이 목록 글자 사이로 비쳐 **청구기호가 읽히지 않습니다.**

### 대출 상태는 누를 때만, 날짜와 함께 보여 줍니다

**원래는 아예 표시하지 않기로 했던 것을 뒤집은 결정입니다.** 되돌리지 마세요.

정보나루가 주는 대출 가능 여부는 **조회일 기준 전날의 상태**입니다(매뉴얼 11절에 그렇게
적혀 있습니다). 실시간이 아니므로 그대로 보여 주면 헛걸음을 만듭니다. 그래서 두 가지를
지키는 조건으로 보여 줍니다.

**첫째, 목록에 미리 붙이지 마세요.** `bookExist` 는 (도서관 하나 × ISBN 하나)를 받습니다.
여러 권 검색 화면에 그냥 달면 30권 × 판본 3개 × 도서관 20곳 = **1,800회**가 되고, 하루 한도
30,000건이 **열여섯 번 만에** 사라집니다. 사용자가 그 도서관을 눌렀을 때만 부릅니다.
그때는 1회입니다.

**한 권 검색과 여러 권 검색이 같은 문구 함수를 씁니다**(`domain/loanStatus.ts`,
`components/LoanCheck.tsx`). 한 권 검색은 소장 도서관마다 `LoanCheck` 단추를 달고, 여러 권
검색은 ③ 의 도서관 줄에 **그 도서관에 있는 책 전부의 대출 상태를 한 번에 묻는 단추**
(`LibraryLoanSweep`) 하나만 둡니다. 「이 도서관에 가면 이 중에 몇 권을 빌릴 수 있나」가 실제
행동이기 때문인데, **그것도 누를 때만, 그 도서관에 있는 책 수만큼만, 차례로** 부릅니다.
여러 권 검색의 줄마다 도서관마다 단추를 달지 마세요. 같은 단추가 (줄 × 도서관)만큼
흩어집니다. 답은 `useLoanSweeps` 가 도서관 목록 전체에서 하나로 들고 있어 **줄을 접었다
펴도 남습니다.** 예전에는 펼친 줄 안에 상태가 있어서 다른 도서관을 펼치면 방금 물어본 답이
사라졌고, 호출은 이미 나갔는데 화면만 잃어 같은 것을 다시 눌렀습니다. 열쇠는 줄 번호가
아니라 책의 ISBN 목록입니다. 후보를 바꾸면 같은 줄이 다른 책이 되므로 줄 번호로 기억하면
예전 책의 답이 새 책의 것처럼 보입니다.

**둘째, 저작에 묶인 판본을 전부 물어보세요.** 대표 판본 하나만 물으면, 도서관이 2판을
가지고 있을 때 1판을 물어보고 **「이 도서관에는 없다」**는 답을 받습니다. 소장한다고
표시해 놓고 누르면 없다고 하는 셈이라 소장 정보 자체를 믿지 못하게 만듭니다.
소장 조회에서 지키는 규칙이 여기서도 그대로 적용됩니다. 하나라도 찾으면 거기서 멈춥니다.

그리고 **전부 물어봤는데도 없다고 나오면 「없습니다」라고 단정하지 마세요.** 소장
조회(`libSrchByBook`)와 대출 조회(`bookExist`)의 답이 어긋난 것이고 어느 쪽이 맞는지
우리는 모릅니다. 「소장 목록에는 있는데 대출 정보에는 안 잡힙니다」로 적습니다.

**셋째, 기준 날짜를 값에서 떼지 마세요.** 「대출 가능」 네 글자만 남으면 사용자는 그것을
지금 상태로 읽습니다. 문구는 `frontend/src/domain/loanStatus.ts` 한 곳에서 만들고, 거기서
항상 날짜를 함께 돌려줍니다. 화면마다 따로 적기 시작하면 언젠가 한쪽이 날짜를 빠뜨립니다.

그리고 **조회에 실패한 것과 「빌릴 수 없다」를 섞지 마세요.** 못 물어본 것은 못 물어봤다고
말합니다. 소장 여부에서 지키는 구분과 같습니다.

**넷째, 누르는 동안 단추의 폭이 변하면 안 됩니다**(`.chip__swap`). 「대출 상태 확인」이
「확인 중」으로 짧아지면 단추도 그만큼 좁아지는데, 줄어든 폭이 마침 도서관 이름 옆에
들어맞으면 **아래 줄에 있던 단추가 이름 옆으로 올라붙습니다.** 두 줄이던 도서관 한 곳이
한 줄로 줄었다가, 답이 도착해 문구가 제자리로 가면 다시 두 줄이 됩니다. 실측으로
줄 폭 320~360px 에서 그랬습니다. **누른 자리가 눈앞에서 두 번 움직이는 것**이라 사용자는
자기가 무엇을 눌렀고 답이 어디에 왔는지를 놓치고, 이름이 긴 도서관에서만 일어나 같은
목록 안에서도 어떤 줄은 뛰고 어떤 줄은 가만히 있습니다. 그래서 두 문구를 한 칸에 겹쳐 두고
쓰지 않는 쪽을 `visibility: hidden` 으로 감춥니다. 폭을 늘 긴 쪽이 정하므로 글꼴이 무엇이든
단추가 제자리에 있습니다. **`display: none` 으로 감추거나 삼항 연산자로 글자만 갈아 끼우는
쪽으로 되돌리지 마세요.** 자리가 흐름에서 빠지면 감춘 문구가 폭을 붙들지 못합니다. 표지가
열리지 않아도 자리를 남기는 것과 같은 규칙입니다.

**다섯째, 답은 자리가 남으면 이름 옆에 서고 모자라면 아래 줄로 통째로 넘어갑니다**
(`.lib > .loan`). 예전에는 늘 아래 줄이었습니다. 칩 옆에 붙이면 이름이 밀려 줄이 흐트러진다는
이유였는데, **폭의 기준을 문구 전체 길이로 두면 그 일이 일어나지 않습니다.** 통째로 들어갈
자리가 있을 때만 옆에 서고 아니면 아래 줄로 넘어가므로, 이름 칩과 답이 서로를 밀어 반씩
쪼개지는 일이 없습니다. **그래서 폭을 `100%` 로도 em 값으로도 잡지 마세요.** 앞엣것은 늘 한
줄을 통째로 쓰게 만들고, 뒤엣것은 글꼴에 따라 문구보다 좁아져 이름 옆에서 두 줄로 눌립니다.

**다만 휴대폰에서는 지금도 아래 줄입니다.** 실측(Noto Sans KR 13px, 화면 412px)으로 답에
남는 자리가 152px 인데 「대출 가능 (2026-09-09 기준)」이 168px 입니다. **16px이 모자랍니다.**
430px 부터 이름이 짧은 도서관이 한 줄에 들어가고, 넓은 화면에서는 긴 이름도 들어갑니다.
휴대폰에서도 한 줄에 넣으려면 문구를 줄여야 하는데 그것은 날짜 표기를 줄이는 일이라,
**위의 셋째 규칙(기준 날짜를 값에서 떼지 마세요)과 함께 판단해야 합니다.** 줄 여백이나
칩 사이를 좁히는 것으로는 6px 밖에 못 벌어 닿지 않습니다.

### 화면을 비운 사이 끊긴 요청은 돌아왔을 때 잇습니다

검색을 누르고 창을 최소화하거나 다른 앱으로 넘어가면 **나가 있던 요청이 끊깁니다.** 휴대폰은
앱을 바꾸는 순간 페이지가 통째로 얼면서 열린 연결이 닫히고, 데스크톱도 오래 숨겨져 있으면
같은 일이 생깁니다. 우리 요청은 **특히 길어서 이것을 정면으로 맞습니다.** `srchBooks` 한
번이 3.7초, `libSrchByBook` 한 번이 4.9초이고 스무 권 확인이 59초까지 가므로, 기다리다
다른 일을 하러 가는 것이 예외가 아니라 보통입니다.

**그것을 실패로 두면 화면에 「검색 서버에 연결하지 못했습니다」가 뜹니다.** 서버는 멀쩡한데
서버가 죽었다고 말하는 것이라, 우리가 줄곧 갈라 놓으려는 「서버가 없다」와 「서버는 있는데
물어보지 못했다」가 여기서 뒤바뀝니다. 운영자는 멀쩡한 서버를 다시 띄우고 사용자는 검색이
고장 났다고 읽습니다. **오류도 로그도 남지 않고, 창을 띄워 둔 채 기다린 사람에게는 멀쩡히
되기 때문에** 재현조차 어렵습니다.

- **끊긴 요청은 화면이 다시 보일 때 한 번 다시 보냅니다**(`domain/visibilityRetry.ts`).
  **숨겨진 사이에 곧바로 다시 보내지 마세요.** 같은 이유로 또 끊기고 하루 호출 예산만
  깎입니다. 비어 있는 쪽이 오히려 더 기다려야 하는 것과 같은 규칙입니다.
- **다시 보내는 것은 한 번뿐입니다.** 두 번째는 화면이 보이는 상태에서 나간 것이라 그 결과가
  진실입니다. 서버가 정말 죽어 있으면 그때는 「연결하지 못했습니다」가 맞는 답입니다.
- **서버가 답을 준 것은 다시 보내지 않습니다.** 429 를 다시 두드려 봐야 남은 몫만 깎이고
  답은 같습니다. 끊긴 것(`ApiUnavailable`)과 답을 받은 것(`ApiUnreadable`)은 다릅니다.
- **실패한 시점의 `visibilityState` 만 보면 안 됩니다.** 페이지가 얼어 있는 동안에는
  자바스크립트가 돌지 않아서, 끊겼다는 사실이 **사용자가 돌아온 뒤에** 보고되는 일이
  흔합니다. 그때 다시 보면 화면은 이미 보이는 상태라 「내내 보였다」로 잘못 읽혀 그냥
  실패로 나갑니다. 요청이 나가 있는 **동안 한 번이라도** 숨겨졌는지를 지켜봐야 합니다.
- **이 보호는 `api.ts` 의 통로 하나에만 있습니다.** 화면에서 `fetch` 를 직접 부르면 이것도,
  서버가 실어 보낸 오류 이유를 꺼내는 것도 함께 건너뜁니다. 그 화면만 조용히 예전 상태로
  돌아가는데 **눈에 띄지 않습니다.** `apiRetry.test.ts` 가 `fetch` 를 부르는 곳이 한 곳뿐인지,
  화면이 직접 부르지 않는지를 함께 고정합니다.
- **낫지 않는 경우가 하나 있습니다.** 브라우저가 메모리를 되찾으려고 탭을 아예 버리면
  돌아왔을 때 페이지가 새로 열리므로 검색하던 상태가 남아 있지 않습니다. 그때는 다시
  검색해야 하고, 이것은 우리가 막을 수 있는 것이 아닙니다.

### 화면에 반드시 표시할 것

- **`getCurrentPosition` 의 `timeout` 만 믿지 마세요.** 그 시계는 **권한을 얻은 뒤에야**
  돌기 시작합니다. 권한이 막혀 있거나 사용자가 물음에 답하지 않으면 **성공도 실패도
  돌아오지 않습니다.** 실제로 「내 위치 다시 잡기」를 누르면 버튼이 「다시 잡는 중입니다」로
  바뀐 채 **15초가 지나도 그대로였고, 잠겨 있어 다시 누를 수도 없었습니다.** 사용자에게는
  그것이 「아무리 눌러도 안 잡힌다」로 보입니다. 그래서 `LOCATE_GIVE_UP_MS` 로 **우리 쪽에서도
  시계를 겁니다.** 그리고 **실패 이유를 갈라 말합니다.** 권한이 막힌 것은 브라우저 설정을
  바꿔야 낫고 기기가 못 잡은 것은 잠시 뒤 다시 누르면 되는데, 한 문구로 뭉뚱그리면 고칠 수
  있는 것을 못 고칩니다.
- **다시 잡았는데 값이 같으면 그 사실을 말합니다.** 아무 표시도 없으면 사용자는 자기가 누른
  것이 먹히지 않았다고 읽고 같은 자리를 계속 누릅니다. 유선 데스크톱처럼 Wi-Fi 나 GPS 를
  쓸 수 없는 기기는 몇 번을 눌러도 같은 IP 기반 위치가 나옵니다.
- **고른 도서관은 이름을 늘어놓고 그 자리에서 해제하게 합니다**(`LibraryPicker` 의
  `ChosenList`, `selection.ts` 의 `chosenLibraries`). 「n곳 선택됨」이라는 숫자만으로는
  무엇을 골랐는지 알 수 없고, 지역 계층은 접혀 있어 이미 고른 곳을 다시 찾으려면 시도와
  시군구를 차례로 펼쳐야 합니다. 어느 시군구였는지 기억나지 않으면 찾을 방법이 아예
  없어서, **한 곳만 빼려던 사람이 전체를 해제하고 처음부터 다시 고르게 됩니다.** 실제로
  그렇게 쓰고 있다는 말을 들었습니다. **목록만 스크롤하는 칸(`.picker__body`) 밖에
  둡니다.** 어느 탭에 있든 고른 것은 보여야 하고, 지역 계층을 훑어 내리는 동안 사라지면
  안 됩니다. **이름이 겹칠 때만 지역을 덧붙입니다.** 이름만으로는 같은 이름이 둘일 때
  어느 것을 지우는지 알 수 없습니다. 실제 목록 1,619곳에 아홉 곳이 그렇고(「아름드리작은
  도서관」 세 곳 등, 2026-09-13), 겹치지 않는 이름까지 달면 줄만 길어집니다.
  **많이 골랐을 때는 이름 대신 지역으로 묶습니다**(`chosenGroups`, `groupsWorthShowing`).
  지역 계층의 「서울특별시」를 한 번 누르면 **359곳**이 들어오는데, 그때 이름을 359개
  늘어놓으면 묻지도 않은 것에 길게 답하면서 정작 「서울 전체」라는 답은 하지 못합니다.
  여러 시도에 걸쳐 있으면 시도로, 한 시도 안이면 시군구로 묶습니다(서울은 스물다섯
  줄입니다). **개수만 보고 묶지 마세요.** 열세 곳을 여섯 구에서 골랐을 때 묶어 보니
  「1곳」짜리 묶음이 넷이라, 줄지도 않으면서 이름만 한 번 더 눌러야 보였습니다. 이름은
  사용자가 실제로 찾는 답이므로 감당되는 동안에는 이름을 보여 줍니다.
  **그리고 지역을 펼쳐도 다른 지역은 그대로 둡니다.** 예전에는 펼치면 나머지 지역 칩을
  지웠습니다. 둘을 함께 두었더니 위아래로 쌓여 **고르는 자리가 0px 이 되었기**
  때문인데(서울 전체에서 구 하나를 펼치자 위쪽 도서관 목록이 통째로 사라졌습니다),
  그것은 목록마다 제 창을 가져서 높이가 그대로 더해지던 때의 이야기입니다.
  **둘을 창 하나(`.chosen__scroll`)에 담으면 그 이유가 없어집니다.** 실측으로 펼치기
  전후의 고르는 자리가 1280×900 에서 341px 그대로, 412×915 에서 510px 그대로이고
  페이지 길이도 변하지 않습니다. 나머지를 지우는 쪽이 훨씬 나쁩니다. 지역을 여럿 고른
  사람은 하나를 펼칠 때마다 어디까지 봤는지 잃고, 옆 지역으로 건너가려면 매번 접어야
  합니다. **창을 하나로 유지하세요.** 안쪽 목록이 저마다 `max-height` 를 되찾으면
  스크롤이 두 겹이 되고 0px 문제도 함께 돌아옵니다.
  **지역 하나를 통째로 지우는 단추는 묶음 칩의 × 입니다.** 펼친 뒤에만 두었던 때에는
  지우려면 먼저 펼쳐야 해서, 실제로 「체크박스로 지우거나 하위를 전부 지워야 한다」는
  말을 들었습니다. 다만 **한 번 잘못 누르면 수백 곳이 사라지고 그것이 무엇이었는지조차
  보이지 않는다**는 사정은 그대로이므로 둘로 막습니다. **펼치는 자리와 지우는 자리를
  가르고**(한 칩처럼 보이지만 단추가 둘입니다), **지운 뒤에는 되돌릴 자리를 띄웁니다.**
  되돌리기는 **지운 직후 그대로일 때만** 살아 있습니다(`sameSelection`). 그 사이에 다른
  것을 건드렸는데도 남겨 두면 되돌리는 순간 그 조작까지 함께 지워져 **누른 적 없는
  해제**가 일어납니다. **× 를 글자로 적지 마세요.** `.chip--drop` 과 같은 이유입니다.
  **목록 자체도 높이를 제한해 그 안에서만 스크롤하되, 상한은 좁은 화면이 더 큽니다**
  (40vh, 넓은 화면은 30vh). 둘의 사정이 반대입니다. 넓은 화면에서 선택 칸은 화면 높이에
  매인 붙박이라 목록이 커진 만큼 **고르는 자리가 눌리지만**, 좁은 화면은 카드가 위아래로
  쌓이고 페이지가 통째로 스크롤되므로 창을 작게 잡을 이유가 없습니다. 30vh 로 두었더니
  320px 화면에서 **서른한 곳 가운데 넉 줄**만 보여 그 안에서만 일곱 번을 밀어야 했습니다.
  40vh 에서 여섯 줄이고 412px 에서는 지역 묶음 스물다섯 개가 한눈에 들어옵니다.
  **이 상한 덕분에 페이지 길이가 고른 수를 따라 늘지 않습니다.** 320px 실측으로 검색
  칸까지 아무것도 고르지 않았을 때 881px, 359곳을 골랐을 때 1,261px 이고 그 뒤로는 몇
  곳을 더 고르든 그대로입니다(2026-09-13). 늘어난 380px 의 대부분이 목록 창 하나이고,
  기본값 881px 의 대부분은 시도 열일곱 줄짜리 지역 계층입니다.
- **소장 정보의 조회 시각.** 캐시에서 나온 값이면 캐시된 시각을 그대로 씁니다. 사용자가 "미소장"을 "확실히 없다"로 읽을지 "한 달 전 기준이다"로 읽을지가 이 표시 하나에 달려 있습니다.
- **출처**: "출처: 도서관 정보나루 (국립중앙도서관)". 이용 조건상의 의무이면서 신뢰 표시이기도
  합니다. **가운뎃점으로 이어 두 곳처럼 적지 마세요.** 정보나루는 국립중앙도서관이 운영하는
  서비스이지 별개의 출처가 아니고, 지금 실제로 쓰는 출처는 정보나루 하나뿐입니다.
  국립중앙도서관 ISBN 서지정보 API 는 별도 서비스인데 아직 붙이지 않았으므로, 그것을 붙일 때
  출처를 하나 더 적습니다. 쓰지도 않는 출처를 적어 두면 표시 자체를 믿을 수 없게 됩니다.
  **같은 이유로 개인정보처리방침 링크도 출처와 한 줄에 두지 마세요.** 가운뎃점으로 이어
  붙였더니 「개인정보처리방침」까지 데이터를 받아 오는 곳 이름처럼 읽혔습니다. 성격이 다른
  것은 줄을 나눕니다.
- **누르는 자리는 한 모양으로 모읍니다**(`.chip`). 예전에는 같은 무게의 행동이 자리마다
  다르게 생겼습니다. 도서관으로 가는 것은 녹색 밑줄 링크, 대출 상태를 묻는 것은 녹색 밑줄
  텍스트 버튼, 정보나루로 가는 것은 **브라우저 기본 파란 링크**였습니다. 셋 다 「누르면
  무언가 일어난다」는 같은 말을 하는데 생김새가 셋이라 매번 다시 읽어야 했습니다.
  `.button` 은 그 화면의 주된 행동(검색, 확인) 하나에만 쓰고 나머지는 전부 `.chip` 입니다.
  **다만 문장 속에 섞이는 것은 `.link-button` 으로 남기세요.** 「목록 고치기」나 「내 위치
  다시 잡기」처럼 글 가운데 있는 자리에 칩을 넣으면 줄 높이가 튀어 문장이 읽히지 않습니다.
  **채운 칩(`.chip--strong`)은 「그 도서관으로 간다」는 뜻으로만 씁니다.** 두 화면에서 같은
  색이 같은 말을 해야 합니다. 화면 밖으로 나가는 것은 `.chip--go` 가 화살표를 붙이므로
  글자로 적지 마세요. 자리마다 손으로 적으면 언젠가 한쪽이 빠집니다.
- **칩의 무게는 색이 말합니다.** 짙은 녹색으로 채운 `.button` 이 주된 행동, 옅은 녹색으로
  채운 `.chip--strong` 이 「그 도서관으로 간다」, 흰 바탕에 어두운 글자의 `.chip` 이 그 밖의
  전부입니다. 예전에는 칩 글자가 전부 녹색이고 테두리가 구분선 색이라 단추로 보이지
  않았고, 실제로 「안 예쁘다」는 말을 들었습니다. 눌리는 것의 테두리는 `--chip-line`, 나누는
  선은 `--line` 입니다. **칩을 여럿 늘어놓을 때는 `.chips` 에 담으세요.** 연령 묶음이 이것
  없이 놓여 있어 여섯 개가 다닥다닥 붙어 있었습니다.
- **못 누르는 단추의 바탕을 건드릴 때는 글자색을 함께 보세요.** `.button:disabled` 가
  바탕을 짙은 녹색으로 되돌리는데, 그 선택자가 `.button--quiet` 보다 명시도가 높아서
  **바탕만 초록으로 덮이고 글자는 `.button--quiet` 의 초록으로 남았습니다.** 초록 바탕에
  초록 글자라 글자가 통째로 사라져서, 「더 보기」가 확인 중일 때 **아무 글자도 없는 흐린
  초록 사각형**이었습니다. 눌러야 하는 것인지조차 알 수 없고, 확인이 끝나면 글자가 돌아와서
  **잠깐 보고 지나치면 재현되지 않습니다.** 못 누른다는 것은 `opacity` 가 이미 말하므로
  바탕까지 바꿀 이유가 없습니다.
- **단추 글자가 바뀌는 자리는 전부 `.chip__swap` 입니다.** 「20개 더 보기」가 「확인 중」으로
  짧아지면 단추가 그만큼 줄고, `.more` 가 flex 라 옆의 글이 따라 움직입니다. 대출 상태
  확인에서 이미 겪은 것과 같은 문제인데 여기만 삼항 연산자로 글자를 갈아 끼우고 있었습니다.
  **한 곳에서 고쳤으면 같은 모양의 다른 자리도 함께 보세요.**
- **도서관 줄(`.lib`)은 테두리가 아니라 바탕색(`--tile`)으로 묶습니다.** 카드 테두리 안에
  테두리 상자, 그 안에 테두리 칩이 들어가면 선이 세 겹이라 어디까지가 한 도서관인지
  오히려 안 보입니다. 바탕색 위에 칩 둘을 놓으면 선은 칩 둘레 한 겹뿐입니다. 이미 바탕색을
  깐 상자(`.daily__card`) 안에서는 도서관 줄이 바탕을 다시 깔지 않습니다.
- **`a` 의 기본 색을 지우지 마세요.** 없으면 브라우저 기본 파란색이 나오는데, 팔레트를
  아무리 맞춰도 그 링크만 사이트 밖에서 온 것처럼 보입니다. 실제로 푸터의 두 링크와
  「정보나루에서 이 책 정보 보기」가 그렇게 파랬습니다.
- 리다이렉트가 상세 페이지인지 검색 결과인지 홈페이지인지 구분해서 표시합니다. 조용히 강등하면
  사용자는 검색 결과 자체가 틀렸다고 생각합니다. 문구 판정은 `frontend/src/domain/opacLink.ts`
  한 곳에서만 합니다.
- **도서관 이름을 누르면 그 도서관으로 갑니다.** 주소 규칙이 없으면 정보나루 책 상세로
  보내 봤는데, 규칙 표가 비어 있어서 결국 모든 도서관이 정보나루로 갔습니다. 도서관을
  눌렀는데 도서관이 아닌 곳이 뜨고, 소장 도서관이 스무 곳이면 같은 링크가 스무 번
  반복되고, 정작 대출 신청을 할 수 있는 곳으로는 못 갑니다. **정보나루 책 정보는 책마다
  한 번만 따로 답니다.**
- **도서관 링크에는 그 도서관이 실제로 가진 판의 ISBN 을 먼저 넣습니다**(`heldIsbns`,
  `opacLink.ts` 의 `isbnsForLink`). 예전에는 저작의 첫 ISBN(`isbn13List[0]`)을 넣었는데,
  소장 조회는 판본 전체의 합집합이라 **그 도서관이 다른 판만 가지고 있으면 OPAC 검색이
  규칙이 맞아도 0건**이 됩니다. 사용자에게는 「소장한다더니 그 책이 없네」로 보이고 판본이
  많은 책일수록 자주 그렇습니다. (ISBN, 지역) 호출은 어느 ISBN 이 그 도서관을 데려왔는지
  이미 알고 있으므로 `HoldingsLookup.Result.isbnsByLib` 가 그것을 버리지 않고 화면까지
  올립니다. **`libCodes` 만 보고 링크를 만들지 마세요.**
- 링크 단계 `DETAIL_LOOKUP` 은 「누를 때 서버가 검색 결과에서 상세를 찾아 보낸다」는 뜻이라
  **못 찾으면 검색 결과로 내려갑니다.** 문구가 그 가능성을 함께 말합니다(「찾지 못하면
  검색 결과」). 확정된 상세(`ISBN_DETAIL`)와 같은 말로 쓰면 검색 결과 화면에 도착한 사람이
  「페이지라더니」로 읽습니다.
- 캐시가 빈 상태의 여러 권 검색은 수십 초가 걸립니다. **책 정보를 먼저 그리고 소장 결과를 도착하는 대로 채웁니다.** 빈 화면으로 기다리게 하지 마세요.

### OPAC 주소 규칙을 추측해서 넣지 마세요

**정보나루는 홈페이지 주소만 줍니다.** 매뉴얼 1절과 13절의 응답 명세를 확인했고, 그 책의
상세 페이지 주소를 주는 항목은 없습니다(`bookDtlUrl` 은 인기대출도서 계열에만 있고 정보나루
자기 페이지입니다). 그래서 도서관별 주소 규칙을 우리가 들고 있어야 하고, 그것이
`backend/src/main/resources/opac/templates.csv` 입니다.

**규칙이 틀리면 그 도서관은 HTTP 200 을 주면서 결과만 0건이 됩니다.** 사용자에게는 「소장한다더니
그 책이 없네」로 보이고, 소장 정보 자체를 믿지 않게 됩니다. 링크가 깨지는 것보다 나쁩니다.
깨진 링크는 눈에 보이지만 이것은 보이지 않습니다. **실제로 열어서 확인한 것만 넣으세요.**

**그래서 지금은 규칙을 통째로 꺼 두었습니다**(`wimb.opac.links-enabled: false`). 표에 315줄이
들어 있지만 실제 OPAC 에서 확인한 것이 아니라, 위의 이유가 그대로 315곳에 적용됩니다.
**아래 규칙들은 없어진 것이 아니라 켜는 날을 기다리는 것입니다.** 규칙을 채우거나 고칠 때
그대로 지키세요. 확인하는 절차와 켜는 방법은 위의 「다음 작업」 절에 있습니다.

- 1,619곳을 다 채우려 들면 이 프로젝트는 끝나지 않습니다. **자주 가는 도서관부터 채우고 나머지는
  홈페이지 폴백에 맡깁니다.** 대신 어느 단계로 보냈는지 화면에 밝힙니다.
- **ISBN 검색을 제목 검색보다 위에 둡니다.** 정확해서이기도 하지만, 숫자라서 인코딩 문제가 아예
  없기 때문입니다. 한글 질의어를 EUC-KR 로 받는 OPAC 에 UTF-8 로 보내면 200 이 오면서 결과만
  0건이 됩니다. 그래서 규칙마다 인코딩을 따로 적습니다.
- 자리표(`{isbn13}`, `{title}`)가 없는 줄은 **서버가 뜨지 않게 막습니다.** 자리표가 없으면 어느
  책을 눌러도 같은 페이지가 나오는데, 조용히 엉뚱한 곳으로 보내느니 뜰 때 실패하는 편이 낫습니다.
- 크롤링으로 규칙을 자동 발견하려 하지 마세요. 주말을 통째로 쓰고 쓸 만한 결과가 나오지 않습니다.
  **`scripts/opac-discover.py` 는 이 규칙의 예외가 아니라 지키는 방법입니다.** 주소를
  짐작하지 않고 그 도서관이 **자기 검색 폼으로 알려 준** 주소를 써 본 뒤, 그 도서관이
  실제로 소장한 책으로 응답을 확인하고(양성), 존재하지 않는 ISBN 으로 검색어를 무시하는
  곳을 걸러 내고(음성), 다른 책으로 한 번 더 봅니다(재확인). 셋을 통과하지 못하면 버립니다.
  **음성 대조를 빼지 마세요.** 검색어와 무관하게 늘 목록을 뿌리는 OPAC 이 전부 「성공」이
  되어, 어느 책을 눌러도 남의 책이 뜨는 규칙이 들어갑니다. 그리고 양성에 쓰는 ISBN 은
  **그 도서관이 소장한 책이어야 합니다.** 아니면 0건이 나왔을 때 규칙이 틀린 것인지 그 책이
  없는 것인지 구별할 수 없습니다. 판정은 `scripts/test-opac-discover.py` 가 가짜 OPAC
  일곱 종류로 고정합니다.
- **다만 자동 조사는 실제로 돌려 보니 0/30 이었습니다(2026-09-09).** 판정이 아니라 대상이
  문제였습니다. **검색어가 경로에 들어가는 OPAC**(노원 `/KeywordSearchResult/{isbn13}`,
  부천 `/search/keyword/{isbn13}`)은 자바스크립트가 그 주소를 만들기 때문에 **HTML 에 그
  형태가 없어 폼을 읽는 방식으로는 못 찾습니다.** 사람은 주소창을 보고 5초면 압니다.
  거기에 국내 공공도서관 상당수가 해외 IP 를 막아 러너에서는 30묶음 중 18묶음이 접속조차
  되지 않았습니다. **그래서 지금 쓰는 방법은 사람이 주소를 주고 `--import-file` 로 반영하는
  쪽입니다.** 자세한 실측은 `docs/도서관-주소-규칙-채우기.md` 에 있습니다.
- **세션 토큰이 붙는 검색은 규칙으로 만들지 마세요.** 대구 달서구는 `_csrf=...` 가 들어가는데
  접속할 때마다 새로 발급되므로 **오늘은 되고 내일은 안 됩니다.** 그리고 **상세 페이지는
  대부분 내부 키**(`book_key`, `regNo`, `bookkey`)를 써서 책마다 달라집니다. 그래서
  `ISBN_DETAIL` 자리표 규칙은 상세 주소에 ISBN 이 그대로 들어가는 드문 OPAC 에만 있고,
  나머지는 아래의 **상세 패턴**으로 갑니다.
- **POST 로 검색하는 OPAC 이라고 포기하지 마세요.** 2026-09-11 에 POST 로 책이 나온 여덟
  묶음(39곳)이 전부 같은 파라미터를 GET 으로 붙여도 열렸습니다. 다만 `_csrf`·`CSRF_TOKEN`
  같은 **토큰 칸은 빼고** 확인하세요. 은평과 전주는 빼도 열렸고, 값이 남은 주소는 위의 규칙대로
  오늘은 되고 내일은 안 됩니다. 절차는 `docs/도서관-주소-규칙-채우기.md` 의 「자주 걸리는
  경우」에 있습니다.
- **묶음키를 소문자로 바꿔 견주지 마세요.** 묶음키의 경로 조각은 홈페이지 주소의 대소문자를
  그대로 살립니다(`www.goyanglib.or.kr/MF`). 가져오기(`--import-file`, `--import-patterns`)가
  받은 키만 소문자로 바꿔 견주던 때에는 고양시립 분관 규칙이 「모르는 묶음」으로 버려질 뻔했습니다.
  `resolve_group_key` 가 대소문자와 `www.` 를 가리지 않고 찾고 `test-opac-discover.py` 가 고정합니다.
- **상세 페이지는 누를 때 검색 결과에서 뽑습니다**(`kr.wimb.opac.DetailResolver`). 내부
  키는 미리 알 수 없지만 ISBN 검색 결과 안에는 그 링크가 있으므로, `/api/go` 가 그 페이지를
  받아 `detail-patterns.csv` 의 정규식(열쇠는 검색 주소의 호스트, 필요하면 `호스트/경로조각`)으로
  첫 번째 링크를 꺼내 302 로 보냅니다. **못 찾으면 검색 결과로 보냅니다.** 지금까지의 동작
  그대로라 나빠지는 것은 없습니다. 한 시스템에 패턴 한 줄이면 그 시스템의 도서관 전체가
  상세로 가므로, 사람이 채운 217줄은 열세 줄이면 됩니다. `DETAIL_LOOKUP` 은 표에 적는
  종류가 아니라 `ISBN_SEARCH` 규칙과 패턴이 함께 있을 때 `kindFor` 가 저절로 돌려줍니다.
- **패턴은 잡을 그룹이 정확히 하나여야 하고 빈 문자열에 맞으면 안 됩니다.** 둘 다 서버가
  뜰 때 막습니다. 그리고 **첫 번째로 잡히는 링크가 그 책이어야 합니다.** 서버는 첫 번째 것을
  쓰므로 「최근 본 책」이나 배너가 먼저 잡히면 어느 책을 눌러도 엉뚱한 곳으로 갑니다.
  다른 기관으로 가는 링크와 검색 결과 자기 주소는 해석기가 거릅니다. **`detail-patterns.csv` 에
  손으로 붙이지 마세요.** `opac-browse.py --emit` 이 낸 줄을 `opac-discover.py
  --import-patterns` 가 거쳐서 넣습니다. 정규식에 따옴표가 흔한데 `csv.writer` 로 쓰면 CSV
  규칙대로 따옴표를 감싸고 서버는 첫 쉼표 뒤를 글자 그대로 읽으므로 **깨진 정규식이 들어가
  서버가 뜨지 않습니다.** 그 통로가 글자 그대로 씁니다.
- **서버는 자바스크립트를 돌리지 않고 UA 가 `DetailResolver.USER_AGENT` 입니다.** 결과를
  자바스크립트로 그리는 OPAC 은 원문에 링크가 없어 패턴이 잡히지 않고, UA 에 따라 다른
  페이지를 주는 OPAC 이 있습니다. 그래서 조사 스크립트는 패턴을 **브라우저가 아니라 서버와
  같은 방식**(자바스크립트 없는 `fetch`, 같은 `UA`)으로 받은 HTML 에 대고 검증합니다.
  **UA 를 바꾸면 양쪽을 같이 바꾸세요.** `DetailResolverTest` 가 둘이 같은지 봅니다.
- **상대 링크는 브라우저 규칙(RFC 3986)으로 풀어야 합니다.** 자바의 `URI.resolve` 는 RFC 2396
  이라 `?query` 만 있는 상대 링크에서 **마지막 경로 조각을 버립니다.** `search00.do?a=1` 에
  `?b=2` 를 풀면 `/site/search/?b=2` 가 되는데 브라우저와 조사 스크립트의 `urljoin` 은
  `search00.do?b=2` 로 풉니다. 실제로 도봉의 상세 링크가 `?manage_code=...` 로 시작해서,
  스크립트가 확인한 주소와 서버가 만드는 주소가 갈렸습니다. `DetailResolver.resolve` 가
  그 경우를 따로 풀고 `DetailResolverTest` 가 고정합니다. **`base.resolve(href)` 로 되돌리지
  마세요.**
- **속성값의 HTML 엔티티는 서버와 똑같이 여섯 가지만 되돌립니다**(`attr_unescape`,
  `DetailResolver.unescape`). 파이썬의 `html.unescape` 는 HTML5 규칙대로 `&reg` `&copy`
  `&para` `&not` `&sect` `&times` 처럼 **세미콜론 없는 옛 이름도 글자로 바꿔서**
  `&regNo=SS1` 이 `®No=SS1` 이 됩니다. 실제로 순천의 상세 링크가 `&regNo=` 를 써서, 맞는
  패턴이 「세션에 묶인 키」로 잘못 버려졌습니다. 조사 스크립트에서 `html.unescape` 를 다시
  쓰지 마세요. `test-opac-discover.py` 가 이것을 고정합니다.
- **서버가 그 OPAC 에 닿는지는 배포된 곳의 나가는 IP 에 달렸습니다.** 국내 공공도서관
  상당수가 해외 IP 를 막고 우리 VM 은 미국 리전입니다. 패턴이 맞아도 그 OPAC 이 우리 IP 를
  막으면 `FETCH_FAILED` 로 검색 결과에 내려앉는데, **화면에서는 「패턴이 안 맞는다」와
  구별되지 않습니다.** `GET /api/diagnose/opac?lib=<부호>&isbn=<ISBN>` 이 지금 실제로 해 보고
  상태(`RESOLVED`/`NO_MATCH`/`FETCH_FAILED`/`BLOCKED_BY_ROBOTS`)를 답하며, `/api/status` 의
  `opacDetailResolved`·`opacDetailMissed`·`opacDetailFailed` 가 어디서 새는지 셉니다.
  **`FETCH_FAILED` 가 시스템 단위로 쌓이면 코드가 아니라 회선 문제입니다.** 그때의 답은
  국내 나가는 IP(국내 VPS 나 Oracle 서울 리전)이고, 정보나루 IP 등록도 함께 옮겨야 합니다.
- **해석은 남의 서버로 가는 요청입니다.** 답은 이레 동안 기억하고, 「링크 없음」은 한 시간만,
  실패는 기억하지 않습니다(소장 캐시와 같은 규칙). 같은 호스트에는 한꺼번에 둘까지만 나가고
  그 이상은 기다리지 않고 검색 결과로 보냅니다. `robots.txt` 가 막은 경로는 받지 않습니다.
  **첫 판에 링크가 없으면 다음 판으로 한 번 더 찾되(`MAX_DETAIL_ATTEMPTS`), 페이지를 아예
  못 받았으면 다른 판으로 또 두드리지 않습니다.** 닿지 않는 호스트를 두 번 기다리게 하는
  것이고 결과도 같습니다. `/api/go` 의 무게는 2 입니다(`RateLimitInterceptor.costOf`).
- **조사 도구는 같은 창을 먼저 봅니다. 순서를 되돌리지 마세요.** 새 창부터 보던 때에는 같은
  창에서도 책이 안 나오는 곳이 전부 「새 창에서 열면 그 책이 나오지 않습니다」로 찍혀 세션
  문제로 읽혔는데, 2026-09-11 에 그 사유로 끝난 14묶음을 따로 확인해 보니 세션 문제는 한 곳도
  없었습니다. 대부분 그 검색이 ISBN 을 못 찾는 경우였습니다. 도구는 이제 주소창에 검색어가
  없으면 실제로 나간 요청(넘어가기 전 GET, POST 본문)으로 GET 주소를 만들고, 검색 항목에 ISBN 이
  있으면 고르며, 이 기계의 네트워크가 끊겨 실패한 묶음은 기록하지 않습니다. robots.txt 는
  **홈페이지부터** 지킵니다. `scripts/test-opac-browse.py` 가 이것들을 가짜 OPAC 으로 고정합니다.
- **자동 조사를 다시 돌린다면 `scripts/opac-browse.py` 로, 국내 회선에서 돌리세요.**
  0/30 이었던 원인 둘이 여기서 사라집니다. 검색어가 경로에 들어가는 OPAC 은 자바스크립트가
  주소를 만들어 **HTML 에 그 형태가 없는데**, 브라우저는 그것을 주소창에서 그냥 읽습니다.
  해외 IP 차단은 국내에서 돌리는 것으로 풀립니다. **판정은 그대로 세 겹이고, 확인을
  쿠키 없는 새 창에서 하는 것이 추가되었습니다.** 주소창에는 검색어가 남는데 새 창에서
  열면 무시하는 OPAC 이 있어서(제주·구미·용산), 같은 창에서 확인하면 **죽은 링크가 규칙으로
  들어갑니다.** **상세 페이지 규칙도 기계가 시도합니다.**
  검색 결과에서 책을 눌러 들어간 주소에 ISBN 이 그대로 있으면 `ISBN_DETAIL` 을 얻고,
  다른 책으로 바꿔 열어 확인합니다. **내부 키를 쓰는 곳은 두 책의 상세 href 에서 공통
  앞머리를 찾아 상세 패턴(`DETAIL_PATTERN`)을 만들고**, 양성(첫 번째로 잡히는 링크가 그
  책)·음성(없는 ISBN 에는 안 잡힘)·재확인(뽑은 링크가 새 창에서 열림)을 거칩니다.
  `--patterns` 는 이미 검색 규칙이 있는 도서관에 대해 이 상세 단계만 돌립니다.
  **다만 `TITLE_SEARCH` 는 만들지 않습니다.** 한글 질의어의 인코딩을 자동으로 틀리면 200 이 오면서 결과만 0건이
  되어 눈에 띄지 않으므로, 그 자리는 사람이 주소창을 보고 넣습니다.
  `scripts/test-opac-browse.py` 가 같은 사이트를 폼 읽기로는 못 찾고
  브라우저로는 찾는 것까지 함께 고정합니다. 로컬 세션에 넘기는 말은
  `docs/로컬에서-규칙-자동으로-채우기.md` 에 있습니다.
- **한 시스템이 묶음 둘로 갈리면 큰 도서관이 조용히 빠집니다.** 묶음키가 홈페이지의 첫
  경로 조각까지 보는데, 작은도서관들은 대개 시스템 최상위 주소(`www.nowonlib.kr`)를 함께
  쓰는 반면 **사람이 실제로 가는 큰 도서관은 분관 페이지를 따로 가집니다**
  (`nowonlib.kr/jungang`). 그래서 주소 하나를 확인해 넣었더니 노원은 **작은도서관 스물일곱
  곳만 채워지고 노원중앙·상계·불암·화랑을 비롯한 여덟 곳이 홈페이지로 갔습니다.**
  **화면에는 아무 이상이 없어 보입니다.** 이름이 나오고 눌리기도 하니 도착한 곳이 검색
  결과가 아니라는 것은 눌러 본 사람만 압니다. `./scripts/opac-discover.py --gaps` 가
  「규칙이 있는 기관인데 규칙을 못 받은 도서관」을 셉니다. 조사 결과의 묶음키 뒤에 `*` 를
  붙이면 같은 기관 전체로 넓어집니다. **다만 넓히는 것을 기본으로 삼지 마세요.** 분관마다
  장서가 따로인 시스템에서 넓히면 어느 책을 눌러도 남의 도서관 목록이 뜹니다. 안산의 지금
  규칙이 `lib.ansan.go.kr/smalllib/...` 라 **작은도서관 전용 검색**입니다. 검색 결과
  화면에 그 분관들의 이름이 실제로 보이는 것을 확인한 뒤에만 붙입니다.
- **우리가 센 소장 도서관 수와 그 도서관 OPAC 의 검색 건수는 다를 수 있습니다.**
  실측으로 노원에서 우리가 「21곳」이라고 답한 책이 노원 통합검색에서는 14곳이었습니다
  (2026-09-10). 양쪽이 서로에게 없는 것을 가지고 있었고, 정보나루 목록에 아예 없는
  도서관(마들이음)과 소속 기관이 다른 도서관(서울시교육청노원평생학습관은 `nwllc.sen.go.kr`)이
  섞여 있었습니다. **정보나루의 소장 정보는 도서관이 주기적으로 올린 것이고 OPAC 은 지금
  상태입니다.** 그래서 규칙이 맞아도 어긋납니다. **이것을 규칙이 틀린 증거로 삼지 마세요.**
  화면의 「조회 기준」 날짜와 출처 표시가 이 어긋남을 설명하는 유일한 장치라 지우면 안 됩니다.
- **`.gitignore` 의 `*.csv` 가 이 파일을 삼키고 있었습니다.** 예외를 `!src/**/*.csv` 로 적어
  두었는데, 패턴 중간에 `/` 가 있으면 저장소 최상위에 고정되므로 `backend/src/...` 에 닿지
  못합니다(래퍼 JAR 에서 이미 겪은 것과 같은 함정이라 `!**/src/**/*.csv` 로 고쳤습니다).
  규칙을 채워도 커밋되지 않고 이미지에도 실리지 않는데, **화면은 그대로 홈페이지로 가므로
  「규칙이 틀렸나」를 먼저 의심하게 됩니다.** 채운 뒤에는 `git status` 에 그 파일이
  보이는지 확인하세요.
- 채우는 절차와 사람이 채워야 하는 표는 `docs/도서관-주소-규칙-채우기.md` 에 있습니다.

---

## 절대 바꾸면 안 되는 식별자

**`library.short_id`와 `work_id`는 재사용하지도, 변경하지도 않습니다.** 둘 다 공유 URL에 들어가므로 재색인 때 번호가 바뀌면 공유된 링크가 전부 조용히 깨집니다.

`work_id`는 재군집화 때 최빈값으로 승계하고, 흡수된 이전 번호는 `bib_work_alias`에 남겨 301로 넘깁니다.

**그리고 `short_id` 가 촘촘한 번호라고 가정하지 마세요.** 지금 서버가 주는 값은 도서관부호
그대로라(`LibraryDto.shortIdOf`) 50,001 부터 14,703,813 까지 흩어져 있습니다. 선택을 주소에
담는 `selectionUrl.ts` 가 **고른 곳이 100 을 넘으면 비트맵으로 바꾸던 때**, 비트맵이 가장
큰 번호만큼 자리를 잡는 탓에 서울 359곳이 **1.4MB** 가 되고 base64 로 바꾼 **1.9MB 짜리
주소**를 선택이 바뀔 때마다 `replaceState` 가 받아 갔습니다. **체크박스 한 번에 화면이
3초씩 멈췄고, 그 길이로는 공유도 되지 않습니다.** 고친 뒤 같은 선택이 2,545자입니다.
**목록과 비트맵은 개수가 아니라 실제 길이로 견주세요.** 비트맵 크기는 가장 큰 번호만으로
정해지므로 만들기 전에 알 수 있고, 그래야 그 할당 자체를 치르지 않습니다.
증상이 「칩이 많이 그려져서 느리다」로 보이지만 **칩 57개짜리가 141ms, 칩 2개짜리가
3,163ms 였습니다.** 실측은 `docs/가정과-검증상태.md` 2-20 에 있습니다.

---

## 서지 정규화

### 판본 표기와 각색 표기를 혼동하지 마세요

이 두 목록을 섞는 것이 **오병합의 가장 큰 원인**입니다.

- **판본 표기는 제거하고 묶습니다**: `개정판 개정증보판 증보판 전면개정판 개역판 신판 초판 재판 제2판 2판 양장본 양장 반양장 무선 보급판 문고판 특별판 한정판 리커버 합본판 완역본`
  같은 내용의 다른 인쇄이므로 묶고, 화면 표시만 다르게 합니다.
- **각색 표기는 병합 거부의 근거로 씁니다**: `만화 만화로보는 청소년판 어린이 그림책 요약 축약본 쉽게읽는 큰글자책 큰글씨책 점자 오디오북 전자책 영어판 일본어판 원서 대활자본`
  내용이 다른 별개 저작입니다. 「데미안」과 「데미안(청소년판)」은 정규화하면 키가 같아지므로, 각색 표기가 없으면 반드시 잘못 합쳐집니다.

권차도 키에 남깁니다. 「미움받을 용기」와 「미움받을 용기 2」는 다른 책입니다.

### 출판사가 다르면 병합하지 마세요

**번역서에서 이것이 결정적입니다.** 표제가 「마의 산」이고 저자가 「토마스 만」이면 R3 이
전부 한 저작으로 합치는데, 실제로 받아 보니 **범우사·을유문화사·열린책들·동서문화사·
지식을만드는지식의 서로 다른 번역 열 판**이 그렇게 묶여 있었습니다. 번역이 다르면 읽는
사람에게는 다른 책입니다.

합쳐 놓으면 두 가지가 함께 무너집니다. 화면에는 대표 하나의 출판사만 남아 **나머지 판을
찾는 사람에게 「그 책이 없다」로 보이고**, 소장 조회는 묶인 ISBN 전체로 나가므로 **범우사
판만 있는 도서관이 「열린책들 마의 산 있음」으로 나옵니다.** 뒤엣것이 더 나쁩니다. 찾는
판이 없는데 있다고 답해 헛걸음을 만들기 때문입니다. **낱권을 권차로 갈라 놓아야 했던 것과
같은 이유이고, 같은 모양의 사고입니다.**

- **한쪽이라도 출판사를 모르면 갈라 놓지 않습니다.** 모르는 것을 근거로 쪼개면 같은 책이
  흩어지고, 소장 조회가 물어보는 ISBN 이 그만큼 줄어 실제로 소장한 책을 놓칩니다.
- 같은 ISBN 은 R0 이 먼저 병합하므로 출판사 표기가 흔들려도 안전합니다.
- 법인격 표기(`(주)`, `주식회사`)는 `normalizePublisher` 가 떼므로 차이로 세지 않습니다.
- **`golden-pairs.tsv` 는 표제와 저자만 담고 있어 이 규칙을 검증하지 못합니다.**
  출판사 판정은 `WorkMatcherTest` 가 실제로 받은 값으로 고정합니다.

### 그 밖의 규칙

- **정규화 함수는 한 곳에만 둡니다.** 적재와 질의가 같은 함수를 호출해야 합니다. 서로 다른 정규화를 적용하면 조용히 어긋납니다.
- 저자 대조는 문자열 비교가 아니라 **토큰 집합과 말뭉치 빈도**로 합니다. `롤링`은 희소해서 결정적이지만 `김`은 아무것도 확정하지 못합니다.
- 정보나루는 부가기호를 `addition_symbol`, 권차를 `vol` 로 **따로 줍니다.** 제목에서 권차를
  파싱하기 전에 `vol` 을 먼저 보세요. **군집화에 이 값을 반드시 넘겨야 합니다**
  (`Candidate.of` 와 `BookRecord.of` 의 `volNo` 인자). 실제로 이것을 빠뜨린 채 돌고
  있었는데, 「레미제라블」처럼 낱권 표제가 다섯 권 모두 같은 책은 권차가 전부 null 이 되어
  **한 저작으로 합쳐졌습니다.** 그러면 두 가지가 한꺼번에 무너집니다. 화면에 하나만 나와서
  **낱권을 고를 수 없고**, 그 하나의 ISBN 목록에 다섯 권이 다 들어가므로 **1권만 있는
  도서관이 「레미제라블 있음」으로 나옵니다.** 찾는 권이 없는데 있다고 답하는 것입니다.
- **권차로 갈라 놓았으면 표제에도 드러내야 합니다**(`SearchDocBuilder.displayTitle`).
  저작은 갈라 놓았는데 표제가 다섯 줄 모두 「레미제라블」이면 사용자는 어느 것이 몇 권인지
  알 수 없고, 갈라 놓은 보람이 없습니다. 표제 끝이 이미 그 숫자면 붙이지 않습니다. 다른 소스가 붙을 수 있으므로 ISBN 에 부가기호가 섞이는 경우의 방어(13자리로 자르고 체크디지트 검증)는 그대로 둡니다.
- 세트 ISBN은 낱권 저작에 직접 병합하지 않고 `bib_set_member`로 연결만 합니다.
- **누락이 오병합보다 낫습니다.** 규칙을 조일지 풀지 고민되면 조입니다. 누락은 눈에 보이지만 오병합은 보이지 않습니다.
- 서명과 저자 필드에는 KORMARC 245의 구분 기호(`/`, `=`, `:`, `;`)가 섞여 들어옵니다. **정규화 전에 먼저 분리하세요.**

---

## 파이프라인

- **소장 캐시는 만료시켜 버리지 않습니다.** 30일이 지난 항목도 조회 시각과 함께 그대로 내보내고 배경에서 갱신합니다. 소장 정보가 없어 빈 화면을 보여주는 것보다 언제 기준인지 밝히는 편이 낫습니다.
- **소장 캐시가 메모리에 있고 파일로 살아남습니다**(`CachingHoldingsClient`,
  `HoldingCacheStore`). DB 는 아직 붙지 않았습니다. 아래 「소장 캐시」 절을 보세요.
- **서지 적재는 `COPY`로 합니다.** 150만 행을 한 행씩 `INSERT`하면 한 시간이 넘고 `COPY`로는 1분 안쪽입니다.
- 멱등성은 `ingest_run`의 `UNIQUE (source_code, scope_key, input_sha256)`으로 확보합니다.
- **검증하지 않은 색인은 절대 전환하지 않습니다.** `IndexVerifier` 를 통과한 뒤에만 `promote_search_doc` 을 부릅니다. 실패하면 새 테이블을 버리고 기존 뷰를 그대로 둡니다. 오래된 색인이 서비스되는 것이 망가진 색인이 서비스되는 것보다 낫습니다.
- **색인 문서의 `searchText` 에는 저작에 묶인 모든 판본의 표기를 모읍니다.** 판본마다 저자 표기가 다른 경우가 흔한데(「조앤 K. 롤링」과 「J.K.롤링」), 대표 판본의 표기만 넣으면 다른 표기로 검색했을 때 걸리지 않습니다.
- 어댑터는 본 테이블에 직접 쓰지 않습니다. 소스 고유 필드가 도메인 모델이나 색인 스키마에 나타나면 안 됩니다.
- 나중에 벌크 적재 어댑터를 켤 때는 CSV **헤더 지문이 다르면 실행을 실패시킵니다.** 컬럼이 밀린 채 적재되면 나중에 발견할 수 없습니다. 인코딩은 UTF-8을 먼저 시도하고 CP949로 대체합니다.

### 소장 캐시

**열쇠는 `(ISBN, 시도)` 이고, 그보다 넓게 잡으면 안 됩니다.** `libSrchByBook` 은 도서관을
지정해 묻는 것이 아니라 그 시도에서 그 책을 소장한 도서관을 통째로 돌려주므로, 한 번 받아
두면 어떤 도서관 조합으로 물어보든 교집합만 다시 계산하면 됩니다. 그래서 캐시가
`HoldingsLookup` **안쪽**(`CachingHoldingsClient`)에 있습니다. 바깥에 두면 「저작 + 고른
도서관」이 열쇠가 되어 **도서관을 한 곳만 더 골라도 통째로 빗나갑니다.** (ISBN, 시도) 쌍은
유한하지만 도서관 조합은 그렇지 않습니다.

**빈 답은 기억하고 실패는 기억하지 않습니다.** 「그 시도에는 소장한 곳이 없다」는 정상적인
답이라, 남기지 않으면 소장하지 않은 책을 물어볼 때마다 호출이 그대로 나갑니다. 반대로
실패를 기억하면 정보나루가 잠깐 흔들린 뒤에도 한동안 「확인 불가」가 이어집니다.

**도서관부호 문자열을 공유합니다**(`CachingHoldingsClient.share`). 부호는 여섯 자리인데
자바 String 객체 하나가 40바이트를 넘게 쓰므로, 파싱한 문자열을 그대로 들고 있으면 같은
부호가 수만 번 중복 저장됩니다. 실측입니다.

| 항목 수 | 소장 | 새로 만들기 | 공유 |
|---|---|---|---|
| 1만 | 179곳 | 90.9MB | **8.9MB** |
| 10만 | 179곳 | **900MB 초과(OOM)** | **89.9MB** |

`CachingHoldingsClientTest` 가 `assertSame` 으로 고정합니다. 값이 같은지가 아니라 **같은
객체인지**를 봅니다. **이 실측이 DB 를 붙이지 않기로 한 근거입니다.** 공유만 하면 캐시가
수십 MB 라, 1GB 기계에 PostgreSQL 을 올리거나 VM 을 늘릴 이유가 없습니다.

**상한을 넘겨도 통째로 비우지 마세요.** 파일 스냅샷이 생기면서 그 한 번이 여러 배포에
걸쳐 쌓은 것을 날리게 되었습니다. 오래된 것부터 4분의 1씩 덜어 냅니다.

**스냅샷은 주기적으로도 저장해야 합니다.** 배포 스크립트가 컨테이너를 바꿀 때 쓰는
`docker rm -f` 는 SIGKILL 을 바로 보내므로 **종료 훅이 돌 기회가 없습니다.** 종료 훅만
믿으면 배포마다 그날 쌓은 캐시가 통째로 사라집니다. `deploy.sh` 가 `docker stop` 으로 유예를
주도록 바꾸었지만, 그것과 별개로 `save-interval-ms` 마다 저장합니다.

**그리고 파일은 컨테이너 밖에 있어야 합니다.** 컨테이너 파일 시스템은 컨테이너와 함께
사라지므로 볼륨이 없으면 주기 저장도 소용없습니다. 이름 있는 볼륨(`wimb-data`)을 쓰는 것은
권한 때문입니다. 컨테이너가 루트가 아닌 `wimb` 로 도는데 **호스트 디렉터리를 붙이면 그
디렉터리의 소유권이 그대로 보여 쓰기가 막힙니다.** 이름 있는 볼륨은 도커가 처음 만들 때
이미지의 `/data` 소유권을 복사합니다.

**이미 만료된 항목은 되살리지 않습니다.** 어차피 다음 조회에서 다시 부를 것이라, 들여
놓아 봐야 상한만 차지합니다.

**`/api/status` 의 `holdingCacheEntries` 를 보세요.** 재배포 뒤 호출이 줄지 않을 때, 이
숫자가 없으면 스냅샷을 못 되살린 것인지 저장이 안 된 것인지 캐시가 원래 안 도는 것인지
구별할 수 없어 추측하게 됩니다. **배포 직후 이 값이 0이면 스냅샷을 잃은 것입니다.**

---

## 데이터 취급 시 주의

- **전국도서관표준데이터에는 도서관부호가 없습니다.** 도서관명과 주소로 대조해야 하고, `confirmed = true`인 연결은 재실행해도 덮어쓰지 않습니다.
- **`libSrch` 응답에는 관종(도서관유형) 항목이 없습니다.** 매뉴얼 1절의 응답 명세를 확인한
  결과 `libCode libName address tel fax latitude longitude homepage closed operatingTime
  BookCount` 가 전부입니다. **그래서 관종으로 거르는 코드를 쓸 수 없습니다.** 계획서는
  전국도서관표준데이터의 `도서관유형` 으로 공공도서관만 남기려 했지만, 위경도를 정보나루가
  주는 덕에 표준데이터를 아예 쓰지 않게 되면서 그 필드도 함께 사라졌습니다.
  거르고 싶어지면 표준데이터를 이름과 주소로 다시 대조해야 하는데, 그것이 계획에서 가장 큰
  위험으로 꼽혔던 바로 그 단계입니다. **목록이 실제로 지저분한지 먼저 확인한 뒤에 판단하세요.**
- **작은도서관도 정보나루 목록에 있습니다.** 예전에 「없다」고 적어 두었는데 사실이
  아니었습니다. 실제 목록 **1,619곳 가운데 385곳**이 이름에 「작은도서관」을 달고 있고,
  그중 「가온 작은도서관」이 소장 조회 결과에도 실제로 잡혔습니다. 전국의 작은도서관이
  빠짐없이 있다는 뜻은 아니지만, **목록에 있는 곳은 소장까지 조회됩니다.** 없다고 단정해
  두면 사용자가 찾는 도서관을 우리가 미리 지우게 됩니다.
- 저장은 `timestamptz`로 UTC, 표시는 전부 `Asia/Seoul`입니다. 일일 호출 예산의 날짜 경계도 한국 시각 기준입니다.

---

## 외부 서버에 대한 예의

- 링크 점검 배치의 동시 요청 상한과 요청 간격을 임의로 올리지 마세요. 같은 호스트에 동시 1개, 전체 6회/분, 도서관당 주 1회입니다.
- `robots.txt`를 확인하고 거부된 경로는 점검하지 않습니다.
- 정보나루 다운로드 화면을 크롤링하지 않습니다.

---

## 비밀 정보

API 키는 `.env`로 관리하고 저장소에 커밋하지 않습니다. `.env.example`에는 키 이름만 둡니다.

필요한 키는 `.env.example`을 참고하세요.

### 설정과 비밀을 구분하세요

**브라우저에 도달해야 동작하는 값은 애초에 비밀이 될 수 없습니다.** `.env` 에 있다고 해서
전부 비밀인 것이 아닙니다.

| | `VITE_API_BASE` | `D4L_AUTH_KEY` |
|---|---|---|
| 누가 알아야 하나 | 브라우저가 그 주소로 요청을 보내야 합니다 | 서버만 |
| 숨길 수 있나 | **불가능**. 빌드할 때 번들에 박히고 네트워크 탭에 그대로 보입니다 | 가능 |
| 새어 나가면 | 아무 일도 없습니다 | 남이 우리 몫의 호출 한도를 씁니다 |

그래서 **`frontend/.env.production` 은 일부러 커밋되어 있습니다.** 규칙 위반이 아니므로
지우지 마세요. 저장소에 두면 값이 코드와 함께 기록에 남고, 재배포를 잊어 번들에 예전 주소가
남는 사고도 없어집니다.

한편 주소가 공개된다는 데서 따라오는 것이 하나 있는데, **비밀 문제가 아니라 접근 통제
문제**입니다. 우리 API 는 인증이 없어서 주소를 아는 사람은 누구나 부를 수 있고 그 호출이
정보나루 예산을 갉아먹습니다. 그래서 **주소별 호출 제한**을 붙였습니다(`RateLimit`,
`RateLimitInterceptor`). 주소를 감추는 것은 답이 아닙니다.

### 주소별 호출 제한

**단위는 요청 수가 아니라 무게입니다.** 요청마다 정보나루를 부르는 횟수가 크게 다릅니다.
`/api/status` 한 번은 한 번도 부르지 않고 `/api/check/resolve` 한 번은 줄마다 두세 번씩
불러 백 번을 넘길 수 있습니다. 같은 한 번으로 세면 **가장 비싼 요청이 가장 싸게
통과합니다.** 무게표는 `RateLimitInterceptor.costOf` 에 있고, 요청 본문을 읽기 전에
정해야 하므로 상한에 가깝게 잡습니다.

**분당 한도와 하루 한도를 둘 다 둡니다.** 앞은 몰아치는 것을, 뒤는 한 주소가 천천히
하루치를 다 가져가는 것을 막습니다. 분당 한도만 두면 후자를 못 막습니다. 분당 400 을 하루
내내 쓰면 57만이라 하루 예산 25,000 은 한 시간이면 사라집니다. **막힌 이유도 갈라
말합니다.** 「잠시 뒤 다시」와 「오늘은 끝」은 사람이 할 일이 다른데, 한 문구로 뭉뚱그리면
하루치를 다 쓴 사람이 30초마다 다시 눌러 보게 됩니다.

**`X-Forwarded-For` 는 마지막 값을 씁니다. 첫 값이 아닙니다.** 이 헤더는 프록시를 지날
때마다 뒤에 덧붙습니다(Caddy 도 그렇습니다). 클라이언트가 지어내 보낸 값은 앞쪽에 남고
우리 프록시가 실제로 본 주소가 맨 뒤에 붙으므로, **흔히 하는 대로 첫 값을 쓰면 아무나
헤더 한 줄로 한도를 빠져나갑니다.** 반대로 `behind-proxy` 를 끄고 프록시 뒤에 두면
**모든 이용자가 프록시 주소 하나로 뭉뚱그려져 서로의 한도를 나눠 씁니다.** 그때 화면에
쏟아지는 「확인 불가」는 정보나루 장애와 증상이 같습니다.

**IPv6 는 /64 로 묶습니다.** 회선 하나에 /64 가 통째로 딸려 오므로 주소 하나씩 세면 같은
사람이 주소만 바꿔 가며 한도를 무한히 늘릴 수 있고, 그러면 IPv6 이용자에게만 한도가 없는
셈이 됩니다.

**필터가 아니라 인터셉터입니다.** 거절 사유를 `ApiErrorAdvice` 로 흘려보내 다른 오류와
같은 모양(`code`, `reason`)으로 내보내기 위해서입니다. 필터에서 던지면 그 앞에서 끊겨
화면에는 「API 오류 429」만 남습니다. **`Retry-After` 도 함께 보냅니다.** 언제가 나중인지
말해 주지 않으면 사람도 프로그램도 곧바로 다시 두드립니다.

**CORS 사전 요청(OPTIONS)은 세지 않습니다.** 정보나루를 한 번도 부르지 않는데 Spring 은
사전 요청에도 등록된 인터셉터를 그대로 태웁니다. 그냥 두면 `/api/check/resolve` 는 값이
두 배가 되고, 더 나쁜 것은 **막혔을 때**입니다. 사전 요청을 거절하면 CORS 헤더가 붙기 전에
끊기므로 브라우저가 그것을 CORS 오류로 보고하고, 화면에는 서버가 실어 보낸 이유 대신
「API 서버에 연결하지 못했습니다」가 뜹니다. **우리가 계속 갈라 놓으려는 「서버가 없다」와
「서버는 있는데 물어보지 못했다」가 여기서 뒤바뀝니다.**

**화면은 429 를 「미소장」이 아니라 「확인 불가」로 그려야 합니다.** 물어보지 못한 것이지
그 책이 없다는 뜻이 아닙니다. 소장 조회는 실패를 `unreadable` 로 받으므로 지금은 그대로
확인 불가가 됩니다. 이 구분이 무너지면 실제로 소장한 책을 없다고 답하게 됩니다.

**축출은 오래된 것부터 4분의 1씩입니다.** 통째로 비우면 그 순간 모두의 한도가 함께 풀려,
마침 몰아치던 주소가 상한을 다시 채우며 계속 통과합니다. 소장 캐시와 같은 방식입니다.

**`/api/status` 의 `rateLimitedClients` 를 보세요.** 호출이 갑자기 줄었을 때 이 숫자가
없으면 사람이 안 오는 것인지 우리가 막고 있는 것인지 구별할 수 없습니다. **이 값이 1 인데
이용자가 여럿이면 `behind-proxy` 설정을 의심하세요.** 주소 자체는 내보내지 않습니다.

---

## 화면 밖의 정적 페이지

`frontend/public/` 에 앱과 따로 도는 것이 둘 있습니다. 둘 다 **도메인이 바뀌면 함께
고쳐야 하는 것**이라 잊기 쉽습니다.

### 개인정보처리방침은 코드와 맞아야 합니다

`frontend/public/privacy.html` 에 있고, **React 앱 밖의 정적 페이지입니다.**
자바스크립트가 돌지 않아도 API 서버가 죽어 있어도 열려야 하기 때문입니다.
개인정보처리방침이 「지금은 볼 수 없습니다」가 되는 것은 그 자체로 말이 되지 않습니다.
`vercel.json` 의 `/privacy` 규칙이 `/(.*)` **앞에** 있어야 하고, `vercel.json` 에 주석
삼아 모르는 키를 넣으면 스키마 검사에 걸려 배포가 통째로 실패합니다.

**개발자 말로 쓰지 마세요.** 읽는 사람은 이 도구를 쓰는 사람이지 만드는 사람이 아닙니다.
「서버」, 「쿠키」, 「로컬 저장소」, 「IPv6 /64」, 「디스크」, 「access log」 같은 말은 뜻이
전해지지 않고, 뜻이 안 전해지면 그 문서는 있으나 마나입니다. **다만 쉽게 쓰느라 사실을
흐리면 안 됩니다.** 「저희 쪽으로 보내지 않습니다」는 되고 「안전하게 관리합니다」는 안 됩니다.
뒤엣것은 아무 말도 하지 않으면서 무언가 말한 것처럼 들립니다. 파일 머리 주석에 같은 규칙이
적혀 있습니다.

**문의처를 비워 두지 마세요.** 개인정보처리방침의 실질은 「이상하면 여기로 말하세요」인데
그 자리가 없으면 문서의 절반이 빠집니다. 지금은 「궁금한 점이 있으시면」 절에 이메일
주소가 들어가 있습니다. 깃허브 이슈로 되돌리지 마세요. 계정이 있어야 하고 쓴 글이 그대로
공개되므로 개인정보에 관해 물어볼 곳으로는 맞지 않습니다.

**「마지막으로 고친 날」도 지금은 없습니다.** 아직 초안이라 고친 날짜라고 적을 것이 없고,
공개하지도 않은 문서에 날짜만 있으면 이미 시행 중인 것처럼 읽힙니다. **공개할 때 그날
날짜로 한 줄을 넣고, 「이 문서를 고치면」 절에 「고친 날짜를 맨 위에 적습니다」를 함께
되돌리세요.** 한쪽만 되돌리면 없는 날짜를 있다고 말하는 문서가 됩니다. 문의처는 채웠으니
**공개 전에 남은 것은 날짜 하나**입니다.

**거기 적힌 것은 전부 실제 동작입니다.** 아래를 바꾸면 그 문서도 함께 고치세요. 어느
한쪽만 바뀌면 그 문서는 거짓말이 됩니다.

- 브라우저 저장소 항목이 늘거나 이름이 바뀔 때(`selectionStorage.ts`)
- **위치를 서버로 보내게 될 때.** 지금은 브라우저 안에서 거리 계산에만 쓰고 보내지 않으며,
  그 사실을 문서가 명시하고 있습니다
- 서버로 보내는 값이 늘 때(`api.ts`)
- IP 보관 방식이 바뀔 때(`RateLimit`)
- **광고를 붙일 때.** 문서가 「광고는 없다」고 못박아 두었으므로 붙이기 전에 먼저 고쳐야
  합니다. **방문자 수 세기는 이미 붙였습니다.** 아래 절을 보세요

**광고를 검토한다면 정보나루 약관을 먼저 보세요.** 실제로 열어 보니 **광고를 콕 집어
금지하는 조항은 없고 공공누리 표시도 없습니다.** 그런데 광고보다 앞서 걸리는 것이
있습니다. 제11조 ③ 이 「도서관 소유의 콘텐트를 제3자가 허락 없이 다른 사이트에 사용 또는
인용하는 것」을 금지하고, 제12조 ① 5호가 「서비스를 통해 얻은 정보를 사전 승낙 없이
서비스 이용 외의 목적으로 복제하거나 제3자에게 제공하는 행위」를 제한합니다. **문자 그대로
읽으면 광고 이전에 이 서비스 자체가 사전 승낙 대상입니다.** 다만 그 약관은 회원가입
표준약관 문구라 Open API 를 위해 쓰인 것이 아니고, API 를 여는 목적과 어긋나 있습니다.
**그 틈을 해석으로 메우지 말고 `libdata@korea.kr` 에 물어 답을 `docs/가정과-검증상태.md`
2-11 에 남기세요.** 약관 전문은 사이트 하단이 아니라 **회원가입 페이지(`/joinPage`)** 안에
있습니다. 하단 링크는 주소가 없는 모달이고 `/terms` 류는 전부 404 입니다.

### 방문자 수는 세되 주소는 떼고 셉니다

Vercel 웹 분석(`@vercel/analytics`)이 `frontend/src/main.tsx` 에 붙어 있습니다. 기기에 남는
표시가 없고(쿠키도 브라우저 저장소도 쓰지 않습니다), 스크립트도 남의 도메인이 아니라 같은
주소(`/_vercel/insights/script.js`)에서 옵니다.

**`beforeSend` 로 주소의 물음표 뒤를 떼고 보냅니다. 이것을 지우지 마세요.** 고른 도서관이
`?libs=` 로 주소에 실리는 것이 이 도구의 공유 기능인데(`domain/selectionUrl.ts`), 선택이
바뀔 때마다 `App` 이 `replaceState` 로 주소를 고쳐 씁니다. 그래서 그대로 두면 **체크 한 번에
한 줄씩 「어느 도서관을 골랐는지」가 Vercel 로 넘어갑니다.** 개인정보처리방침이 「고른
도서관은 이 기기에만 남는다」고 적어 둔 것과 정면으로 어긋나고, **오류도 경고도 나지 않아
새는 것을 알 방법이 없습니다.** 떼어도 잃는 것은 없습니다. 화면이 하나뿐이라 셀 것은 어차피
경로뿐이고, 오히려 같은 화면이 선택마다 다른 주소로 흩어지는 것을 막아 줍니다.

`src/__tests__/analyticsPrivacy.test.ts` 가 셋을 함께 붙들어 둡니다. 의존성과 진입점이 따로
노는 것, 문서가 「분석 도구는 없다」고 말한 채로 남는 것, `beforeSend` 가 빠지는 것입니다.
**`siteUrl.test.ts` 와 같은 종류라 값이 무엇인지는 보지 않고 서로 어긋나는지만 봅니다.**
세는 도구를 떼는 것도 정상적인 일이라 막지 않습니다.

### 공유 미리보기 그림은 원본과 함께 둡니다

고른 도서관을 주소로 공유하는 것이 이 도구의 기능인데, 미리보기가 없으면 카카오톡이나
슬랙에 붙였을 때 주소만 덩그러니 남습니다. **검색 순위가 아니라 실제 사용에 관한
것입니다.**

- **그림만 두지 마세요.** 원본이 `frontend/tools/og-image.html` 이고
  `node tools/make-og-image.mjs` 로 다시 만듭니다. 원본이 없으면 색 하나 바꾸려 해도
  손댈 수 없는 그림이 저장소에 남습니다.
- **크로미움을 직접 부르지 마세요.** 헤드리스 크로미움은 `--window-size` 로 요청한 높이보다
  **85px 적게 그리면서** 남는 자리를 배경색으로 채운, 요청한 크기의 파일을 내놓습니다.
  잘렸다는 것이 눈에 띄지 않아 아래쪽에 둔 것이 조용히 사라집니다. 실제로 띠 하나가
  그렇게 없어졌고 CSS 를 세 번 고쳐 본 뒤에야 렌더러 쪽 문제인 것을 알았습니다.
  그 보정이 스크립트에 들어 있습니다.
- **한글 글꼴이 없는 기계에서 만들면 글자가 네모로 나옵니다.** 스크립트는 그것까지는
  알지 못하므로 만든 뒤 반드시 눈으로 확인하세요.
- **미리보기 태그의 주소는 절대 경로여야 합니다.** 상대 경로는 미리보기를 만드는 쪽에서
  찾지 못하고, 사이트맵의 `<loc>` 도 절대 경로가 규칙입니다. 그래서 **도메인이 다섯 군데에
  글자로 박혀 있습니다.** `index.html` 의 네 줄(`canonical`, `og:url`, `og:image`,
  `twitter:image`), `privacy.html` 의 `canonical`, `sitemap.xml` 의 `<loc>`,
  `robots.txt` 의 `Sitemap:` 입니다. 하나를 빠뜨리면 **아무것도 깨지지 않은 채 조용히
  예전 주소를 가리킵니다.** `src/__tests__/siteUrl.test.ts` 가 서로 어긋나면 실패합니다.
  값이 무엇인지는 보지 않고 **서로 같은지**만 봅니다. 도메인을 바꾸는 것은 정상적인 일이고,
  막아야 하는 것은 그중 하나만 바뀌는 것입니다.
- `canonical` 을 지우지 마세요. 고른 도서관이 주소에 `libs=` 로 붙으므로, 없으면 도서관
  조합마다 내용이 같은 페이지가 끝없이 만들어집니다.

### 크롤러 규칙은 사이트와 API 양쪽에 따로 있습니다

**`frontend/public/robots.txt` 는 거의 다 열어 둡니다.** 막을 것이 없습니다. 다만
**`?libs=` 와 `/privacy.html` 을 일부러 막지 않았습니다.** 둘 다 중복이라 막고 싶어지는데,
**막으면 오히려 나빠집니다.** 크롤러가 페이지를 읽지 못하면 그 안의 `canonical` 도 못 읽어서
어느 쪽이 정본인지 알려 줄 방법이 사라집니다. 중복은 `robots.txt` 가 아니라 `canonical` 로
정리합니다.

**`backend/src/main/resources/static/robots.txt` 는 반대로 거의 다 막습니다.** 여기서
막는 이유는 중복 색인이 아니라 **호출**입니다. 화면의 도서관 링크가
`{API}/api/go/{도서관부호}` 로 나가는 `<a>` 태그라, **화면을 렌더링하는 크롤러가 그것을
따라갑니다.** 소장 도서관이 스무 곳이면 링크도 스무 개이고 하나하나가 호출 제한을 깎습니다.
`/api/diagnose` 는 열 때마다 정보나루를 한 번 부릅니다.

**그런데 `/api/libraries` 는 열어 두어야 합니다.** 구글은 자바스크립트를 실행해 화면을 그려
보는데, 이 호출을 막으면 도서관 목록을 받지 못해 **「API 서버에 연결하지 못했습니다」 배너가
뜬 화면을 색인합니다.** 그 문장이 검색 결과에 실리는 것이 크롤링 몇 번보다 나쁩니다. 이
호출은 서버가 메모리에 담아 둔 목록으로 답하므로 정보나루를 부르지 않습니다.
`ApiRobotsTest` 가 이 둘을 함께 고정합니다. **파일만 두고 확인하지 않으면 조용히 404 가
되고, 그러면 크롤러는 막는 규칙이 없는 것으로 보고 그대로 훑습니다.**

**사이트맵에 `lastmod` 를 넣지 마세요.** 손으로 적으면 코드가 바뀔 때마다 같이 고쳐야 하는데
그것이 지켜지지 않아 곧 거짓이 되고, 배포할 때 자동으로 찍으면 내용이 그대로인 날에도
바뀌었다고 말하게 됩니다. 둘 다 틀린 날짜라 아예 말하지 않는 쪽을 골랐습니다.
`changefreq` 와 `priority` 는 구글이 보지 않습니다.
